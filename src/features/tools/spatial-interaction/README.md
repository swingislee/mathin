# 立体教具共用交互

状态：2026-09-19 开发端增量，待产品负责人实际手感验收。范围是教具空间的操作，不增加题目、提交或判题。

## 操作约定

- 同一个左键／单指手势按命中位置分流：命中当前动作的对象或手柄则操作对象，未命中则交给 `SpatialCameraRig` 旋转视角。右键平移和滚轮缩放保持原控件规则。
- 正常观察状态同时提供对象操作。专用平移按钮改变的是空白手势；移动、折叠、剪棱等动作不会以 `navigationMode="object"` 关闭整块画布的旋转。
- 直接拖物体按起拖方向选择可见轴，一次手势锁定该轴；XYZ 箭头明确指定轴。轻点选中，拖动另一物体直接提交它的终点，选择不抢先发送课堂写入。
- 已编组的结构／Soma 拼块保持刚体；立方体普通观察选择已编组或面相连部分，原移动、多选工具仍可操作单位块。
- `SpatialRotationControls` 在对象旁提供世界轴与正负 90° 入口，并显示方向圆弧。精确面板调用同一领域操作，动画终点继续满足领域约束。
- 教学变化保留过程；减少动态效果时采用短过渡。首个绘制帧开始计时，编译或绘制等待不消耗动画。动态帧只负责显示，不保存为数学参数。
- 手动拖动实时预览、松手写一次。`useSpatialDirectCommit` 在课堂回执前保留已经展示的终点，失败或外部状态替换则返回权威现场。相同回执不重播；按钮动作等待权威状态后播放。
- 对象写入忙碌和相机控制分开。教师可在对象动画或持久化期间观察；展示端原有只读权限保持不变。

## 当前库核查

| 教具／能力 | 对象操作与观察 | 共用接入／保留的领域操作 |
| --- | --- | --- |
| 立方体结构、投射 | 直接移动相连结构／分组、单位块多选、对象旁转动；空白旋转 | 共用轴拖动、动作计时与转动入口；投射继续读取同一展示帧。搭建、挖除、层显隐、标色、单面染色、透明、标注、标数、分割及撤销保持原数学操作 |
| Soma | 自由拼搭中直接拖整宝，转动整宝；空白旋转 | 同一套移动和旋转控件；`useSpatialPresentation` + 刚体姿态动画。1–7 宝选择、单块观察和 24 个合法朝向保持 v1 合同 |
| 骰子 | 移动骰子、对象旁转动；空白旋转 | 共用真实网格拾取和轴拖动；转动继续用原课堂 tween。移面、相对面观察、动态透视、点数显隐、面色与透明、受控滚动和投掷保持各自动作 |
| 正方体展开图、剪棱展开 | 命中纸面折叠／展开，空白旋转；剪棱轻点与拖动观察区分 | 保留已验收的折纸手势和可中断教学动画；11 图转换、逐面展开、移面、标注等不改数学路径 |
| 自由折纸、长方体／三棱柱展开图 | 共用纸面折叠拾取；空白旋转 | 复用 `CubeNetFoldInteraction` 与教学播放；折线、面数与角度范围由各自领域校验 |
| 立体观察 | 实际网格拾取，移动实体、世界轴转动、参数与透明过渡；空白旋转 | 复用动作入口和表现层；面／棱／顶点选择期间保留专用拾取，不被移动抢走 |
| 截面、测量 | 拖切平面平移／倾斜，空白旋转；测量仍关联同一实体 | 保留截面直接手柄、主舞台关联图和精确输入；单位填充、分层、面展开等采用原测量过程 |
| 容积比较 | 视角可自由观察，液量／尺寸／倒水动画保留 | 复用实体画布和相机。器皿当前是固定排布的比较现场，自由摆放或任意倾斜液体属于新增领域能力，本增量不假装已有 |

## 接入与验证边界

`policy.ts` 管理共用动作计时与普通操作状态；`picking.ts` 读取实际表面，适配尺寸、转角、透明与裁切。现有 `CubeMoveHandles`／`bindCubeAxisDrag` 作为共享移动入口复用，领域提供选择范围、落点有效性和提交函数。

`useSpatialPresentation` 接收严格目标的稳定键与领域插值。`rigid-motion.ts` 使用四元数和固定轴圆弧，Soma 的格点模型与显示姿态分离，旋转中不让各小方块独立滑动。旧立方体历史仍由既有语义旋转解析，复用相同动作计时，不改发布版本。

定向检查覆盖鼠标／单指命中分流、自动轴与一次提交、实际网格拾取、7 × 24 个 Soma 姿态的精确对应、真实 R3F 中间转角、回执去重与失败回退，以及展开图、截面、测量、容积和课堂工作台回归。它们证明覆盖的代码合同，不替代课堂手感验收。内容版本、数据库和生产环境保持不变。
