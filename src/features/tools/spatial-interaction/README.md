# 立体教具共用交互

状态：2026-09-20 开发端增量，待产品负责人实际手感验收。范围是教具空间的操作，不增加题目、提交或判题。

## 操作约定

- 同一个左键／单指手势按命中位置分流：命中当前动作的对象或手柄则操作对象，未命中则交给 `SpatialCameraRig` 旋转视角。右键平移和滚轮缩放保持原控件规则。
- 正常观察状态同时提供对象操作。专用平移按钮改变的是空白手势；移动、折叠、剪棱等动作不会以 `navigationMode="object"` 关闭整块画布的旋转。
- Soma 试用连续二维拖动：默认沿 X-Z 桌面保持高度，移动面板可临时切到屏幕平面；松手才对齐网格。其他已接入工具暂留原起拖选轴规则，待本次手感验收后推广。XYZ 箭头明确指定轴；轻点选中，拖动另一物体直接提交它的终点，选择不抢先发送课堂写入。
- 已编组的结构／Soma 拼块保持刚体；立方体普通观察选择已编组或面相连部分，原移动、多选工具仍可操作单位块。
- `SpatialRotationControls` 在对象旁提供世界轴与正负 90° 入口，并显示方向圆弧。精确面板调用同一领域操作，动画终点继续满足领域约束。
- 模式按钮和参数面板共用 `useSpatialToolState`：切换操作时同步更换拾取方式；再点当前操作、关闭面板或在工作台内按 Esc，返回常规直接操作。显示开关、一次性命令与操作模式分开；关闭参数不隐藏舞台上仍在展示的截面手柄。
- `SpatialCameraRig` 统一保护轻点命令：超过移动阈值后即使拖回起点，也不会误触标色、删除或剪棱；双指、右键、取消和已由对象接管的拖动同样不会留下点击。
- `SpatialRollControls` 与 `SpatialRollButtons` 提供 X/Z 正负方向翻滚，指向对象显示支撑棱与中心路径。每次绕行进方向的底部支撑棱翻 90°，不是平移再原地旋转。
- 教学变化保留过程；减少动态效果时采用短过渡。首个绘制帧开始计时，编译或绘制等待不消耗动画。动态帧只负责显示，不保存为数学参数。
- 手动拖动实时预览、松手写一次。`useSpatialDirectCommit` 在课堂回执前保留已经展示的终点，失败或外部状态替换则返回权威现场。相同回执不重播；按钮动作等待权威状态后播放。
- 对象写入忙碌和相机控制分开。教师可在对象动画或持久化期间观察；展示端原有只读权限保持不变。

## 当前库核查

| 教具／能力 | 对象操作与观察 | 共用接入／保留的领域操作 |
| --- | --- | --- |
| 立方体结构、投射 | 直接移动相连结构／分组、单位块多选、对象旁转动；空白旋转 | 共用轴拖动、动作计时与转动入口；投射继续读取同一展示帧。搭建、挖除、层显隐、标色、单面染色、透明、标注、标数、分割及撤销保持原数学操作 |
| Soma | 连续拖整宝；Shift 拖自由旋转，触屏点旋转后拖对象；空白旋转 | 共用平面手势与 Arcball、真实刚体拾取、精确轴控件；v2 默认在邻近合法朝向松手吸附，可关闭以保留自由角度。旧 v1 保持离散朝向 |
| 骰子 | 移动骰子、对象旁转动；空白旋转 | 共用真实网格拾取和轴拖动；转动继续用原课堂 tween。移面、相对面观察、动态透视、点数显隐、面色与透明、受控滚动和投掷保持各自动作 |
| 正方体展开图、剪棱展开 | 命中纸面折叠／展开，空白旋转；剪棱轻点与拖动观察区分 | 保留已验收的折纸手势和可中断教学动画；11 图转换、逐面展开、移面、标注等不改数学路径 |
| 自由折纸、长方体／三棱柱展开图 | 共用纸面折叠拾取；空白旋转 | 复用 `CubeNetFoldInteraction` 与教学播放；折线、面数与角度范围由各自领域校验 |
| 立体观察 | 实际网格拾取，移动实体、世界轴转动、参数与透明过渡；空白旋转 | 复用动作入口和表现层；面／棱／顶点选择期间保留专用拾取，不被移动抢走 |
| 截面、测量 | 拖切平面平移／倾斜，空白旋转；测量仍关联同一实体 | 保留截面直接手柄、主舞台关联图和精确输入；单位填充、分层、面展开等采用原测量过程 |
| 容积比较 | 视角可自由观察，液量／尺寸／倒水动画保留 | 复用实体画布和相机。器皿当前是固定排布的比较现场，自由摆放或任意倾斜液体属于新增领域能力，本增量不假装已有 |

以上工作台的操作按钮均接入共同模式／面板规则；展开方式总入口只切换子工作台，不叠加另一套拾取模式。投射继续复用立方体工作台的交互规则。

本次翻滚接入：当前版立方体结构、自由拼搭的 Soma、骰子，以及立体观察中的正方体／长方体。立方体与 Soma 保持整组选中对象的刚体和精确格点，长方体按真实尺寸翻动，骰子沿用已有受控滚动命令与点数轨迹。缺少支撑棱、超出领域边界或路径被其他对象阻挡时停用该方向。曲面滚动、任意倾斜实体的接触演化不在本次范围。投射的冻结 v1 仍不接受带旋转标注的状态，本次未为它开放翻滚。

## 新增操作的接入步骤

1. 先区分操作模式、显示开关与一次性命令。需要拾取对象／面／棱的参数面板，在 `SpatialToolDefinition.panels` 中声明所需模式；使用 `controls.bindings` 限定键盘退出与状态反馈的实例范围。
2. 按命中对象接管指针；复用相机的空白手势和点击保护。对舞台上的直接手柄与精确输入，提供同一个领域目标函数。
3. 准确终点由领域模型校验。移动、旋转、翻滚复用现有控件和刚体表现层；需要旋转吸附时用 `rotation-snap.ts` 查找邻近允许朝向，领域校验位置／碰撞，返回 `snapped` 候选。拖动保留自由帧，松手由共用控制器平滑收拢，不要求另点对齐。翻滚使用 `rolling.ts` 的接触棱规划，并由领域检查合法落点和路径。对旧版本明确关闭不支持的动作。
4. 仅提交终点／语义命令；本地预览与课堂回执沿用现有桥接。为新动作补充中间帧、精确终点和撤销／复原用例。
5. 运行按钮交互、工作台接入检查及受影响领域测试。接入检查遍历所有使用空间工具栏的工作台，允许组合入口复用已接入的子工作台。手感和视觉继续由产品负责人实际验收。

## Soma 自由轨迹球（2026-09-20）

- 普通鼠标／单指拖动保持连续跟手，默认桌面平面不猜轴、不逐格跳动。侧看桌面无法可靠求交时提示转动视角或选择屏幕平面；切换只影响本次操作，不改变备课状态。
- 常规状态保留 Y 抬高手柄；移动面板显示 XYZ 精确手柄。平移与旋转轴分别保存为 UI 状态。电脑 Shift 拖拼块旋转；触屏点对象旁或右侧旋转按钮后直接拖拼块，再点返回常规操作，按钮本身不是摇杆。
- `arcball.ts` 按对象投影尺寸、抓取起点和当前点映射球面：中心附近翻转，边缘绕圈拧转。手势开始锁定相机、球心和半径，以起点计算总四元数，不累加欧拉角。球外拖动映射到圆周；显示圆环不扩大对象命中区。
- v2 支点采用源模型固定包围中心，转动中不随世界 AABB 变化。“旋转吸附（松手时）”提供自由、轻吸附 15°、标准 30°、强吸附 45°，默认标准，与顶部视角吸附独立。档位阈值统一定义于 `rotation-snap.ts`。接近 24 个网格朝向中的一个且最近格点能放下时，细虚线预告落位；松手用 160 ms 平滑收拢。自由档、远离目标或网格有冲突时保留合法自由末帧；自由末帧自身无效则回退。90° 按钮使用相同吸附策略，吸附后的格点姿态可直接翻滚。
- 平移与旋转预览复用 `SpatialLandingOutline`，从已有单位块棱边去重规则生成一像素虚线，不渲染填充面、不占用拾取。实际对象保持自由跟手，候选轮廓只说明松手终点。
- 网格落位保持中心尽量接近，每轴最多修正半格；半格等距优先保留源姿态的同一单元锚点，避免连续 90° 操作产生取整漂移。开关只影响后续操作，不改写已保存的姿态；显示网格开关也不改变数学约束。
- 自由姿态的网格仅作参考，允许绕中心转到参考平面下方；坐标范围、单位四元数与拼块之间的实际 OBB 碰撞由前后端共同检查。旧 v1 格点、地面与支点语义保持不变。
- 单指开始移动前加入第二指，将完整起点交给相机平移／缩放；对象已开始移动后保留第一指归属。松手只提交一次，160 ms 短过渡用于平移落位、邻近旋转吸附或无效落点回退；无修正时立即结束。取消、失焦、外部权威替换均不发布对象操作。
- `object-gesture-math.ts` 保留已验收的平面平移；`object-gesture-controller.ts` 管理设备归属和一次提交。`rigid-geometry.ts` 管理局部空间拾取与单位 OBB 碰撞。领域通过 `SpatialObjectInteraction` 提供选择范围、支点、包围半径、合法候选与提交，由 `CubeMoveHandles` 保持精确轴优先级。

当前是 Soma 的开发试用，不代表骰子、立方体结构和其他教具的默认手感已同步改动。自动检查覆盖数学、指针事件、真实 R3F 姿态和课堂回执；桌面、iPad 与大屏的实际手感待产品负责人验收。

## 接入与验证边界

`policy.ts` 管理共用动作计时与普通操作状态；`picking.ts` 读取实际表面，适配尺寸、转角、透明与裁切。现有 `CubeMoveHandles`／`bindCubeAxisDrag` 作为共享移动入口复用，领域提供选择范围、落点有效性和提交函数。

`useSpatialPresentation` 接收严格目标的稳定键与领域插值。`rigid-motion.ts` 使用四元数和固定轴圆弧，Soma 的格点模型与显示姿态分离，旋转中不让各小方块独立滑动。旧立方体历史仍由既有语义旋转解析，复用相同动作计时，不改发布版本。

定向检查覆盖鼠标／单指分流、一次提交、真实几何拾取、7 × 24 个旧姿态对应、Arcball 尺寸映射／逆向／拧转、真实 R3F 自由姿态、回执去重与失败回退。v2 同时覆盖场景保存、晚加入、撤销、复原及显式升级；本机 SQL 校验已启用，生产环境未变更。机器检查不替代课堂手感验收。

2026-09-20 增量增加按钮互斥与退出、工作台接入守卫、原路拖回的误点击保护、接触棱翻滚、Soma 刚体过程、长方体尺寸与支撑平面、当前立方体半格支点历史、骰子课堂语义与轨迹用例。当前版立方体旋转允许半格支点，但落点仍为整数格，旧历史格式的旋转边界保持不变。
