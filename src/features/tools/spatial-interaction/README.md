# 立体教具共用交互

状态：2026-09-22 开发端增量，待产品负责人实际手感验收。范围是教具空间的操作，不增加题目、提交或判题。

## 按需操作手柄（2026-09-22）

- 选中只确定操作目标，不弹出 XYZ 箭头、平面方片或旋转快捷栏。直接拖模型仍沿 XZ 移动，拖空白仍转视角。
- 点右侧“精确移动”显示轴与平面手柄；点“旋转”显示轴环与旋转快捷栏。再次点当前按钮、关闭面板、轻点空白或按 Esc 收起；鼠标和触屏使用相同显式入口，不增加长按、双击或隐含按键手势。
- `SpatialToolDefinition.transformPanels` 是工作台唯一手柄模式声明；`useSpatialToolState.transformMode` 统一传到画布。未声明的面板、普通选中和旧调用默认不显示手柄。`gizmo-adapter` 与旧轴控制器同时关闭隐藏手柄的命中区域，本体拾取独立保留。
- 已接入立方体结构／投影、Soma、骰子、立体观察及真实切块；测量／单位、面棱点观察、标色、透明与尺寸面板不叠加变换控件。截面手柄仅在截面操作中出现，排水 Y 轴手柄仅在浸入／提起操作中出现，关闭后保留教学结果。
- 正方体／立体展开图的折痕、纸面抓取，明确打开的移面箭头，旋转成体的教学转轴不是通用移动控件，保留各自领域入口；倒水器皿没有额外变换控件。坐标轴显示开关与标识也保持独立。
- 验证覆盖默认、选择、切换面板、显式开启、重复关闭、空白与 Esc、隐形命中和本体 XZ 拖动；示教状态与保存合同不变。接入规则写入 tools 局部约束，新增面板需要主动声明才能出现变换手柄。

## 功能与图标共用（2026-09-21）

- [功能清单](control-inventory.ts)覆盖 9 个工具入口、10 个操作现场，包含主工具栏、对象旁控件和参数功能。`actions.ts` 是动作名称与 SVG 语义的权威；`SpatialActionIcon` 唯一实现图形。`SpatialActionButton` 组合原有 shadcn 按钮，不复制按钮样式。
- 对照页从实际组件与工作台绑定生成：`node scripts/spatial-controls-catalog.mjs` → `.tmp/spatial-controls-comparison.html`。同时列出旧图／新图、各舞台功能及所用图标、全部动作与视角／轴／标记参数图形。
- 整体移动用三轴箭头；移面用纸面向外移出的立方体。对象旋转、视角旋转、视角平移各自独立。展开使用展开符号，复原保留回到起点；透明用半透明圆，液量用烧杯；截面使用穿过立体的切平面，剪刀用于剪棱／拆分。
- 共用组件：`SpatialViewButtons`（视角与适合画面）、`SpatialAxisSteps`（移动／旋转 XYZ 准确步进）、`SpatialWorkbenchControls`（按钮、浮窗、颜色、轴与标记）、`SpatialOpacitySlider`（帧级预览与一次提交）、现有对象手柄／旋转／翻滚。原 Cube 命名入口仅 re-export 兼容，当前工作台直接引用共用层。
- 合并 UI 和动作入口，不合并不同数学过程。折纸的面与折痕、骰子的点数和投掷、液体守恒、截面／测量仍使用各自模型；旧冻结版本与场景格式不变。数据缩略图（七宝、展开图、点数、截面多边形）从数学状态生成，不当作功能按钮 SVG。
- 参数行里的加减、方向、关闭等基础符号允许复用；旋转正负方向始终带轴与角度，不用单独的回转箭头冒充“展开”。工作台接入测试防止再次本地导入 Lucide 或内联 SVG 自选功能图标。

## 操作约定

- 同一个左键／单指手势按命中位置分流：命中当前动作的对象或手柄则操作对象，未命中则交给 `SpatialCameraRig` 旋转视角。右键平移和滚轮缩放保持原控件规则。
- 轻点空白或工作台内按 Esc 取消选中，收起对象高亮、手柄、快捷栏与对象参数；拖动空白保留选中并转视角。再次点模型、对象列表，或打开对应对象参数可恢复操作目标。输入框的 Esc 保留自身行为，多个实例互不影响；仅被动工作台容器的焦点外框不显示，内部控件焦点样式保留。
- 取消选中是本机操作焦点，不移动对象、复原移面、展开纸片或改写备课现场。旧快照仍可保留必要的当前对象 ID，通过 `selectionActive` 控制舞台反馈。立体观察的已开启截面和测量保留，仅停止直接手柄拾取。
- 共用手势读取允许交互对象为空：React 可先更新 ref，再清理旧监听。清理只结束该绑定持有的手势／收拢动画及原回调，先移除监听并释放捕获，不读取已清空或替换的当前对象；重复清理保持无副作用。接入回归保留真实 `CubeMoveHandles` 的 effect 生命周期，不仅检查工作台传给 Canvas 的属性。
- 共用工作台将舞台标签、按钮字样和说明文字设为不参与原生框选，避免拖动／Shift 旋转误选网页文字；输入框、文本域和可编辑区域显式保留选字能力。保护限于工作台，不修改文档全局选择或指针事件。
- 正常观察状态同时提供对象操作。专用平移按钮改变的是空白手势；移动、折叠、剪棱等动作不会以 `navigationMode="object"` 关闭整块画布的旋转。
- 当前版立方体结构／投影、骰子、立体观察和 Soma 连续拖本体固定沿 X-Z 保持高度；其他平面通过 XZ／XY／YZ 方片指定。起拖平面锁定到松手，松手按领域约束落位。XYZ 箭头明确指定轴；接入 `bodyGesture` 后旧轴控制器只响应真实轴箭头，不再抢走本体并猜轴。轻点选中，拖动另一物体直接提交它的终点，选择不抢先发送课堂写入。
- 已编组的结构／Soma 拼块保持刚体；立方体普通观察选择已编组或面相连部分，原移动、多选工具仍可操作单位块。
- `SpatialTransformHandles` 在精确移动时提供 XYZ 箭头间的 XZ／XY／YZ 平面方片，在旋转时提供明确的世界轴旋转环；`SpatialRotationControls` 仅在旋转操作中显示，并保留正负 90° 入口。精确面板调用同一领域操作，动画终点继续满足领域约束。
- 旋转、翻滚快捷栏共用 `SpatialObjectToolbar`。领域提供当前姿态的世界顶点和移动手柄，投影后按实际 HTML 尺寸在模型／句柄热区外找空位；优先保持可用的一侧，给舞台工具栏保留边界。极端放大或窄舞台没有完整空位时收起浮动栏，侧栏入口保持可用，不把快捷栏硬压回模型上。
- 模式按钮和参数面板共用 `useSpatialToolState`：切换操作时同步更换拾取方式；再点当前操作、关闭面板或在工作台内按 Esc，返回常规直接操作。显示开关、一次性命令与操作模式分开；关闭截面参数保留截面结果并收起调整手柄。
- `SpatialCameraRig` 统一保护轻点命令：超过移动阈值后即使拖回起点，也不会误触标色、删除或剪棱；双指、右键、取消和已由对象接管的拖动同样不会留下点击。
- `SpatialRollControls` 与 `SpatialRollButtons` 提供 X/Z 正负方向翻滚，指向对象显示支撑棱与中心路径。每次绕行进方向的底部支撑棱翻 90°，不是平移再原地旋转。
- 教学变化保留过程；减少动态效果时采用短过渡。首个绘制帧开始计时，编译或绘制等待不消耗动画。动态帧只负责显示，不保存为数学参数。
- 手动拖动实时预览、松手写一次。`useSpatialDirectCommit` 在课堂回执前保留已经展示的终点，失败或外部状态替换则返回权威现场。相同回执不重播；按钮动作等待权威状态后播放。
- 对象写入忙碌和相机控制分开。教师可在对象动画或持久化期间观察；展示端原有只读权限保持不变。

## 当前库核查

| 教具／能力 | 对象操作与观察 | 共用接入／保留的领域操作 |
| --- | --- | --- |
| 立方体结构、投射 | 沿 XZ 直接移动相连结构／分组、单位块多选、对象旁转动；空白旋转 | 共用本体、平面／圆环手势及轴箭头；投射继续读取同一展示帧。搭建、挖除、层显隐、标色、单面染色、透明、标注、标数、分割及撤销保持原数学操作 |
| Soma | 连续拖整宝，平面方片直接选面；旋转默认拖世界轴圆环，自由轨迹球可选；空白旋转 | 七宝共用固定支点、平面／圆环手势、Arcball、真实刚体拾取；v2 默认在邻近合法朝向松手吸附，可关闭以保留自由角度。旧 v1 保持离散朝向 |
| 骰子 | 沿 XZ 拖本体、对象旁转动；空白旋转 | 共用真实网格拾取、本体与精确手柄路由；松手保留原移面状态，关闭网格吸附时本体与平面手柄保留小数坐标。移面、相对面观察、动态透视、点数显隐、面色与透明、受控滚动和投掷保持各自动作 |
| 正方体展开图、剪棱展开 | 命中纸面折叠／展开，空白旋转；剪棱轻点与拖动观察区分 | 保留已验收的折纸手势和可中断教学动画；11 图转换、逐面展开、移面、标注等不改数学路径 |
| 自由折纸、长方体／三棱柱展开图 | 共用纸面折叠拾取；空白旋转 | 复用 `CubeNetFoldInteraction` 与教学播放；折线、面数与角度范围由各自领域校验 |
| 立体展开图的曲面 | 抓圆柱／圆锥的侧面卷展、抓底面折合；空白旋转 | 复用 `useSpatialParameterDrag` 的指针归属、取消和一次提交；领域提供材料点的等距轨迹及封闭终点，固定取景覆盖完整过程 |
| 立体观察 | 实际网格拾取，沿 XZ 移动实体、世界轴转动、参数与透明过渡；空白旋转 | 共用本体与精确手柄，即使当前没有选中对象也能直接拖动；面／棱／顶点选择期间保留专用拾取，不被移动抢走 |
| 截面、测量与切块 | 拖切平面平移／倾斜；真正切块后分别移动、旋转、分离和拼回；测量关联实体 | 切块复用真实网格拾取和共用对象手势，不新增移动组件；单位显示换算不缩放实体，单位长／面／体累积复用表现层 |
| 容积比较与排水测体积 | 视角可自由观察；倒水与浸入均有过程，原水位保留 | 复用实体画布、相机、工具面板和 Y 轴手柄；浸入使用显式连续轴参数，液体守恒由领域计算。器皿固定排布，自由摆放与任意倾斜液体仍需独立数学边界 |
| 旋转成体 | 抓长方形／直角三角形绕固定边旋转；空白旋转视角 | 通过 `SpatialObjectInteraction.bodyConstraint` 显式声明固定轴，用同一对象控制器连续转过整圈；播放／暂停同步语义时间线，扫体由领域生成 |

以上工作台的操作按钮均接入共同模式／面板规则；展开方式总入口只切换子工作台，不叠加另一套拾取模式。投射继续复用立方体工作台的交互规则。

本次翻滚接入：当前版立方体结构、自由拼搭的 Soma、骰子，以及立体观察中的正方体／长方体。立方体与 Soma 保持整组选中对象的刚体和精确格点，长方体按真实尺寸翻动，骰子沿用已有受控滚动命令与点数轨迹。缺少实际支撑棱时，以当前姿态沿世界轴的最小外接长方体对应底棱作为虚拟支撑，悬停／聚焦方向按钮用细虚线提示。路径与落点碰撞、范围边界仍由原领域检查；支撑提示不进入录制或快照。曲面滚动、任意倾斜实体的接触演化不在本次范围。投射的冻结 v1 仍不接受带旋转标注的状态，本次未为它开放翻滚。

## 全库底层能力统一方案（2026-09-21）

统一的对象是教具的基础能力和操作约定，而不是把不同教学现场做成同一个工具。按钮已经共用不等于下方的命中、约束、动画和持久化已经共用。后续按下表逐层迁移，领域模块只保留数学差异。

| 层 | 唯一共用入口／合同 | 领域保留与核查重点 |
| --- | --- | --- |
| 舞台与相机 | `SpatialCameraRig`、视角栏、点击保护、`useSpatialToolState` | 对象操作与空白观察同时存在；专用画布只提供显示范围和初始视角。只读权限和对象写入忙碌分开 |
| 目标与指针 | `picking`、`object-gesture-controller`、`CubeMoveHandles` | 真实可见表面／单位块／刚体单元的命中由领域提供；按命中对象选择、分组，不用侧栏当前 ID 代替。明确手柄优先于本体，空白归相机 |
| 变换与合法终点 | `SpatialObjectInteraction`；离散工具用 `gizmo-adapter` | 本体 XZ、轴箭头、平面方片、轴环和可选轨迹球共用手势；整数／半格、Soma 姿态、骰子碰撞、实体范围由领域校验。预览和准确参数应归到同一终点函数 |
| 反馈与动画 | `SpatialObjectToolbar`、`SpatialHeightGuide`、`SpatialLandingOutline`、`useSpatialPresentation`、`rigid-motion` | 对象外避让、纵向提示、细虚线候选；中间帧仅展示。领域提供折痕、滚动接触、截面或液体插值，不在按钮里另写计时循环 |
| 显示与属性 | 动作目录、`SpatialWorkbenchControls`、`SpatialOpacitySlider` | 标色、透明、标记的作用范围用对象／面明确表达；骰子点数、展开面字母、实体几何不是同一种数据，但可复用输入与预览组件 |
| 备课与课堂 | `scenes/`、现有课堂端口、`useSpatialDirectCommit` | 教具、备好的起点、课件实例分开；只发准确终点或语义命令，恢复／撤销／晚加入可重建现场，回执不重播手势 |

本增量已实施：修复刚体本体手势入口；`gizmo-adapter` 接收 `objectFor(id)` 和实际命中点，脱离“只能操作当前选中对象”的限制。Dice、Cube／投影、SolidGeometry 与 Soma 共用 XZ 本体规则，圆环打开时未抓环仍能拖本体；Soma 明确选择自由轨迹球或 Shift 时保留旋转。轴箭头仍由同一个旧兼容控制器执行精确轴操作，不是另一套本体移动。冻结版本和未接入新版提交端口的调用继续保留旧合法动作边界。

接下来的施工批次（尚未完成，不因本次检查通过而标记收口）：

1. **变换终点统一**：继续将旧轴箭头、精确步进与本体／方片的目标解析收敛到领域适配器；逐项核对格点吸附、连续预览、碰撞、撤销粒度。当前骰子／实体旧轴步长与本体小数落点仍有领域适配差异，应在这里消除，而不是扩大所有工具的保存格式。
2. **非刚体过程统一**：折纸、剪棱、截面复用相同的指针归属、双指交接、取消与一次提交合同，保留铰链拓扑／切平面数学。液体复用过程播放和恢复合同；容器自由摆放与倾斜需要单独补数学边界。现有已验收折叠手感先保留，逐场景替换并人工验收。
3. **表现与教学属性统一**：核对各舞台的虚线候选、工具栏避让、恢复对象／恢复场景的范围、面色／透明／标记的作用对象，以及相机吸附和对象吸附是否独立。以能力复用，不按工具复制面板。
4. **场景宿主统一**：将旧捕获／命令桥和新版快照宿主的公共生命周期逐步归并；每批覆盖备课、课件实例、直接操作、动画中止、回执、撤销和恢复。保留版本适配，避免为新教具再做一套课堂专用实现。

每一批按“共用合同 → 一个真实现场 → 同类接入 → 定向回归 → 人工验收”推进。新增刚体工具必须同时接入真实命中、默认本体 XZ、明确轴／面／环、空白观察、取消和一次提交；专用操作若不支持刚体移动，应明确说明领域边界，不能靠复用一个图标宣称能力已统一。

### 本批共用扩展：曲面、浸入与固定轴（2026-09-21）

- `useSpatialParameterDrag` 与 `parameter-trajectory` 管理曲面等非刚体参数的材料点轨迹：领域只提供参数到屏幕位置的映射，共用层冻结起拖路径、锁定局部分支、预览、松手提交及 Esc／取消／失焦／丢失捕获清理；第二指在起拖前可交还相机。退化投影保留当前值，不凭空漂移。
- `SpatialObjectInteraction.bodyConstraint` 是专用演示的显式约束槽。旋转成体固定绕边转动并保留整圈连续角度；未提供约束时仍沿原本体 XZ 移动，既有 Soma、骰子和立体结构不改变手感。
- 轴控制器的 `continuousDistance` 只由需要连续物理量的适配器开启，并与关闭网格吸附配合。排水法沿 Y 连续浸入；其余调用默认半格／整数领域边界不变。
- 曲面折展和旋转成体保存终点、起始时间与时长，不发布逐帧状态。暂停形成准确静态终点；晚加入定位到当前进度。直接操作沿用 `useSpatialDirectCommit`，回执不重放已经完成的手势。
- 这些扩展覆盖本批真实现场；上文尚未完成的旧轴终点收敛、全部非刚体迁移与宿主归并继续按批推进，不以本批通过测试宣称全库架构已收口。

## 新增操作的接入步骤

1. 先区分操作模式、显示开关与一次性命令。需要拾取对象／面／棱的参数面板，在 `SpatialToolDefinition.panels` 中声明所需模式；使用 `controls.bindings` 限定键盘退出与状态反馈的实例范围，把 `controls.onPointerMissed` 传到实际 Canvas。用 `onClearSelection` 清理本机选择；快照需要保留当前 ID 时使用 `selectionActive` 关闭反馈，并在选中或直接拖动成功后 `activateSelection`。收起控件后保留对象拾取，既不留下隐形热区，也不要求重新切换模式。
2. 按命中对象接管指针；复用相机的空白手势和点击保护。刚体必须接入 `bodyGesture`，本体默认沿 XZ，轴／平面方片／世界轴圆环显式覆盖约束。用 `transformPanels` 声明精确移动／旋转入口，将 `controls.transformMode` 传给画布；`gizmo-adapter` 的 `objectFor(id)` 按真实命中 ID 解析对象或分组，mode 为空或 `showHandles` 未明确开启时不生成控件及其热区，但保留本体拾取。支持自由姿态的领域显式提供 Arcball 适配。直接手柄与精确输入使用同一个领域目标函数。
3. 准确终点由领域模型校验。移动、旋转、翻滚复用现有控件和刚体表现层；对象旁控件传入包含实体大小的当前世界顶点，以及实际移动手柄，复用屏幕空间避让。需要旋转吸附时用 `rotation-snap.ts` 查找邻近允许朝向，领域校验位置／碰撞，返回 `snapped` 候选。拖动保留自由帧，松手由共用控制器平滑收拢，不要求另点对齐。翻滚使用 `rolling.ts` 的接触棱规划，并由领域检查合法落点和路径。对旧版本明确关闭不支持的动作。
4. 仅提交终点／语义命令；本地预览与课堂回执沿用现有桥接。为新动作补充中间帧、精确终点和撤销／复原用例。
5. 运行按钮交互、选择生命周期、工作台接入检查及受影响领域测试。接入检查遍历所有使用空间工具栏的工作台，新增工作台遗漏空白取消选中会失败，允许组合入口复用已接入的子工作台。手感和视觉继续由产品负责人实际验收。

## Soma 自由轨迹球（2026-09-20）

- 普通鼠标／单指拖动保持连续跟手，沿 XZ 桌面移动；平面方片 XZ 保持 Y，XY 保持 Z，YZ 保持 X，不随视角改选。侧看桌面无法可靠求交时提示换视角或使用可见方片、轴箭头。
- 2026-09-21 精简旧入口：移除平面选择、按视角切换阈值、俯仰角读数和灰色平面框，保留三标准平面方片及单轴箭头。`SpatialHeightGuide` 只画纵向细虚线与地面小十字，随整块、方片和轴拖动更新，不参与拾取，不写入场景或课堂快照。
- 常规状态不显示 XYZ 精确手柄及轴间平面方片，点右侧精确移动后出现。平移与旋转轴分别保存为 UI 状态。点旋转后默认拖轴向圆环，再点返回常规操作；面板可切到自由轨迹球后直接拖对象，电脑 Shift 拖拼块也保留自由旋转。
- `arcball.ts` 按对象投影尺寸、抓取起点和当前点映射球面：中心附近翻转，边缘绕圈拧转。手势开始锁定相机、球心和半径，以起点计算总四元数，不累加欧拉角。球外拖动映射到圆周；显示圆环不扩大对象命中区。
- v2 支点采用源模型固定包围中心，转动中不随世界 AABB 变化。“旋转吸附（松手时）”提供自由、轻吸附 15°、标准 30°、强吸附 45°，默认标准，与顶部视角吸附独立。档位阈值统一定义于 `rotation-snap.ts`。接近 24 个网格朝向中的一个且最近格点能放下时，细虚线预告落位；松手用 160 ms 平滑收拢。自由档、远离目标或网格有冲突时保留合法自由末帧；自由末帧自身无效则回退。90° 按钮使用相同吸附策略，吸附后的格点姿态可直接翻滚。
- 平移与旋转预览复用 `SpatialLandingOutline`，从已有单位块棱边去重规则生成一像素虚线，不渲染填充面、不占用拾取。实际对象保持自由跟手，候选轮廓只说明松手终点。
- 网格落位保持中心尽量接近，每轴最多修正半格；半格等距优先保留源姿态的同一单元锚点，避免连续 90° 操作产生取整漂移。开关只影响后续操作，不改写已保存的姿态；显示网格开关也不改变数学约束。
- 自由姿态的网格仅作参考，允许绕中心转到参考平面下方；坐标范围、单位四元数与拼块之间的实际 OBB 碰撞由前后端共同检查。旧 v1 格点、地面与支点语义保持不变。
- 单指开始移动前加入第二指，将完整起点交给相机平移／缩放；对象已开始移动后保留第一指归属。松手只提交一次，160 ms 短过渡用于平移落位、邻近旋转吸附或无效落点回退；无修正时立即结束。取消、失焦、外部权威替换均不发布对象操作。
- `object-gesture-math.ts` 保留已验收的平面平移；`object-gesture-controller.ts` 管理设备归属和一次提交。`rigid-geometry.ts` 管理局部空间拾取与单位 OBB 碰撞。领域通过 `SpatialObjectInteraction` 提供选择范围、支点、包围半径、合法候选与提交，由 `CubeMoveHandles` 保持精确轴优先级。

自由轨迹球仍是 Soma 的可选方式；当前共用本体 XZ／平面／圆环手柄已接入 Soma、立方体结构、骰子和立体观察。各领域的合法终点保持原版本合同。自动检查覆盖数学、指针事件、真实 R3F 姿态和课堂回执；桌面、iPad 与大屏的实际手感待产品负责人验收。

## 明确平面与旋转环（2026-09-21）

- 移动方片与 XYZ 轴使用相同中心；XZ 保持 Y，XY 保持 Z，YZ 保持 X。抓住方片即可连续移动，不需要先选平面再抓模型。当前刚体工具直接拖本体固定沿 XZ。
- 旋转环以世界 XYZ 为轴，抓住环后沿环转动。共用控制器冻结起拖相机、轴、固定支点与圆环尺寸，模型变化不反过来改变操作方向。接近侧看的平面／环淡化并停止拾取，不暗中换成其他方向。
- `transform-handles.ts` 同时提供可视几何与屏幕命中；平面方片优先于旧轴手柄和物体拾取。触屏扩大命中余量，单指未移动前加入第二指仍交给相机，取消不提交。中间帧与松手收拢共用控制器，准确参数仅在松手写入一次。
- Soma 沿用自由姿态与吸附合同。骰子、实体和立方体结构用 `gizmo-adapter` 将每次旋转手势限制在正负 90°，预览连续，松手落到最近允许的四分之一圈；这些工具没有因此获得任意角度的保存合同。
- 立方体平面移动原子提交两条原有轴向步骤，保留已有录制／回放格式和每轴撤销粒度；两种轴顺序均不合法或录制容量不足时整体回退。本轮不升级冻结快照。直接旋转的去重同时使用几何与语义历史，均色单块的撤销仍有过程动画。
- 骰子与立体观察的手柄复用现有终点写入；拖动过程留在本机。立体观察的截面与测量继续跟随同一展示实体，展开图／剪棱的已验收折纸手势不变。

## 接入与验证边界

`policy.ts` 管理共用动作计时与普通操作状态；`picking.ts` 读取实际表面，适配尺寸、转角、透明与裁切。现有 `CubeMoveHandles`／`bindCubeAxisDrag` 作为共享移动入口复用，领域提供选择范围、落点有效性和提交函数。

`useSpatialPresentation` 接收严格目标的稳定键与领域插值。`rigid-motion.ts` 使用四元数和固定轴圆弧，Soma 的格点模型与显示姿态分离，旋转中不让各小方块独立滑动。旧立方体历史仍由既有语义旋转解析，复用相同动作计时，不改发布版本。

定向检查覆盖鼠标／单指分流、一次提交、真实几何拾取、7 × 24 个旧姿态对应、Arcball 尺寸映射／逆向／拧转、真实 R3F 自由姿态、回执去重与失败回退。v2 同时覆盖场景保存、晚加入、撤销、复原及显式升级；本机 SQL 校验已启用，生产环境未变更。机器检查不替代课堂手感验收。

2026-09-20 增量增加按钮互斥与退出、工作台接入守卫、原路拖回的误点击保护、接触棱翻滚、Soma 刚体过程、长方体尺寸与支撑平面、当前立方体半格支点历史、骰子课堂语义与轨迹用例。当前版立方体旋转允许半格支点，但落点仍为整数格，旧历史格式的旋转边界保持不变。
