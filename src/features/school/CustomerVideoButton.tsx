"use client";import{useState,useTransition}from"react";import{useTranslations}from"next-intl";import{Button}from"@/components/ui/button";import{getVideoSignedUrl}from"./video-actions";
export function CustomerVideoButton({videoId}:{videoId:string}){const t=useTranslations("school.videos");const[url,setUrl]=useState("");const[pending,startTransition]=useTransition();return url?<video src={url} controls className="mt-2 w-full max-w-lg rounded-lg bg-black"/>:<Button size="sm" variant="secondary" disabled={pending} onClick={()=>startTransition(async()=>setUrl(await getVideoSignedUrl(videoId)))}>{t("playback")}</Button>}
