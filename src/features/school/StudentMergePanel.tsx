"use client";
import{useEffect,useState,useTransition}from"react";import{GitMerge,LoaderCircle}from"lucide-react";import{useTranslations}from"next-intl";import{Button}from"@/components/ui/button";import{ConfirmDialog}from"@/components/ui/confirm-dialog";import{findDuplicateStudentsForMergeAction,mergeStudentsAction,type DuplicateStudentRow}from"./actions";import{useRouter}from"@/i18n/navigation";
export function StudentMergePanel({studentId,name,phone}:{studentId:string;name:string;phone:string}){const t=useTranslations("school.students");const router=useRouter();const[rows,setRows]=useState<DuplicateStudentRow[]>([]);const[target,setTarget]=useState<DuplicateStudentRow|null>(null);const[failed,setFailed]=useState(false);const[pending,startTransition]=useTransition();useEffect(()=>{let live=true;findDuplicateStudentsForMergeAction(name,phone).then(value=>{if(live)setRows(value.filter(row=>row.id!==studentId))}).catch(()=>{if(live)setFailed(true)});return()=>{live=false}},[name,phone,studentId]);const merge=()=>{if(!target)return;setFailed(false);startTransition(async()=>{try{await mergeStudentsAction(studentId,target.id);setTarget(null);setRows(current=>current.filter(row=>row.id!==target.id));router.refresh()}catch{setFailed(true)}})};if(rows.length===0&&!failed)return null;return <section className="mt-6 rounded-xl border border-line bg-card p-4"><h2 className="flex items-center gap-2 font-medium"><GitMerge size={16}/>{t("possibleDuplicates")}</h2><p className="mt-1 text-xs text-muted">{t("mergeKeepsCurrent")}</p><ul className="mt-3 divide-y divide-line">{rows.map(row=><li key={row.id} className="flex flex-wrap items-center gap-3 py-3 text-sm"><span className="min-w-0 flex-1">{row.name} · {row.phone||t("none")} · {t(row.status)}</span><Button type="button" size="sm" variant="secondary" disabled={pending} onClick={()=>setTarget(row)}>{pending&&target?.id===row.id?<LoaderCircle size={14} className="animate-spin"/>:<GitMerge size={14}/>} {t("mergeIntoCurrent")}</Button></li>)}</ul>{failed&&<p role="alert" className="mt-2 text-xs text-rose">{t("actionFailed")}</p>}<ConfirmDialog open={target!==null} onOpenChange={open=>{if(!open)setTarget(null)}} title={t("mergeStudentTitle")} description={target?t("mergeStudentConfirm",{name:target.name}):""} confirmLabel={t("mergeIntoCurrent")} cancelLabel={t("cancel")} onConfirm={merge} pending={pending}/></section>}
