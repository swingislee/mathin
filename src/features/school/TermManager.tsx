"use client";
import{useState,useTransition}from"react";import{CalendarRange,LoaderCircle}from"lucide-react";import{useTranslations}from"next-intl";import{Badge}from"@/components/ui/badge";import{Button}from"@/components/ui/button";import{Dialog,DialogContent,DialogDescription,DialogFooter,DialogHeader,DialogTitle}from"@/components/ui/dialog";import{Input}from"@/components/ui/input";import{useRouter}from"@/i18n/navigation";import{activateSchoolTermAction,createSchoolTermAction}from"./actions";import type{SchoolTermRow}from"./courses";
export function TermManager({terms}:{terms:SchoolTermRow[]}){const t=useTranslations("school.courses");const router=useRouter();const[open,setOpen]=useState(false);const[year,setYear]=useState(new Date().getFullYear());const[term,setTerm]=useState<1|2>(1);const[name,setName]=useState("");const[start,setStart]=useState("");const[end,setEnd]=useState("");const[failed,setFailed]=useState(false);const[pending,startTransition]=useTransition();const create=()=>startTransition(async()=>{try{await createSchoolTermAction({year,term,name,startsOn:start,endsOn:end});setOpen(false);router.refresh()}catch{setFailed(true)}});const activate=(id:string)=>startTransition(async()=>{try{await activateSchoolTermAction(id);router.refresh()}catch{setFailed(true)}});return <section className="mt-6 rounded-xl border border-line bg-card p-4"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="flex items-center gap-2 font-medium"><CalendarRange size={16}/>{t("schoolTerms")}</h2><p className="mt-1 text-xs text-muted">{t("schoolTermsHint")}</p></div><Button type="button" size="sm" onClick={()=>{setFailed(false);setOpen(true)}}>{t("newSchoolTerm")}</Button></div><ul className="mt-3 divide-y divide-line">{terms.map(row=><li key={row.id} className="flex flex-wrap items-center gap-3 py-2 text-sm"><span className="min-w-0 flex-1">{row.name} · {row.startsOn} — {row.endsOn}</span>{row.isCurrent?<Badge variant="secondary">{t("currentSchoolTerm")}</Badge>:<Button type="button" size="sm" variant="secondary" disabled={pending} onClick={()=>activate(row.id)}>{t("activateSchoolTerm")}</Button>}</li>)}</ul>{failed&&<p role="alert" className="mt-2 text-xs text-rose">{t("actionFailed")}</p>}<Dialog open={open} onOpenChange={setOpen}><DialogContent><DialogHeader><DialogTitle>{t("newSchoolTerm")}</DialogTitle><DialogDescription>{t("newSchoolTermHint")}</DialogDescription></DialogHeader><div className="grid gap-3 sm:grid-cols-2"><label className="grid gap-1 text-xs text-muted">{t("schoolYear")}<Input type="number" min={2020} max={2100} value={year} onChange={e=>setYear(Number(e.target.value))}/></label><div className="grid gap-1 text-xs text-muted"><span>{t("semester")}</span><div className="flex gap-2"><Button type="button" variant={term===1?"primary":"secondary"} onClick={()=>setTerm(1)}>{t("semesterOne")}</Button><Button type="button" variant={term===2?"primary":"secondary"} onClick={()=>setTerm(2)}>{t("semesterTwo")}</Button></div></div><label className="grid gap-1 text-xs text-muted sm:col-span-2">{t("name")}<Input value={name} onChange={e=>setName(e.target.value)} maxLength={100}/></label><label className="grid gap-1 text-xs text-muted">{t("startsOn")}<Input type="date" value={start} onChange={e=>setStart(e.target.value)}/></label><label className="grid gap-1 text-xs text-muted">{t("endsOn")}<Input type="date" value={end} onChange={e=>setEnd(e.target.value)}/></label></div><DialogFooter><Button type="button" variant="secondary" onClick={()=>setOpen(false)}>{t("cancel")}</Button><Button type="button" disabled={pending||!name.trim()||!start||!end} onClick={create}>{pending&&<LoaderCircle size={14} className="animate-spin"/>}{t("create")}</Button></DialogFooter></DialogContent></Dialog></section>}
