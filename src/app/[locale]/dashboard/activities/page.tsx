import{getTranslations,setRequestLocale}from"next-intl/server";import{ActivitiesManager}from"@/features/school/ActivitiesManager";import{listActivities}from"@/features/school/activities";import{getMyPerms,requirePerm}from"@/lib/auth";
export default async function ActivitiesPage({params}:{params:Promise<{locale:string}>}){const{locale}=await params;setRequestLocale(locale);const user=await requirePerm(locale,"activity.register");const[t,rows,perms]=await Promise.all([getTranslations("school.activities"),listActivities(),getMyPerms(user.id)]);return <div className="mx-auto w-full max-w-6xl"><ActivitiesManager title={t("title")} activities={rows} canManage={perms.has("activity.manage")}/></div>}
