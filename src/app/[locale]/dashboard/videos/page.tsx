import{getTranslations,setRequestLocale}from"next-intl/server";import{SchoolPageHeader}from"@/features/school/PageHeader";import{VideoReviewPanel}from"@/features/school/VideoReviewPanel";import{listReviewVideos}from"@/features/school/videos";import{requirePerm}from"@/lib/auth";
export default async function VideosPage({params}:{params:Promise<{locale:string}>}){const{locale}=await params;setRequestLocale(locale);await requirePerm(locale,"video.review");const[t,rows]=await Promise.all([getTranslations("school.videos"),listReviewVideos()]);return <div className="mx-auto w-full max-w-6xl"><SchoolPageHeader title={t("title")}/><VideoReviewPanel rows={rows}/></div>}
