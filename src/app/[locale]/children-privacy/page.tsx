import type{Metadata}from"next";import{getTranslations,setRequestLocale}from"next-intl/server";import{SiteHeader}from"@/components/site-header";import{buildMetadata}from"@/lib/seo";
export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const{locale}=await params;const t=await getTranslations({locale,namespace:"compliance"});return buildMetadata({locale,path:"/children-privacy",title:t("childrenTitle"),description:t("childrenIntro")});}
export default async function ChildrenPrivacyPage({params}:{params:Promise<{locale:string}>}){const{locale}=await params;setRequestLocale(locale);const t=await getTranslations("compliance");return <main className="min-h-screen"><SiteHeader/><article className="mx-auto max-w-3xl px-6 pb-16"><h1 className="font-display text-3xl">{t("childrenTitle")}</h1><p className="mt-4 leading-7 text-muted">{t("childrenIntro")}</p><h2 className="mt-8 font-display text-xl">{t("consentTitle")}</h2><p className="mt-3 leading-7 text-muted">{t("consentBody")}</p><h2 className="mt-8 font-display text-xl">{t("videoTitle")}</h2><p className="mt-3 leading-7 text-muted">{t("videoBody")}</p></article></main>}
