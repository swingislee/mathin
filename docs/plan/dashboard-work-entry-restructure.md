# 仪表盘工作入口重组

> **状态**：开发端已交付，待人工视觉与业务验收。
> **依据**：2026-09-19 产品负责人要求直接重构正式页面，并明确旧网址无需保留接入新入口。
> **范围**：侧栏、工作页编排、站内链接与既有功能迁移；当前生产阶段继续以 doc 04 顶部为准。

## 1. 一级入口

员工先按要做的事选入口，再按阶段、班级和时间缩小名单。角色决定可见范围与办理权限。

| 顺序 | 入口 | 工作内容 |
| --- | --- | --- |
| 1 | 总览 | 查看概况与已有待办，进入对应办理页面 |
| 2 | 课表 | 按时间找课、进入课次 |
| 3 | 线索 | 导入、核对、分配联系人 |
| 4 | 沟通 | 按学生阶段展开登记，连续处理联系结果 |
| 5 | 测评 | 查看预约、登记测评与反馈、查看报告 |
| 6 | 学生 | 保留原五标签、搜索、资料与快捷办理逻辑 |
| 7 | 班级 | 分班、进入班级、连续教学登记、查看完成情况 |
| 8 | 续班 | 沿用目标周期、沟通结果、报名与缴费工作 |
| 9 | 活动 | 按场次安排与登记 |
| 10 | 财务 | 沿用原账务入口 |

教学资源继续分组展示。组织管理包括机构、校区、员工、权限、学年和报名配置；系统管理沿用现有入口。协同记录、管理分析退出侧栏并返回 404，底层历史记录继续保留。

## 2. 沟通

正式地址为 `/dashboard/communication`。标签沿用学生阶段的权威推导：**待首联、待测评、待报名、待续班、已停读／课程结束**。

- 待首联复用已验收的首联摘要行与行下详情，包括联系结果、微信、意向、承接、备注、下次联系和显式保存。历史仅有学生档案的记录继续使用已有首联承接方式。
- 待测评默认展开邀约登记；待报名默认展开报名登记；待续班和已停读／课程结束默认展开沟通备注，保留续班或重新报名等既有快捷动作。表内展开、保存与下一位继续使用现有连续录入逻辑。
- 学生页五标签及人数口径继续共用原数据层。学生页原有默认展开方式、搜索、资料和登记能力保持现状。
- “联系工作单”进入 `/dashboard/communication/worklists`，保留原工作安排、待安排、沟通记录和固定名单。明确的线索或工作单定位也进入该办理面。
- 只有原沟通权限但没有学生列表读取权限的员工继续使用工作单界面；数据读取和写入仍由原权限及服务端合同控制。
- 跨阶段搜索使用学生阶段表原有行内登记。首联表自身列筛选仍作用于当前加载名单；本轮没有重写五标签的人群推导或跨页草稿规则。

首联授权与保护基线见 [首联面板合同 §10.6](school-support-followup-panel-contract.md#106-2026-09-19-沟通入口与五阶段编排开发端待验收)。

## 3. 班级

正式地址为 `/dashboard/classes`，共用三个主视图：

| 视图 | 内容 |
| --- | --- |
| 班级与分班 | 原分班的期次、年级、班级和学生席位；沿用选座、调班及原容量校验 |
| 教学登记 | 原教学工作台的班级与课次列表、连续教学记录、作业检查等功能 |
| 完成情况 | 原教学完成情况与“我的待办”，按权限查看本人或团队范围 |

- 班级名称进入已有班级详情；班级行增加“教学登记”，带上班级及期次定位。
- 教师默认进入教学登记，其余员工默认进入班级与分班；显式 `view` 优先。
- 主视图切换保留 URL 中的班级、教师、期次等定位，清除各视图独有的字段筛选和分页。分班表当前所选期次随教学入口传递。
- 班级目录作为辅助入口保留原列表、建班、导入和测试班级管理，使用 `view=directory`。
- 原教学记录接口、作业批阅和复核子页迁入班级路径。班级详情和专业登记继续使用原有权限，一级入口扩展不授予新的写权限。

## 4. 地址与已有业务记录

线索、测评、续班直接使用 `/dashboard/leads`、`/dashboard/assessments`、`/dashboard/renewals`。原 `/dashboard/followups/**`、`/dashboard/teaching/**`、`/dashboard/enrollments`、`/dashboard/invitations`、`/dashboard/opportunities` 页面退役，未建立兼容路由或重定向。

站内按钮、返回路径、缓存刷新和总览办理链接已使用当前入口。通知、推送和待办读取已存业务地址时生成当前目的地，不改写数据库历史记录，也不恢复旧网址访问。

## 5. 验证与验收位置

- 机器检查覆盖侧栏顺序、路由退役、教师默认入口、学生原有交互、沟通默认登记、班级定位、首联工作会话、分页、教学记录接口及相关权限合同；类型检查和受影响 ESLint 通过。
- 按仓库 verify 配方核对本机目标，复用固定主管／教师账号进行认证后的 HTTP 页面读取。已检查中文各主入口及沟通五阶段、班级分班／登记／完成情况和英文沟通页面；未进行业务写入。
- 本批无数据库迁移和生产操作。视觉、连续填写手感与功能排布由产品负责人在开发页面验收，未关闭正式 Gate。

直接体验：[仪表盘](http://192.168.5.213:3130/zh/dashboard)、[沟通](http://192.168.5.213:3130/zh/dashboard/communication)、[班级与分班](http://192.168.5.213:3130/zh/dashboard/classes?view=arrange)、[教学登记](http://192.168.5.213:3130/zh/dashboard/classes?view=records)。
