# 立方体结构 · 统一操作工作台

> 2026-09-07 产品负责人选入的独立 Tools 验收增量。状态：M1、M2、M3 及六项开发端清单均已获产品负责人验收，范围见 §2.1；账号草稿与教师自建组合页固定副本已接入本机开发环境，均待人工验收，范围见 §2.3、§2.4。保留原粗边；斜视轮廓不等宽仍作为单独待修项。正式课程讲次编辑入口及真实课堂同步尚未接入；本次不改变 doc 04 当前阶段、不关闭 SML-0，也不代表生产部署或正式发布 Gate 通过。

> **切割追加反馈**：选线／选面已自动共存，选面使用实际点击平面；大截面采用平面几何，拆开组的原粗棱边使用同色系深色。连续切割按“点击哪一部分，就切哪一部分”，新组不再锁住下一刀。实际浏览器诊断进一步修复了高亮线被原黑棱边盖住、切割笔尖与点击热点错位的问题；zh/en 选线与锁定、zh 确认切割已复测。产品负责人先确认棱线选择修复 `ed6409ea` 通过，随后确认 §2.1 的六项清单全部通过，包含其余切割／拆开展示与左视图。沿用两线定截面、独立可视几何命中、屏幕像素容差与 0% 填充面穿透合同。诊断与修复见 [切割选择诊断](../diagnostics/cube-cut-selection-2026-09-07.md)。

## 1. 产品合同

分层计数、遮挡计数、三视图、表面染色、挖空与表面积统一进入 **立方体结构 / Cube Structures**。五项作为同一工作台中的支线起点；选择支线不限制工具能力。长方体尺寸测量与正方体展开图继续独立。

备课与课堂演示使用相同画布、SVG 工具按钮、工具参数与操作模型。按钮改变下一次点击的作用，鼠标与悬浮预览表达当前工具。录制默认关闭，明确开始后，每次确认的场景操作才记录为一步；批量操作仍是一条记录。选中、hover 和相机拖动中间帧属于本地交互态，不逐帧进入教学步骤。

用户已确认：**切开并移开属于拆开展示，原结构体积与表面积保持不变；真正挖除才重新计算。** 后续实现保留逻辑整数坐标与展示位移两层数据，避免把展示间隙误算为新增表面。

## 2. 分批验收

| 批次 | 范围 | 当前状态 |
| --- | --- | --- |
| M1 · 分层计数基础 | 合并入口；空白/支线起点；逐面搭建、真正挖除；整块底色、逐面与批量染色；多选/整层选择；三视图平滑旋转；XYZ 按层显隐；初始状态与真实操作记录；前后步/播放/本机预演 | 用户已验收 |
| M2 · 切割与空间组织 | XYZ 截面切割、连续切割、拆开展示；选择编组；单块/组/整体沿 XYZ 定步移动；拆开展示与逻辑移动明确区分 | 开发端用户已验收，见 §2.1 |
| M3 · 教学标注与透视 | 分类 SVG 标记；旁边/表面/半透明中心位置；从 1 开始连续点数；单块半透明、可调透明度、被遮挡边虚线 | 开发端用户已验收，见 §2.1 |
| 账号草稿 | 登录账号保存、命名/另存、跨设备列表与打开、版本冲突保护 | 开发端已交付，待人工验收，见 §2.3 |
| 教师自建课件固定副本 | 从账号草稿选择当前模型或已录步骤，组合页保存、固定副本预览与既有审核发布合同 | 开发端已交付，待人工验收，见 §2.4 |
| 正式课程与课堂接入 | 正式讲次编辑入口、单写者语义事件、snapshot/replay、展示端/学生端/晚加入收敛 | 保持独立阶段边界，尚未接入 |

后续工具沿用同一工作台，不回到各支线的专用面板。五条支线在当前工具能力下的代表题演示已纳入本次开发端验收；后续新增教学能力仍按具体范围单独确认。

### 2.1 六项开发端人工验收 · 2026-09-07

产品负责人圈选完整六项清单并回复“验收通过”。本次据此登记以下范围全部通过；这是用户人工确认，独立于 §5 中各次交付时的机器检查记录。

- [x] **切割与连续切割**：两线定截面、点击面定截面；切开后可点击剩余主体或移开的分块继续切；本刀作用于点击部分，拆开／合拢保持原体积与表面积。
- [x] **拖动、吸附与平滑移动**：单块、编组与整体的 XYZ 箭头／选轴后实体拖动；磁铁开关、吸附终点和松手衔接；切开、移动、合拢及撤销动画。
- [x] **分类标记与连续编号**：不同符号及旁边／表面／半透明中心位置；从 1 连续点数、重复点击不重复计数、撤销恢复编号；符号与数字共存并跟随方块移动。
- [x] **半透明与遮挡虚线**：单块及整组不透明度，重点覆盖 0%、30%、100%；滑块手感、0% 保留棱边与标注，以及被遮挡棱边的虚线表现。
- [x] **视角、组色与面板**：新增左视图、俯视后继续拖动旋转；临时选中黄色与组色区分、组色修改，以及横竖屏工具面板位置。
- [x] **完整教学演示**：分层计数、遮挡计数、三视图、表面染色、挖空与表面积的代表题；新工具录制与正确回放、一次确认对应一步、临场操作保持备课记录不变。

### 2.2 后续待办与接入边界

账号草稿（§2.3）保持待人工验收。产品负责人随后要求继续下一步，本批先复用教师自建组合页接入固定副本与既有版本化发布（§2.4）；正式课程讲次入口和教师／展示端／学生端、晚加入与重连的课堂同步继续单独分批。开发账号草稿不自动复制到生产环境；只有用户明确插入的教学副本进入对应课件，之后与原私有草稿独立。

斜视棱边粗细不一致保留为单独待修项；任意斜切和切碎单位块不在本次范围。R1-Live、SML-0 与生产 Gate 状态保持不变。

### 2.3 账号草稿增量 · 开发端待人工验收

产品负责人明确选择“直接保存到账号，支持跨设备打开”。入口位于左上“模型设置”（滑杆图标）面板的“账号草稿”，支持命名保存、另存新草稿、列表刷新和打开。未登录可继续使用工具；登录或完成账号安全验证在新标签页进行，返回刷新列表后继续保存当前现场。

- [ ] 创建包含切割、编组、标注、透明度和录制步骤的模型，命名保存；刷新同一页面，确认模型、撤销/重做和教学步骤恢复，录制处于暂停状态。
- [ ] 另一台设备登录同一账号、打开同一开发网站，在草稿列表中打开该草稿并继续编辑；换账号时只显示该账号的草稿。
- [ ] 两台设备打开同一版本：其中一台保存后，另一台保存时提示冲突；可另存为新草稿保留两份内容。
- [ ] 切换到课堂演示试操作，再返回备课保存，确认备课内容保持独立；打开其他草稿或新起点前确认未保存修改的处理。

持久信封为 `cube-structures-saved-draft-v1`，内部保留 `cube-structures-draft-v3` 的备课历史和独立教学历史。保存不包含临场副本、选择/hover、逐帧相机或播放进度；自动录制重新打开时暂停。页面 URL 记住明确保存/打开的草稿 ID，刷新恢复其已保存版本；普通打开工具页不会自动替换当前现场，其他设备可从账号列表选择。

新表 `cube_structure_drafts` 使用账号所有者 RLS；账号状态和安全要求沿用现有授权入口。所有持久更新通过带预期 revision 的 RPC，版本冲突时保留服务端新版本与本页修改。打开时校验版本、完整结构、历史依赖和稳定 ID；异常输入不替换当前现场。账号变更后阻止将旧账号现场保存到新账号；回到原账号可继续保存。

当前每账号最多 200 份、每份 4 MB，明确点击保存才持久化。未保存编辑仍在当前页面内，刷新/关闭页面时请求浏览器提示；切换工具前先保存。此批不增加删除、正式发布、共享或课堂联网同步。

### 2.4 教师自建课件固定副本 · 开发端待人工验收

进入教师自建课件的组合页，在舞台顶部“工具组件”（扳手）中选择“立方体结构 · 账号草稿”。账号列表沿用私有草稿鉴权，提供“当前模型”与“已录教学步骤”；插入前可预览，插入后选中该磁贴可“预览固定副本”。组合页依旧通过已有自动保存、备课选用和可选的审核发布流程处理，不新增独立教师工具页。

`spatial-lab / cube-structures-lesson-v1` 是独立内容版本，保存标题、固定起点和有效录制步骤；不保存个人草稿 ID、自由操作的撤销历史、撤销后的未来步骤或播放位置。当前模型仅保存当前场景；已录步骤从录制起点开始。旧 `tool-embed-v1` 保持无 payload，新版单份内容最多 512,000 字节，包含新版的组合页最多 750,000 字节，为现有保存信封预留容量。

Studio、备课、试讲和课堂复用同一固定内容 renderer。编辑器弹窗允许本地观察与前后步预览；组合页及课堂目前只展示固定起点，并保持 `tool-state-v1` 只读 provider。课堂播放、临场编辑和多端同步在后续批次开放；同页游戏的同步权限不会传给立方体磁贴。

- [ ] 分别插入“当前模型”和“已录教学步骤”，核对切割、编组、面染色、标注、透明度和视角；在插入弹窗中核对步骤顺序。
- [ ] 等待课件保存后刷新，选中磁贴并打开“预览固定副本”，确认起点和步骤仍一致；修改原账号草稿后，课件副本保持不变。
- [ ] 按需要走已有审核发布流程；发布后继续修改课件草稿，确认已发布版本不变。课堂起点只读属于本批明确边界。

## 3. M1 实现合同

- `cube-structures-draft-v3` 是本地 Tools 草稿，不是新的 `CoursewareDoc`。不改写冻结的 `spatial-page-v1`，正式课堂同步 provider 保持 read-only。
- 单位块使用稳定 ID；删除再搭建不复用旧 ID。后续移动、编组和标注以 ID 关联，不以变化后的坐标充当身份。
- 整块底色与六个面的染色分开保存。修改底色保留逐面颜色；“清除面染色”保留底色。染色分类只统计明确染过的面。
- 隐藏层只改变可见集合。数量、内外表面积以及“涂外表面”的几何判定使用完整逻辑结构；隐藏相邻块不把其共用面变成外表面。单面工具可对显露的内部面明确染色。
- 记录包含实际对象 ID、面方向、颜色、层、可见性和视角等参数。批量操作重放目标已物化，不依赖重放时的临时选择。
- 日常撤销历史与教学录制分离。开始录制捕获当前结构；替换已有录制或载入新起点需确认。暂停可自由试操作；继续录制若需恢复已录末步，会明确确认，避免未录动作进入后续依赖。回看不修改当前现场，退出回放后继续自由操作。
- 录制完成后可选择某步，在其前一状态重新执行一次操作替换该步；支持拖动、上下移、移动到指定步和删除。搭建／编组重录保留原对象身份。调序、替换和删除验证整段对象依赖与碰撞，失败保留原记录并提示受影响步。
- 图标工具条全部在 4:3 画布内：横屏右侧竖排、竖屏底部横排；视图条始终右上横排。工具参数面板贴近工具栏展开，横屏位于工具栏内侧、竖屏位于工具栏上方；重复点击当前工具可收起／展开参数。组名条固定画布右下角，工具栏为其预留空间。结构／模板设置仍贴近左上齿轮。观察与平移是独立工具；平移允许鼠标左键或单指操作视野，保留双指缩放。
- 选择后输入名称编组，画布内的组名条选择操作范围。组外块保留；批量染色、移动、显隐及新增块使用当前作用域，新块加入当前组。逻辑移动真实改变几何统计；移动面板明确区分逻辑移动与拆开展示。
- XYZ 层列表使用包括隐藏块的完整作用域，逐层恢复与全部恢复入口保留。层面板非模态，切换观察仍可操作画布。显隐按稳定块 ID 保存，移动后仍能恢复。
- 红 X、绿 Y、蓝 Z 轴及对应字母可开关。原点固定在创建时左后下顶点，初始层从 1 开始；增减、移动和显隐不平移原点。空模型在第一块创建时固定原点。
- 普通边线保留原三维粗边；实验性细线与叠加轮廓已撤下。原棱边替色和面高亮方式已获用户验收；本轮按追加反馈将临时选中的黄色独立于组色，首组默认叶绿。选择面板复用填充色板设置新组颜色，并可明确应用到当前组；只更新组的颜色，保留组 ID、名称、成员和顺序，支持撤销／录制。共享棱边仍去重，宽度、位置和遮挡保持原几何。组高亮的描边使用同色系深色，保持与浅色实体和面高亮的对比；取消临时选择恢复组描边，底色与教学面染色保持不变。新配色人工确认见 §2.1；斜视边宽问题单独保留。
- 本机演示从备课记录复制；临场操作不修改备课。重演备课恢复其初始状态与操作记录。
- 共用原有 Orbit 手动控制及平滑视角切换。染色、显隐、选择不触发相机自动重定位；视角按钮与重新居中明确记录观察操作。书签的 `up` 只定义画面朝向，开始手动手势时统一用世界 Y 轴作为轨道竖直轴；俯视极点保留屏幕方位并使用 Orbit 所需的微小离极偏移，避免俯视后绕转轴变成 −Z。Three/R3F 与场景覆盖层共同动态加载。
- 当前容量为 512 个单位块、256 个录制步骤、各轴坐标 −12～12；日常自由操作滚动保留最近 256 步撤销窗口。全部挖除后可在固定原点高度的地面重新搭建。账号持久保存见 §2.3；未保存修改仍只在页面内，界面明确提示。

## 4. M2 / M3 实现合同

- XYZ 切割沿整数层之间的截面进行，保持单位块完整。选线与选面自动共存：靠近可见棱线优先选线，移入面内选面。第一条线只高亮并提示第二步；两条不同共面直线有效后才出现完整截面候选，点击第二条锁定，再点击“切割”提交。共线线段、异面线、斜截面、外侧平面和范围不符分别提示；无效第二条保留第一条。悬浮到面内仍保留第一条线，点击面才转为按面确定截面。两线确定的层界保持与所选线一致，换层界时重新选择；正负移开方向可调整。
- 棱线高亮使用独立组的绘制顺序，位于原粗棱边组之后；单独提高线对象顺序不足以跨过 Three 的组级排序。悬浮、保留第一条以及临时观察／平移时继续显示所选线。切割光标的实际热点与左下笔尖重合，避免图标所指位置与事件落点分离。
- 选面使用实际点击平面：正面层界为 `position[axis] + 0.5`，负面为 `position[axis] - 0.5`，并叠加同轴展示位移。悬浮只高亮命中的小面，点击有效层界面后才显示大截面；外边界／单层方向保留小面高亮并解释不可切，界面不自动移向其他层。空层两侧都有对象时保留实际层界。面来源的锁定截面可明确微调层界，调整时清除旧面高亮。大截面使用零厚度平面、正常填充遮挡与细虚线轮廓，轮廓作为穿透可见的教学辅助；以可见对象决定范围，切割两侧的判定仍包括隐藏对象。
- 每刀首次命中自动确定范围：展示空间中通过面接触相连的单位块视为一部分，部分面接触有效，仅棱／顶点接触不合并；隐藏和透明只影响可见性，不移除部分成员。临时多选是明确的范围覆盖，清除后恢复按点击部分识别。两线过程中保留第一条所在部分，第二条落在其他部分时解释并保留第一条；点击面可重新确定部分与截面。锁定后范围保持不变；确认切割创建新组但回到自动选择，下一刀可直接点击剩余主体或任意分块。分组按钮仍服务其他工具的作用域，旧组不限制自动切割。进度、面板与确认显示本刀实际部分／多选数量，合拢所选部分也使用同一范围。
- 两线或面来源的展示平面都需与本刀范围的法向位移兼容，否则提示合拢或临时选择同一位移的单位块。取消／Escape 清空选择；视图按钮及临时切到观察／平移保留已选线，切割左键／单指专用于选择。沿正／负方向移开 0.5～8 个单位，自动编组并支持连续切割。悬浮、选线、锁定和取消不录制；确认才产生一次既有语义操作。任意斜切与切碎单位块继续作为独立产品边界。
- 切割使用展示坐标建立独立几何索引，不依赖共享 renderer 的颜色分组或实例顺序。面候选剔除内部共用面，最近外露面的等深候选按朝向与几何键稳定选择；棱边按物理线段去重，命中区覆盖实际原粗棱边，并以鼠标 8／触控 12 CSS px 中心线容差补足缩小时的命中范围。实际棱边表面按前后深度优先于邻近容差候选，交点保留 2 px 滞回。切割仍使用数学中心线定面，绘制粗度不改变层界。可见性检查排除被实体遮住的线和仅露出端点的内部线；隐藏块不提供候选。0% 面允许穿透，可见棱边仍可选；大于 0% 的实体先选择最近外壳。面板和选择进度明确本刀实际部分／多选数量及所含隐藏块，并可清除残留临时选择、恢复按点击部分识别。共享课堂 renderer 的现有事件合同保持不变。
- 移动工具的鼠标左键／单指用于轴向移动，不再旋转相机。可拖动 XYZ 箭头，或在面板选轴后拖动实体；单击选单块，拖动沿用当前选择／分组／整体。按当前相机与缩放投影轴向距离，朝向屏幕的轴提示切换立体视角或使用数值按钮。开启右上磁铁后，逻辑移动实时对齐逻辑整数格并保留已有展示位移；拆开展示以被拖动块的实际位置对齐完整单元格，手柄使用首个可见目标块作锚点，整组选用同一位移以保持相对位置。预览与松手提交使用同一吸附终点。关闭吸附时连续预览，松手取整数格（拆开展示为半格）。每次拖动只提交一个既有移动操作；碰撞或超限保持逻辑状态并平滑回位。Escape、指针取消、失焦或切换工具撤销预览。手柄与命中共用世界长度；开始拖动接管未完成的视角动画。非拖动时保留滚轮缩放、右键平移。
- 每块 `displayOffset` 独立于逻辑整数 `position`，以半单位步长调整，各轴偏移限制 ±24。展示实体重叠、超限和无效截面保持原状态并提示。局部合拢发生重叠时保持原状态；整体合拢同时复位全部位移。逻辑几何、外表面染色和统计保持按原结构计算。
- 展示后命中通过展示坐标还原稳定块 ID；相邻面搭建使用原逻辑格并继承该块展示位移。面染色、标记、编号和显隐均跟随同一单位块。重新居中使用包含展示间隙的边界，相机平时仍保持原观察位置。
- 切割、逻辑移动、展示移动、合拢、撤销及步骤重放使用按稳定 ID 插值的平滑过渡，短程约 0.4～0.6 秒、长程最多 0.95 秒。展示层同时插值实体、原粗棱边、面染色和标注，录制只保存确定的终点，不记录中间帧。动画中场景编辑短暂禁用，加载不同起点直接初始化；尊重减少动态效果偏好。
- 圆形、三角形、方形、星形、菱形、十字六种分类符号复用 SVG 路径。支持旁注、点击面和半透明中心；符号和编号可在同块共存，面／中心使用并排留位。中心标注将该块不透明度降至最多 30%，随后可在透明度面板调整。
- 连续编号从 1 开始，记录实际数字与块 ID；重复点击已有编号的块不重复计数。撤销恢复计数器；清除某块编号与“清空全部编号、从 1 开始”分别提供，符号可独立清除。
- 不透明度 0～100% 可逐块或批量应用。滑块使用 1% 精度、44px 高拖动区和 24px 手柄；读数独立即时更新，场景预览每帧至多一次，松手提交一个操作，取消拖动恢复未提交状态。仍可点击单块应用当前值。仅透明度变化时复用原棱边实例，减少无关几何更新。整块底色和逐面染色使用同一不透明度，0% 保留棱边与标注；隐藏依然是独立状态。
- 半透明块使用独立对象中心进行渲染排序。虚线功能启用且存在半透明块时，在更新相机后绘制实体深度 pass，对原棱边材质的被遮挡片段做虚线裁剪；可见片段保留原粗边和选择色。关闭虚线或全部恢复不透明时撤下附加深度 pass。实际像素效果由人工验收，不以 shader 合同测试替代。
- 新工具与现有工具共用备课／本机演示、撤销、独立录制及步骤编辑。切割记录物化两侧对象和新组身份，重录保留组 ID；调序验证几何／对象依赖。正式课堂传输、持久课件、生产部署继续独立。

## 5. 验收入口与窄检查

开发链接：[立方体结构](http://192.168.5.213:3130/zh/tools/spatial-lab)；[English](http://192.168.5.213:3130/en/tools/spatial-lab)。

### 5.1 后续变更的定向回归参考

切割参考：在同一个切割工具中从棱线移入面内，确认自动识别且第一条线仍保留；点击外表面检查小面高亮与外边界提示，隐藏一层后点击外露的内部层界面，确认截面贴合所选面。再用正面内部竖线及顶部共面线完成切割，核对拆开后组内各块的深色原粗描边、撤销与录制一次提交。本次产品验收结论见 §2.1；以下参考在相关代码变化时按风险复用，不作为待验收清单。

连续切割参考：第一刀后直接点击剩余主体，再点击此前移开的分块；检查本刀范围随点击部分变化，不需要切换组。两线跨部分时应保留第一条并解释，锁定和确认只作用于当前部分；撤销、重做及教学录制每刀仍为一个操作。

窄检查覆盖确定性重放、身份稳定、撤销重做、演示副本隔离、空结构与容量、六面邻格、底色/面色分离、批量外表面、XYZ 显隐、真正挖除内外表面积、入口合并、双语键和课堂只读声明。共用 renderer 的相机合同继续复用现有定向测试；机器检查不替代页面视觉、指针命中与教学手感的人工验收。

### 5.2 历次交付的机器检查记录

以下保留各次交付当时的状态与证据范围；其中“待人工验收”为历史状态，M1/M2/M3 当前产品验收结论以 §2.1 为准。六项人工确认的登记批次仅检查文档差异并复用已有机器结果；其后的账号草稿实现与检查另见 §5.3。

2026-09-07 机器结果：最终定向 5 文件 35 项通过（工作台、入口、课堂同步门、面染色、体素渲染运动），共用相机定向 4 项亦通过，受影响文件 ESLint 与 diff 检查通过；zh/en 页面均返回 200 并包含新工作台。全库 TypeScript 仍被既有学服测试中 `NextIntlClientProvider` 缺少 `children` 的 4 处错误阻挡，本次文件无类型错误。未运行视觉自动化、完整发布 Gate 或生产部署。

2026-09-07 七项反馈修订：现有定向 6 文件 50 项通过；新增录制／分组／显隐／原点 16 项与扩展相机 16 项通过。受影响文件 ESLint 通过，zh/en 页面返回 200，包含 v2 与 4:3 工作区且不再渲染画布外模板按钮。全库 TypeScript 另受来源核对脚本相关测试类型错误阻挡；本次文件未报类型错误。以上是机器覆盖结果，边线问题未解决，横竖屏布局、触控手感、选择可读性与步骤编辑仍待人工验收。

追加选择反馈：原棱边替色与面高亮的几何／状态定向检查通过（工作台 14 项、录制与分组 18 项、体素渲染 10 项）；工作台、共享 renderer 及相关测试的定向 TypeScript 检查通过。全库 TypeScript 仍受并行学服及来源核对改动阻挡，未记为全库通过；视觉效果继续等待人工验收。

M2/M3 本轮机器结果：受影响 7 个定向测试文件共 80 项通过（含新增切割／标注／透明度合同，复用同代码状态下的已通过结果），受影响文件 ESLint、定向 TypeScript 与 diff 检查通过。zh/en 开发页面均返回 200，包含 v3 与四个新增工具入口。检查覆盖确定性状态、对象依赖、渲染模型与 shader 拼接；实际 WebGL 像素、触控命中和视觉教学手感仍待产品负责人验收。未运行浏览器自动化、全库 Gate、数据库写入或生产部署。

平滑移动／点面切割反馈修订：5 个受影响定向文件 81 项通过（新增 16 项覆盖六面自动选轴、锁定前不写记录、位移后截面定位、精确动画端点／中间帧、撤销方向、标注和面染色随动、微小间隙棱边去重、面板位置及滑块提交边界）。受影响文件 ESLint 与定向 TypeScript 通过；zh/en 页面返回 200，包含新的运动状态入口。当前为开发端已交付、待人工视觉验收；动画帧率、点面选择手感和真实拖动灵敏度仍由产品负责人确认，未运行浏览器自动化或发布 Gate。

轴向拖动／选线／组色／俯视反馈修订：9 个相关定向文件合计 126 项通过，复用未受后续修改影响的已通过结果。新增检查覆盖原生指针捕获／取消／松手一次提交、轴向投影与吸附、真实 React 预览到动画的无跳回衔接、碰撞回位、层间线命中与交点保持、组色独立及真实 R3F/Orbit 的俯视后手势。受影响文件 ESLint、定向 TypeScript 与 diff 检查通过，zh/en 页面均返回 200。当前为开发端已交付、待人工视觉验收；没有浏览器自动化、WebGL 像素验收、数据库写入、生产部署或正式 Gate 收口。

移动单元格吸附反馈修订：磁铁开关接入移动预览与提交，移动工具中提供中英双语的单元格吸附提示。3 个受影响定向文件 43 项通过，覆盖正负方向、越格与返回原格、半格展示偏移后的整组对齐、开关切换、碰撞、撤销和单次录制；受影响文件 ESLint、定向 TypeScript 与 diff 检查通过，zh/en 开发页面返回 200。开发端已交付，待人工确认吸附手感；未运行浏览器自动化、完整 Gate 或生产部署。

左视图增量：视图条在正视与右视之间增加左视，使用 −X 方向的正交相机及对应 `left` 投影；Y 轴保持向上，沿用平滑切换、重新居中、撤销与录制。中英标签、独立左向图标及相机／投影／重放定向检查通过；2 个受影响测试文件 33 项、ESLint、定向 TypeScript 与 diff 检查通过，zh/en 页面返回 200 且包含左视入口。开发端已交付，待人工验收；未运行视觉自动化或生产部署。

### 5.3 账号草稿 · 2026-09-07 机器检查

版本化保存/往返、完整工具状态与历史、稳定 ID、API 鉴权与输入上限、React 异步恢复及失败保留的定向测试通过；复用同代码状态下已通过的工作台与会话合同。受影响文件 ESLint、定向 TypeScript、数据库生成类型摘要与差异检查通过，zh/en 开发页面可启动。

本机数据库按执行主机、实际 Supabase origin、监听进程、Docker 网络和数据库标识完成只读核对后，应用两份新增迁移。RLS/直接写入拦截/版本冲突通过事务验证并确认回滚。真实 HTTP 使用现有固定账号的两个独立登录会话验证保存、列表、重新打开、更新和冲突保留，另一账号读取/覆盖被拒绝，账号切换保护生效。最初版本冲突请求出现超时，追加迁移将业务冲突与数据库事务重试类错误区分后，真实请求返回明确冲突；验证草稿均已按随机 ID、所有者和精确名称清理。

复用入口：`node scripts/cube-drafts-local.mjs --preflight` 后运行 `--check`；首次应用使用 `--apply`。实际 API 检查使用 `node scripts/cube-drafts-http-local.mjs`，仅面向已核对的隔离开发库并复用固定身份。机器检查不替代跨设备实际浏览器和界面体验验收；本批状态为“开发端已交付，待人工验收”，未运行浏览器自动化、完整发布 Gate 或生产部署。

### 5.4 教师自建课件固定副本 · 2026-09-07 机器检查

内容冻结／往返、有效录制片段、坏引用与容量拒绝、旧版本兼容、共用 renderer 和课堂只读合同的 4 个定向文件共 24 项通过；受影响 ESLint、定向 TypeScript、中英键一致性、数据库生成类型摘要和差异检查通过。额外检查的旧微课 UX 文件有 2 项失效断言，分别仍依赖旧工具栏名称和旧返回链接模板；通过核对本轮修改前的 HEAD 确认既有失配，未在本批改动这些断言。

本机按主机、实际 origin、监听进程、网络与数据库标识完成只读 preflight 后，在事务内应用新增版本并验证数据库白名单、旧工具兼容、保存边界、revision 往返、提交后编辑及真实审核发布的固定副本。全部验证对象和发布记录随事务回滚，随后只应用新增校验迁移；无持久验证课件、生产业务写入或生产部署。复用入口为 `node scripts/cube-courseware-local.mjs --preflight`、`--check`，首次启用使用 `--apply`。

中英文鉴权课件路由 HTTP 返回 200；此检查只覆盖服务器响应，不证明客户端编辑器、WebGL 或实际操作体验。未运行浏览器自动化、全库回归或正式发布 Gate，产品状态保持“开发端已交付，待人工验收”。
