# 学辅共用跟进面板：需求澄清与重构合同

> 规划状态：reference
>
> 整理日期：2026-09-06
>
> 核对代码基线：`c3dcf449`
>
> 当前交付：需求分析与施工合同草案；本文所述目标尚未完成实现，待产品负责人确认与开发端人工验收。
>
> 范围：学辅表格、行下详情及学生入口中的共用跟进能力；不调整当前生产阶段，不表示发布 Gate 关闭。

## 1. 这次反馈要求解决什么

四张反馈截图是同一位对象在不同填写状态下的详情：尚未选择本次结果、已沟通、号码无效、暂缓／下次联系。它们应该是同一个跟进面板的状态变化。

目标是：**以同一个人为主体，使用同一套背景加载、草稿编辑、业务模块、保存和历史读取规则；页面入口只提供当前业务上下文与推荐操作。**

本合同细化产品负责人在 2026-09-06 提供的升级规划中“表格与行下详情”“共用快捷登记”“一次提交、各处一致展示”的要求，不重排整个学辅升级阶段。

需要一起解决四类问题：

1. 表格主行与详情承担不同职责，减少重复输入及状态歧义。
2. 联系结果变化时，核心输入区、动作含义与保存位置保持一致。
3. 跨入口复用同一份身份、事实、记录与模块逻辑，不只复用外壳样式。
4. 保留白色表格和圆点状态表达，以轻量暖色区分选中行，适应普通笔记本。

## 2. 当前问题与代码依据

| 问题 | 现有实现 | 使用后果 |
| --- | --- | --- |
| 大面积冷蓝色覆盖 | `globals.css` 的选中规则同时命中主行及 `data-followup-inline-details`，两者都使用蓝色混合背景 | 与暖色页面、边框和红色主按钮形成多个视觉重点，表单面积越大越明显 |
| 同一结果与备注重复编辑 | `LeadContactEntryRow` 同时在主行和详情渲染结果选择器、备注入口 | 用户难以判断在哪里填写、哪一处算本次操作 |
| 历史结果与新草稿混用 | `displayedOutcome = outcome || lead.lastContactOutcome`；后续可见性及校验却依赖本次 `outcome` | 看上去已选中“最近沟通”，实际本次草稿仍为空 |
| 四种结果不是同一保存流程 | `QUICK_SUBMIT_OUTCOMES` 使“未接通”“号码无效”在选择时直接提交，另外两项只展开表单；数字快捷键调用同一分支 | 选择动作有时写记录、有时不写，尚未完成备注就可能提交 |
| 切换结果清除草稿 | `chooseOutcome` 在部分分支清空微信、意向、邀约及提醒；暂缓也会清空邀约草稿 | 查看或改选结果会丢失未保存输入 |
| 表单骨架跟随结果切换 | `showArrangementDetails` 决定双栏或单栏；备注、提醒及保存动作随分支换位置 | 同一个人连续处理时需要重新寻找输入和保存区，并产生大块空白 |
| 复用主要停留在局部 | `FollowupInlineDetails` 是行下容器；`QuickFollowUpEntry` 是学生备注；首联、邀约、测评各有自己的状态与动作 | 共用一个组件名或容器，尚不能保证跨入口的行为一致 |
| 列表加载承担了部分面板上下文 | `loadCommunicationWorkbench` 先按工作日、范围、分页筛选组合行，再加载相应线索；邀约与线索行读取摘要的分支不同 | 同一对象从不同类型行进入时，当前上下文及摘要口径可能不同 |

依据文件：

- [首联行与详情](../../src/features/school/LeadFirstContactWorkbench.tsx)
- [共享行下容器](../../src/features/school/dashboard-page/FollowupInlineDetails.tsx)
- [普通跟进速记](../../src/features/school/QuickFollowUpEntry.tsx)
- [邀约与组合名单](../../src/features/school/InvitationCoordinationWorkbench.tsx)
- [沟通名单加载](../../src/features/school/communication-workbench-data.ts)
- [表格主题样式](../../src/app/globals.css)

上述是代码核对结论，不是对生产数据内容的审计。

## 3. 三种数据语义分别呈现

| 层次 | 含义 | 加载和编辑规则 |
| --- | --- | --- |
| 已保存事实 | 已加微信、当前邀约、最近一次联系、当前课程等 | 读取权威记录；以只读背景或明确的“更新该事项”模块呈现 |
| 本次跟进草稿 | 这一次新发生的情况、沟通及业务变动 | 默认新建；本次结果和备注为空，或恢复本人的未提交草稿；选择字段只修改草稿 |
| 历史修订 | 刚才记错了，需要修改某一条记录 | 从历史中的“修改这条记录”进入，带记录 ID、版本和原始值，保留修订痕迹 |

具体约定：

- “最近跟进”用于已保存摘要；“本次联系结果”用于本次录入，不再共用一个标签和回退值。
- 已有微信事实正常显示；“本次未确认”表示此次未更新，不等于“尚未加微信”。意向资料也区分未更新与明确修改。
- 已有邀约加载原事项，更新同一邀约；另约其他场次才新建相应事项。
- 单独写情况不需要先选联系结果、下一步或日期。只登记业务结果时，备注也可以为空。
- “号码无效”描述本次所用号码／联系渠道，不自动否定对象身份、既有微信或其他课程关系。
- 新增一次联系与修订旧记录使用不同动作。一次联系附带多个业务结果，沟通次数仍计一次。

## 4. 主行与面板的职责

### 4.1 主行：识别、浏览、进入操作

保留姓名与必要识别信息、本页关键事实、最近已保存摘要和更新时间。提供易找的“记情况”及当前阶段最常用的 2～3 个业务操作，其余放入“更多”。

主行的本次结果选择器、可编辑备注和独立保存按钮移入共用面板，避免同一草稿有两套编辑区。若保留快捷结果入口，其语义统一为“打开面板并预选草稿”，提交仍由面板完成。

有未提交内容时可显示“有草稿”；主行事实及“最近更新”在服务端保存成功后更新，不能把未保存结果当作业务事实。

### 4.2 共用面板：本次情况、业务登记、一次保存

面板有稳定的内容次序。下面是结构示意，不是已经交付的视觉稿：

```text
主行：对象标识 │ 本页事实 │ 最近跟进摘要 │ [记情况] [相关业务操作]
└─ 跟进面板（白色表面；左侧细线与当前主行关联）
   本次情况                 [联系渠道／本次联系结果，按所做操作提供]
   [情况输入：始终可用，完整宽度；不因结果换到另一侧]
   下次联系：未设置，可选

   相关登记：[测评邀约] [活动] [报名] [更多]
   ┌ 已展开的业务模块：当前事项背景 → 圆点进度 → 本次字段 ┐
   └ 字段、数据来源和业务校验由同一模块提供              ┘

   提交状态／待处理提示                       [保存] [保存并下一位]
   ▸ 历史记录（统一来源，按需展开与加载更多）
```

- 嵌入主行下方时减少姓名、电话、来源的重复展示；学生浮层没有可见主行，外层补充对象标识。业务模块需明确当前活动、测评、课程或期次。
- 页面操作可以直接展开对应业务模块并定位它；本次情况输入始终可用，面板骨架及保存规则保持一致。
- 联系结果是实际联系的登记字段，不是整个面板的开关。教师观察、现场反馈等速记使用合适的记录类型，不强迫选电话结果。
- 备注与提醒各有一个编辑位置。提醒空值显示“未设置，可选”，点击后使用同一日期控件；不自动产生待办。
- 联系提醒与测评／活动预约时间是两种事实，分别标注。提醒的适用条件由业务模块和服务端共同校验。
- 历史默认折叠；展开后可修订指定记录。新建与修订有明确模式提示。
- “保存”成功后留在当前行；“保存并下一位”在有连续名单的入口出现，全部必要提交成功后再前进。浮层没有队列时保留普通保存。

### 4.3 联系结果变化时，哪些可以改变

| 本次结果 | 核心保持一致 | 允许变化的业务内容 |
| --- | --- | --- |
| 尚未填写 | 情况输入、可选提醒位置、模块入口、保存区、历史 | 可先速记；按实际选择展开业务，不借用上次结果伪装本次已选 |
| 未接通 | 同上；选择本身不保存 | 可以记录尝试及下次联系；既有身份、微信等事实继续保留 |
| 已沟通 | 同上；选择本身不保存 | 可更新本次确认的资料、意向、邀约等事项 |
| 暂缓／下次联系 | 同上；选择本身不保存 | 可登记暂缓原因与可选提醒；不自动取消既有邀约或报名 |
| 号码无效 | 同上；选择本身不保存 | 记录号码问题；其他渠道和业务事实继续可查看。依业务条件处理号码或渠道，不自动安排重拨 |

切换结果时保留已填写的其他模块草稿。与当前结果不兼容的变动显示解释，允许调整或明确选择“本次暂不提交该项”；保留暂不提交的草稿，不静默写入矛盾结果，也不静默丢弃。

圆点继续表达邀约协调等模块内真实的当前步骤和完成情况。联系方式、联系结果、资料事实与协调进度分别表达，避免用一个大色块代表所有状态。

## 5. “加载一致、逻辑一致”的具体边界

一致性的比较条件是：**同一对象、同一业务事项、同一权限和可见范围。** 不同课程、不同活动或不同权限本来就可能产生不同的可用字段，面板应解释对象及权限差异。

### 5.1 入口传上下文，共用加载器读事实

入口传入稳定的 `leadId`／`studentId`、相关测评／活动／报名 ID、优先展开模块以及可选的队列信息。入口不自行拼一份“该学生状态”，也不决定某个结果是否自动提交。

共用上下文读取包含：

1. 正确身份及已有线索—学生关联，当前正在操作的业务对象。
2. 已保存事实、当前有效事项及服务端版本。
3. 当前角色允许查看和执行的模块与限制原因。
4. 最近记录及完整历史的分页入口，统一事件 ID、作者和服务端发生时间。

名单筛选、工作日期与分页只决定本轮处理哪些行；打开跟进面板后，人的背景由稳定身份加载。查看某日登记时可以过滤历史，但明确显示过滤条件，不将当日片段当作完整历史。

身份聚合优先复用 [Student360 的身份约定](../../src/features/school/student-360-contract.ts) 与 [授权读取](../../src/features/school/actions/student-360.ts) 中适用的能力。现有读取需要评估、适配，不视为已经提供完整编辑上下文。首屏读取必要事实及最近记录，完整历史按需加载，不为每一表格行预取全部档案。

同一学生有多条线索或多个业务对象时使用实际关联；遇到不能唯一确定的事项，业务模块要求选择。单独速记可以只关联明确的主体，不能按相同姓名或手机号擅自合并身份。

### 5.2 草稿共用，业务对象不串用

- 共用草稿控制器管理本次情况、联系结果、可选提醒和各业务模块，不由每个入口复制一组状态。
- 草稿按当前用户、主体、业务上下文及登记会话隔离；相同上下文重开可恢复，另一位学生或另一门课程使用自己的草稿。
- 切换模块、折叠面板、切换行时保护输入；明确放弃才清除。离开页面时有未提交内容的保护。
- 加载中、加载失败、无权限、空历史及保存中使用同一套状态表达。加载未完成时不把未知值当成“没有记录”，也不提交缺失上下文。
- 后台刷新不覆盖已修改字段；检测到事实版本变化时提示核对，保留输入。
- 保存成功后刷新相关事实与列表摘要；继续未完成模块时，已成功部分不再次当作新草稿提交。

### 5.3 一次保存，多个业务结果

共用面板提供一个提交入口，协调本次情况和已修改、已纳入提交的业务模块。

- 仅展开且未改动的模块不校验必填、不提交。不完整的已修改模块提示补齐或明确暂不提交。
- 同域关联写入优先采用事务；确需分步提交时，结果列明成功与失败，保留失败草稿，重试只处理尚未成功部分。
- 提交使用防重标识，阻止重复点击，并支持安全重试。记录 ID、发生时间和版本来自服务端，刷新后的显示与当场反馈一致。
- 全部必要内容保存成功后才执行下一位。成功行在本轮名单中保持位置，主动刷新、切换视图或分页时再按最新筛选整理。
- 服务端重新校验权限、业务状态和需要检查的容量，冲突保留输入并给出处理办法。
- 同一份跟进事实有一个权威来源；联系、测评、活动、报名各自保留业务记录与关联。统一面板不等于把全部专业记录塞进一张表。

当前 [学生跟进 Action](../../src/features/school/actions/followups.ts) 只接收学生身份；[线索联系 Action](../../src/features/school/actions/leads.ts) 及相应 RPC 要求联系结果并有提醒适用条件。要兑现“线索和学生都能只写备注”“跨模块一次保存”，需要统一读取与提交合同、补齐身份支持，不能仅抽取前端组件就宣称完成。

## 6. 配色与普通笔记本布局

### 6.1 建议方向：白底、暖色选中、圆点状态

| 区域 | 浅色模式 | 深色模式 |
| --- | --- | --- |
| 表格普通行、表头 | 不透明白色 `--card` | 不透明深色 `--card` |
| 选中／正在编辑的主行 | 从现有 `--moon` 与白色派生浅暖米色，整行包含固定列连续着色 | 从深色卡片派生低亮度暖色反馈，保持正文对比 |
| 行下详情 | 白色卡片表面，不随展开面积铺满选中底色 | 深色卡片表面 |
| 当前行与详情的关联 | 共用窄暖色边线，颜色从现有 `--crater` 等主题标记派生 | 暖金细线，避免大面积高亮底色 |
| 业务结果、进度、异常 | 圆点、文字及局部语义色 | 对应深色语义色；颜色之外保留文字和形状提示 |

该方案延续“选中整行变色”，将着色范围限定为表格主行；详情通过细线保持从属关系。品牌红继续用于主要动作，业务状态色保留各自语义。

这是替换当前蓝色选中方案的建议。落地时在 [设计系统](01-design-system.md) 的权威规则中同步更新色彩语义，并覆盖显式深色与系统深色模式；本次分析不把未验收色值写成已确定的视觉结果。

### 6.2 容器宽度优先，减少移动视线

- 优先验收 `1366×768`、`1440×900`，同时考虑侧栏占用及浏览器缩放。断点依据面板实际宽度，不只依据整屏宽度。
- 本次情况与保存动作不因联系结果切换到不同栏；通过纵向顺序承接动态模块，不用大空白撑齐两个无关区域。
- 普通笔记本默认以连续主内容区为主。只有业务模块内容确实适合并排且容器足够宽时才分栏，宽屏不把备注挤到很窄的最右侧。
- 选择项完整显示文字，优先自适应宽度及换行；“暂缓／下次联系”“尚未加微信”等关键文案不以省略号作为常态。
- 不为塞入更多按钮压缩正文。次要说明就近放置；长来源信息进入背景或历史，不反复占据面板主体。
- 圆点进度按业务先后横向呈现，空间不足时允许整体合理换行，保留每一步的完整含义。
- 长面板的提交区可在所属滚动容器底部保持可达，同时预留内容空间；验证它不遮住输入、历史或系统弹层。

## 7. 复用、重构、移除、补齐

| 类别 | 对象 | 本轮处理方向 |
| --- | --- | --- |
| 复用 | `FollowupInlineDetails`、圆点进度、日期控件、现有专业字段 | 保留合适的承载与业务表达，统一接收共享草稿和禁用状态 |
| 复用并适配 | Student360 的身份与历史读取、现有 RLS 与服务端业务规则 | 提供共用上下文，保持原可见范围与受保护操作边界 |
| 重构 | `LeadContactEntryRow`、邀约 `InvitationEditor`、`QuickFollowUpEntry` | 接入同一个面板核心，不各自管理另一套通用备注、提醒及保存流程 |
| 重构 | 学生名单、首联、测评与其他现场入口 | 只负责当前名单、对象上下文、推荐模块和下一位定位 |
| 移除替代逻辑 | 重复的行内编辑、选择结果即写入、独立的通用备注／提醒保存分支 | 由统一草稿与提交入口承接；保留业务必要的校验和审计能力 |
| 补齐 | 共用加载、草稿、模块与提交合同 | 支持学生／线索主体、正确事项关联、部分失败、防重、刷新再读与修订 |

共享核心应能在“行下详情”与“学生浮层”两种容器中使用。容器负责布局，模块负责专业字段和校验，统一控制器负责加载、草稿与提交；同一业务模块在不同入口不复制实现。

## 8. 施工批次与验收边界

### 8.1 施工顺序

1. 以本合同明确主行去重、状态语义、白底暖色方向及统一保存动作。先形成真实可用的共享核心，不以另一套皮肤作为跨入口复用完成。
2. 打通原升级规划要求的“测后未报名”纵向路径：测评行下只记情况／填写成绩与反馈／可选活动登记 → 保存或保存并下一位 → 学生入口和相关业务入口刷新再读同一结果。让学生入口同时接入该核心。
3. 首联接入同一核心，补齐线索主体的普通速记；将本次截图中的各联系状态迁入同一骨架，统一快捷键、草稿及提交含义，保留邀约圆点进度。
4. 邀约协调及其他工作现场逐项接入同一模块实现。报名、续班及退课按原规划和专业权限另行形成业务批次，不借本次面板整理改动生产流程。

每个可独立验收的增量运行覆盖其风险的定向检查后交付。涉及 Action、RPC 或数据库时补充对应身份、权限、事务及重复提交测试；数据库执行前另做实际目标核验。本合同不授权生产写入或开发数据清理。

### 8.2 核心验收清单

| 编号 | 场景 | 通过条件 |
| --- | --- | --- |
| F01 | 同一个人、同一事项从测评、首联或学生入口打开 | 身份、事实、历史、相同业务模块的数据与校验一致；入口只改变推荐／初始展开及队列能力 |
| F02 | 不选联系结果、不设日期，只写本次情况 | 学生及线索均可按权限保存并刷新再读；不自动生成联系任务 |
| F03 | 来回切换四种联系结果、使用数字快捷键 | 没有隐式写入；备注、提醒及其他模块草稿保留，核心布局及保存含义不变 |
| F04 | 已加微信、已有邀约，打开新一次跟进 | 既有事实正确加载；空白本次结果不会覆盖旧事实；更新已有事项不重复新建 |
| F05 | 展开测评模块但不填成绩，仅保存情况 | 未改动模块不阻挡普通速记；已填但不完整的模块可明确暂不提交并保留草稿 |
| F06 | 一次联系填写情况并登记多个业务结果 | 对象关联正确，沟通次数为一次，跨入口刷新再读相同结果 |
| F07 | 重复点击、提交超时、部分失败后重试 | 不重复记录；展示真实成功／失败，保留未成功输入，必要内容完成前不跳下一位 |
| F08 | 保存后对象不再符合当前筛选；切换下一位再返回 | 本轮名单位置稳定，上一位成功内容可回看；草稿及课程上下文不串到下一位 |
| F09 | 修改旧记录，然后另记一次新联系 | 修订保留原作者、发生时间与痕迹且不增加次数；新联系追加新记录 |
| F10 | 某日名单进入详情，再查看完整历史 | 日期过滤有明确标识，完整历史仍可访问；刷新后 ID、时间和摘要稳定 |
| F11 | 1366×768、1440×900，浅色及深色 | 主行选中清晰、详情表面干净；关键文字完整，备注及保存可达；圆点含义可读，无无效大块空白 |
| F12 | 不同权限、多课程、多线索或并发修改 | 身份及业务对象明确，服务端权限未扩大，冲突保留草稿并要求核对 |

机器检查只证明对应合同。布局、颜色和操作体验由产品负责人在开发页面人工验收；代码完成后使用“开发端已交付，待人工视觉验收”状态，不等同于用户已验收或正式 Gate 已收口。

## 9. 本次整理的交付状态

- 已完成：截图问题归因、现有组件与读写路径核对、统一面板需求合同、施工拆分及验收清单。
- 尚未完成：共享核心实现、旧分支替换、色彩落地、跨入口持久化验证与人工视觉验收。
- 本次没有修改页面、执行数据库写入或部署生产；现有开发页面仍是代码基线的视觉与行为。
