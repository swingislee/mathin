> **规划状态**：`partial`
>
> **当前用途**：R1-5 教师备课生产链，以及开发轨 `DEV-TMC-1` 普通教师班级级微课孵化与校内共享、`DEV-TMC-2` 课次课件多方案协作与选用、顶层管理员兼任教师/教研岗位的专题合同。
>
> **已落地**：左侧三步备课切换、右侧常驻课件预览、与上课教室共用且可向下收起的白板工具栏和 BoardItem 对象层、解析成品查看与 WebP/原件导出、可见 revision 审核链、BlockNote 标准模板与备课质量状态、开课前 14/7 天备课提醒与课后“补交”标记，课次级课件方案的编辑、派生、任课教师选用、课堂冻结、教研审核和注册表驱动游戏页。旧 `DEV-TMC-1` 把每个课次方案各自投影成一门单讲课程，该目录聚合不符合本文件 2026-08-29 更正后的班级级合同。
>
> **剩余项**：把同一自由班的多个课节课件聚合为一门教师微课、按班级年级/运营学期进入共享课程族、以多讲课程供其他教师查看和建班；独立教案管理页面的创建/绑定入口、服务端打印 PDF 派生文件，以及 `DEV-TMC-1/2` 的产品负责人生产页面写态验收。页面级教学备注已按产品判断暂缓。R1-Live 中备课产物、审核和检查项只作质量提示，不阻断教师冻结快照或开课；Production 1.0 再评估质量门。
>
> **最后核对**：2026-08-29；既有教师生产链依据迁移 `20260730010500_doc26_teacher_workflow.sql`、`20260731000700_doc26_annotation_board_items.sql` 与 `20260823000200_r1_live_preparation_attention_window.sql`、隔离/生产数据库断言、定向合同测试及产品走查。`DEV-TMC-1` 已于 2026-08-27 首轮部署并保持 `teaching.teacher_microcourses_v1` version 2 / true；`DEV-TMC-2` 与班级重新启用增量以 commit `087b497…` 部署。2026-08-29 又以 migration `20260829000200_admin_self_staff_roles` 与应用 `ba98a8e…` 放行顶层管理员管理自己的员工岗位，app-only `bf81aa4…` 修正教研在普通课次页的课件入口，app-only `6185352…` 修正空 kind 来源绑定的 H5 解析和待审微课快照入口；ledger 保持 `220`，current/previous=`20260829-045135` / `20260829-033955`。生产只读核查确认现有 1 个自由班的 3 个课节被旧模型错误拆成 3 门单讲草稿，且 2 门 `courseSeason=null` 触发共享课程详情 Zod 500；目录兼容修复已在本机提交，班级级数据迁移和生产发布均未获授权、尚未执行。

# Mathin 教师备课生产链升级规划

## 文档目的

本规划用于指导 Mathin 教师端备课工作流升级。

本次升级围绕当前 `/dashboard/sessions/[sessionId]`
课次工作区展开，将课件预览、试做记录、解析记录、标准教案整合为连续生产流程。

目标：

-   教师在课件页面完成试做与板书记录；
-   板书记录直接生成解析材料；
-   教师在教案阶段完成结构化教学设计；
-   教案与当前课次、课件形成绑定；
-   保留上传入口，形成双入口生产模式；
-   继续复用现有审核、revision、材料状态体系。

------------------------------------------------------------------------

# 一、当前实现与设计约束

当前教师备课核心入口：

    /dashboard/sessions/[sessionId]

该页面已经形成课次生命周期：

    课前
     ├── 研读课件与试做
     ├── 形成教案与课堂检查
     └── 磨课、调整并定稿

    课堂
     ├── 点名
     └── 行课

    课后
     └── 复盘

当前页面布局：

    左侧：课前流程与材料任务

    中间：课件目录

    右侧：课件预览工作区

因此：

-   板书属于课件页面能力；
-   教案属于教学设计阶段能力；
-   教案不采用右侧浮窗形式，避免破坏课件预览区域。

------------------------------------------------------------------------

# 二、整体升级目标

升级后：

    课件工作区

        |
        +-- 页面试做
        |
        +-- 板书记录
        |
        +-- 生成解析记录


    教案工作区

        |
        +-- 教学设计模板
        |
        +-- BlockNote编辑
        |
        +-- 标准教案提交


    审核流程

        |

    课堂执行

------------------------------------------------------------------------

# 三、功能一：课件页面板书系统

## 目标

教师在当前课件页面直接书写：

-   解题过程；
-   思路分析；
-   页面批注；
-   课堂讲解痕迹。

完成后直接生成解析记录。

------------------------------------------------------------------------

## 交互位置

保持当前：

    CoursewarePreviewWorkspace

增加：

    翻页

    书写

    生成解析

板书采用：

    课件页面

    +

    Canvas书写层

不新增独立页面。

------------------------------------------------------------------------

## 数据模型

新增：

    courseware_annotations

字段：

    id

    session_id

    page_doc_id

    user_id

    annotation_type

    content

    created_at

    updated_at

其中：

    annotation_type = board

content 保存白板 `BoardItem[]` JSON，包括归一化笔迹与图形；尺规本身是临时教具，不进入持久化。

------------------------------------------------------------------------

## 保存原则

不以图片作为主数据。

保存：

    BoardItem[]

原因：

-   可编辑；
-   文件小；
-   可重新渲染；
-   支持未来 AI 分析。

解析板书直接复用上课教室的 `Toolbar`、`CanvasSurface` 与对象状态合同，不维护独立简化工具栏。图形、颜色、填充、线宽、尺规、撤销、清空和导出随共享工具栏同步升级；后续计算器与函数图像也应先进入这条共享能力链，再由课堂与解析场景共同获得。

公式识别暂不进入当前版本。仓库不保留识别入口、识别服务、部署依赖或公式对象持久化合同；未来若接入自研手写识别库，应重新立项并定义笔画、候选纠错与输出对象的数据迁移。

------------------------------------------------------------------------

## 导出解析记录

现有解析流程保留。

增加来源：

    solution_source

    upload

    board

流程：

    板书保存

    ↓

    生成解析记录

    ↓

    revision

    ↓

    审核

------------------------------------------------------------------------

## 图片输出

解析成品不是脱离课件的空白板书。每条 `board` 解析以稳定 `page_doc_id` 找到对应 `PageDoc`，把该页 4:3 渲染作为底层，再叠加同 revision 的 `BoardItem[]`。教师档案弹窗、审核页预览与 WebP 导出复用同一个合成 DOM；关联页不可用时明确提示并禁用导出，不得退化成空白底图。

展示与导出：

    PageDoc 4:3 + BoardItem[] → 1920 × 1440 WebP

不使用 PNG 作为默认格式。

当前客户端派生质量为 `0.82`；Vector/BoardItem 与 PageDoc 继续作为可编辑权威数据，WebP 只承担审阅与交换。

打印：

单独生成 PDF。

------------------------------------------------------------------------

# 四、功能二：左侧教案流程步骤

## 设计原则

教案不是课件旁边的辅助栏。

它属于：

    形成教案与课堂检查

阶段的主要生产任务。

因此采用：

    左侧备课流程步骤切换

右侧课件目录与预览始终保留，不以教案编辑器替换。

------------------------------------------------------------------------

# 五、教案编辑工作区

进入：

    形成教案与课堂检查

后：

    左侧：

    标准教案编辑器
    上传教案入口


    右侧：

    常驻课件目录
    4:3 课件预览
    页面板书层

教师边查看右侧当前课件页，边在左侧完成教学设计；不新增重复的课件参考窗口。

------------------------------------------------------------------------

# 六、教案模板

首次创建自动生成模板。

模板：

## 一、课前判断：学情三问

### 1. 当前学生的学习情况是什么？他们遇到了什么问题？

已有基础：

主要问题：

典型表现或证据：

### 2. 基于这些问题，老师需要往哪个方向努力？

本阶段教学方向：

优先解决的问题：

暂时不作为重点的问题：

### 3. 本节课可以在这个方向上为学生提供什么帮助？

本节课提供的具体帮助：

判断学生是否获得帮助的证据：

------------------------------------------------------------------------

## 二、课程设计

### 1. 授课对象

年龄群体：

能力描述：

班级特点：

### 2. 教学目标

知识目标：

能力目标：

情感目标：

### 3. 教学流程

### 4. 教学重难点

重点：

难点：

学生可能卡住的位置：

### 5. 教学方法

主要教学方法：

使用这些方法的原因：

------------------------------------------------------------------------

## 三、作业设计

作业内容：

完成标准：

与本节课目标的关系：

------------------------------------------------------------------------

## 四、课后反思

学生实际表现：

本节课有效的地方：

本节课需要调整的地方：

下节课需要调整的地方：

是否需要更新学情版本：

------------------------------------------------------------------------

# 七、编辑实现

复用当前 BlockNote。

不新增编辑器。

数据：

    lesson_plans

字段：

    id

    session_id

    template_version

    content

    status

    revision

    created_by

    updated_at

template_version:

    mathin-teaching-plan-v1

用于未来模板升级。

------------------------------------------------------------------------

# 八、页面级教学备注（暂缓）

当前课次备课的核心动作是边看右侧课件、边完成整体教学设计。页面备注暂时缺少独立完成门、审核对象和课堂消费位置，会增加左侧认知负担，因此当前工作流不显示页面选择器和页备注输入。

迁移中已有的 `lesson_page_notes` 只作为休眠的预留结构保留，不查询、不写入，也不参与完成状态。未来只有在课堂执行或教案审阅形成明确的页级消费场景后，才重新评估入口。

------------------------------------------------------------------------

# 九、双入口

## 解析记录

入口：

1.  板书生成解析

2.  上传解析文件

统一进入：

    solution_records

## 教案

入口：

1.  session 工作区创建

2.  教案管理页面创建并绑定课次

------------------------------------------------------------------------

# 十、开发顺序

## Phase 1：左侧教案流程步骤

实现：

-   lesson_plans 数据结构；
-   BlockNote模板；
-   教案编辑工作区；
-   revision审核。

## Phase 2：板书系统

实现：

-   annotation数据；
-   Canvas；
-   页面绑定；
-   导出解析。

## Phase 3：融合

形成：

    课件

    ↓

    试做

    ↓

    板书解析


    课件

    ↓

    教学设计

    ↓

    标准教案


    ↓

    课堂执行

    ↓

    课后复盘

------------------------------------------------------------------------

# 十一、实现约束

1.  不新增独立课件查看页面。
2.  教案编辑器只出现在左侧备课步骤；右侧始终承载课件目录与预览。
3.  复用现有 Session Workspace。
4.  复用 BlockNote。
5.  图片采用压缩格式。
6.  保存结构化数据。
7.  保持当前备课流程连续性。

## BlockNote Markdown 语法评估（2026-07-31）

当前锁定的 BlockNote 0.51.4 已内置常用 Markdown 输入规则：块级覆盖 `# ` 标题、`- `/`+ `/`* ` 无序列表、`1. ` 有序列表、`[ ] `/`[x] ` 检查项、`> ` 引用、`---` 分隔线和三反引号代码块；行内覆盖 `**粗体**`、`*斜体*`、`~~删除线~~` 与反引号代码。粘贴 Markdown 会自动解析；`tryParseMarkdownToBlocks` 和 `blocksToMarkdownLossy` 分别提供导入与有损导出，BlockNote JSON 继续作为无损权威存储。

| 范围 | 结论 | 预计工作量 |
| --- | --- | --- |
| 让教师直接使用现有块级与行内快捷语法 | 已具备；只需补一个轻量帮助入口和回归测试 | 0.5 天，低风险 |
| 增加 Markdown 文件/文本导入与导出按钮 | API 已具备；需要覆盖覆盖确认、文件校验和有损能力提示 | 0.5～1 天，中低风险 |
| 增加链接、表格等尚未内置的实时输入语法 | 需自定义 BlockNote/Tiptap input rule，并处理撤销、中文标点和粘贴一致性 | 1～2 天，中风险 |
| 提供 Markdown 源码与可视化编辑双向切换 | 需要解决表格、媒体、嵌套块和未来自定义块的无损往返 | 3～5 天，高风险 |

当前建议先显式告诉教师已经可用的块级与行内快捷语法；若需要交换文件，再增加导入/有损导出。源码双模式不进入 doc 26 当前施工范围。

------------------------------------------------------------------------

# 十二、2026-07-30 实现证据与边界

已实现：

- `lesson_plans`、`lesson_page_notes`、`courseware_annotations`、`solution_records` 四个 RLS 对象及受控 RPC；
- 左侧切换带同一行容纳三个备课步骤与课次审校人；进入教案步骤时创建固定版本模板并自动保存，右侧课件目录与预览保持常驻；选项卡用虚线圆、时钟、警告和绿色勾选表达未完成、待审核、需修改和已完成，状态文字只保留在无障碍名称与悬停提示；课件目录的保存状态也使用带无障碍名称的云端状态图标，不再占用一段标题文字；
- 复用课堂白板 `Toolbar`、`CanvasSurface` 与 `BoardItem` 对象层，保存归一化笔迹和图形，按稳定 `page_doc_id` 绑定并生成统一解析记录；教师档案与审核页均显示 `PageDoc` 课件底图和板书对象的 4:3 合成形态，导出同一合成为 1920×1440 WebP；上传解析可下载原文件。共享工具栏可向屏幕下缘收起/展开，备课场景把工具栏放入翻页工作区并移除常驻键盘提示。尺规保持临时态，未来计算器与函数图像沿同一共享工具链扩展；
- 上传解析继续保留，并投影到 `solution_records(solution_source='upload')`；迁移补投影旧上传记录，非解析字段保存不增加其 revision；
- 复用 `session_preparation_reviews`、通知和备课质量状态；备课审核队列成为课件侧栏显式入口，审核人将解析/教案与同课次真实课件并排审阅并通过或退回，决定同步教师状态与教案状态；产物入口返回对应备课步骤，所选课件页以稳定 `page_doc_id` 直达编辑位置；待审核教案允许原提交教师显式撤回并恢复草稿。课次新增显式审校人，第一阶段由教师选择并允许本人，队列只对所选审校人与管理员开放；assignment source 同时预留主管指定，切换后教师不可覆盖。上述状态在 R1-Live 是可见的质量信号，不是冻结快照或开课的服务端前置条件。备课工作项只在开课前 14 天出现、前 7 天到期，逾期后向主管投影；开课后完成的备课在课次工作区标为“补交”；
- BlockNote 教案使用紧凑排版，附件入口和编辑器标题区均为单行；标题缩小后同步校准一至三级块句柄命中高度，避免侧边菜单覆盖相邻块。正式模板页面以不可变 `ref` 展示，教师不能重命名；历史误存的同 ID 页面副本会自愈为模板引用，只有教师新增页可编辑标题。页面教学备注已从工作流撤下，其预留表不再由课次页查询或写入。
- 组织设置新增默认关闭的 `teaching.preparation_archive_edit` 开关。管理员临时开启后，任课教师可补改当前课次快照与档案：重排页面，增删白板、视频和图片，补勾本课逐生检查页，补写板书并重新生成/导出解析，修订包含课后反思的结构化教案，以及替换或删除磨课百度网盘链接。保存后重新进入教室读取新版课次快照；正式 release、正式页面文档与课次审校人始终不变，关闭开关即恢复只读。

当前未实现：

- 课次工作区以外的教案管理页面创建/绑定入口；
- 服务端打印 PDF 派生文件（当前教师与审核人已可按需导出 WebP，上传原件可直接下载）。

因此既有教师生产链部分保持 `partial`；当前施工阶段仍由 doc 04 决定。

------------------------------------------------------------------------

# 十三、DEV-TMC-1 普通教师班级级微课

## 2026-08-29 产品语义更正（权威）

“教师微课”的根对象是一个自由班，不是一个自由课次。老师可新建自由班并生成多个课节；每个课节各自选择、导入或从空白制作课件方案。同一班级中按课节顺序选用的多份课件共同组成一门教师微课，课程族页面按班级年级与运营学期归档，页面坐标和爱学习、魔法校等课程产品保持一致。

| 对象 | 权威含义 | 结果 |
| --- | --- | --- |
| `classrooms` | 一门教师微课的来源根；名称、年级、`term_id` 和任课关系来自真实自由班 | 一个自由班只产生一门当前教师主线微课，不按课次数量拆课程 |
| `class_sessions` | 微课中的课节顺序与真实排期 | 每个有效课节对应共享课程的一讲 |
| `teacher_microcourses` | 某一课节的可编辑课件方案；同课节可有教师/教研并行方案 | 任课教师的“本节使用”决定课堂冻结；只有纳入班级微课的方案进入该课程的讲次集合 |
| `courses(course_kind='microcourse')` | 班级级教师微课目录投影 | 同一课程包含多个 `course_lectures`，标题/年级/学期属于课程，不属于某一讲 |
| `course_lectures` | 按来源课节排序的微课讲次 | 每讲保留自己的 page/resource/H5/game revision、审核与 immutable release |

保存和共享使用当前班级各课节的“本节使用”方案。某课节没有方案时在课程页显示未完成讲次，不伪造空 release；课次冻结后继续读取当时快照，后续保存或发布不改写历史课堂。并行方案仍属于课次协作能力，不能把同一班级拆成多门默认微课；未来若要保存另一套完整课程方案，必须显式执行“另存为一门微课”，而不是按方案数隐式分组。

## 当前状态与证据

| 子门 | 状态 | 证据范围 |
| --- | --- | --- |
| 本机实现与机器检查 | **PASS** | 基础 commits `cd3b2bf`、`26698e3`、`96f3301`、`a4fa9e4`、`5c13d5d`、`f418dd9` 与迁移 `20260826000200`～`20260826000600` 已通过三份事务回滚 SQL、微课 Vitest 17/17、固定账号 Playwright 1/1 和 R1-Live 60/60；整讲来源增量 commit `dd9a755`、migration `20260827000100_teacher_microcourse_lecture_source_picker` 通过内容 SQL、微课 Vitest 4 文件 19/19 与完整旅程；通用游戏页增量 commit `1135099`、migrations `20260827000300`～`20260827000400` 通过回滚式内容 SQL、定向 Vitest 6 文件 47/47、固定账号 Playwright 1/1 及最终 CI 16/16，全量 Vitest为 106 文件、742 通过/1 条件跳过且 production build 成功；课程产品选择器复用增量 migrations `20260827000600`～`20260827000700` 已通过事务回滚、固定教师权限矩阵、微课 Vitest 4 文件 21/21 与完整 Playwright 1/1 |
| 产品负责人开发端初验 | **BLOCKED BY MODEL CORRECTION** | 课次编辑与课堂选用可继续验收；班级级保存、目录和多讲建班必须按本节更正后重新交付 |
| 生产迁移与启用 | **DEPLOYED LEGACY MODEL / CORRECTION NOT DEPLOYED** | 既有课次方案、审核、release 与课堂冻结能力已上线；生产当前 1 个自由班/3 个课节/3 个已选方案仍被旧模型表示为 3 门单讲草稿。班级级聚合迁移、目录修正和已有数据归并尚未获得生产授权 |

完整机器证据见 [`../evidence/r1/teacher-microcourse-dev.md`](../evidence/r1/teacher-microcourse-dev.md)、[`../evidence/r1/teacher-microcourse-research-entry-hotfix-production.md`](../evidence/r1/teacher-microcourse-research-entry-hotfix-production.md) 与 [`../evidence/r1/teacher-microcourse-source-preview-hotfix-production.md`](../evidence/r1/teacher-microcourse-source-preview-hotfix-production.md)。本机随机 E2E 课程族、班级、课次和微课项目已按精确名称/ID 清理并复核为 0；固定开发账号与既有基线数据未改变。

## 用户流程与目录映射

固定流程为：

    自由班
      → 生成多个课节
      → 每个课节导入或自制课件，并选择“本节使用”方案
      → 按课节分别试讲、冻结与审核
      → 保存为一门包含多讲的教师微课
      → 按年级/学期进入“教师微课”课程族
      → 其他教师查看完整教学计划并建班

“教师微课”是一个校内 `course_families` 课程包；一个来源自由班保存为一门 `courses.course_kind='microcourse'` 课程，班内多个课节对应多个 `course_lectures`。年级取自由班年级，课程季节取班级 `term_id → school_terms.term`；同一课程族、版本、年级和学期允许多名教师保存不同微课。发布只推进对应讲次的审核元数据与 immutable release；全部有效讲次均有可用 release 后课程才进入其他教师的新建班目录。保存和发布均不把来源自由班改成课程班，也不回写已经冻结的课次。

## 元数据、权限与审核

- 一个自由课次可关联多个课件方案；一个班级级微课在每个来源课次最多纳入一个方案。方案保存作者、来源课次、所属班级微课、讲次、草稿元数据 head 与已发布元数据 head；每次修改生成不可变 revision。
- 主主题使用可增停的校方目录，首批固定为数与代数、图形与几何、统计与概率、逻辑与策略、综合与实践；自由关键词独立保存。
- `courseware.microcourse.author` 只允许作者操作本人且仍由本人任教的来源自由课次，不推导 `course.manage`、`courseware.page.edit` 或其他 Studio 权限。作者、来源课次教师、教研审核者和管理员可读草稿；校内其他教师只读已发布 revision/release。顶层管理员同时承担教师/教研工作时，可在员工页给自己授予对应岗位；普通 staff 仍不能自授岗，管理员自停用也继续拒绝。该能力已随 `20260829000200_admin_self_staff_roles` / `ba98a8e…` 部署，机器 postflight 通过，实际生产自授岗待产品负责人验收。
- 提交时把页面 revision、资源 revision、H5 SHA-256 与元数据 revision 固定到同一审核事实。审核期间继续编辑只推进草稿 head，不替换已提交快照。退回后重提生成新的提交快照；通过与发布复用 `cw_lecture_workflows`、`cw_review_cycles` 和 immutable release，并在一个事务内推进 release、课程投影和目录可见性。
- 发布后修改仍须重新审核。撤回只把课程从新建班目录隐藏，历史 release、既有班级和已冻结课次保持可读。

## 页面能力与来源边界

教师自建页使用两层版本化合同：`microcourse-page-v1` 继续承载组合页、历史数独页与 H5；所有新建游戏页使用通用 `game-page-v1`。旧 revision 不回写。

| 模式 | 权威内容 | 提交门 |
| --- | --- | --- |
| `composition` | 可选的来源页不可变快照、来源 release/revision/出处，以及教师自己的文字、富文本/公式、图片和形状叠加层；没有来源基底时承载标题、题目或图文页 | 文档与资源引用合法 |
| `sudoku`（兼容） | 历史 81 格手工原型题、展示选项和服务端求解结果 | 永久可读/可编辑；草稿可不完整，提交必须无冲突、可解且唯一解 |
| `game-page-v1` | `gameId + contentVersion + payload` 与服务端校验事实；首个 `sudoku-authored-v1` 适配器显式保存稳定 `variantId`，支持四宫、六宫和九宫 | 草稿可不完整并可立即试讲；提交要求当前 revision 存在同 payload hash、validator version 与详情的不可变凭证，且 `publishable=true` |
| `h5` | 单文件 HTML artifact ID、规范化内容 SHA-256 和入口 | 不超过 5 MiB；审核快照中的同一 hash 已准备好提升为不可变包 |

来源入口直接复用建班课程产品的 `CoursePicker` 和 `list_class_build_course_variants` 筛选逻辑：老师先按课程族/版本、年级、课程季节、班型和历史版本筛选一门正式 `curriculum` 课程，再从紧凑列表按课次名称和总页数选择整讲。选择阶段不加载页面预览文档、缩略图或签名素材；一次事务按当前 release 顺序插入本讲全部页面后，老师在微课编辑器中删除、移动或补充页面。普通教师不因此获得 `class.create`：共享目录 RPC 保留建班权限分支，并为 `courseware.microcourse.author` 增加只能访问 `purpose='production'`、`course_kind='curriculum'` 的固定分支。整讲插入复制每页文档快照和绑定 revision，任一页面或素材快照失败时整讲回滚；资源字节继续复用 CAS。来源格式由统一 revision capability predicate 判定，已登记、可复制且有发布校验凭证的 `game-page-v1` 自动进入同一链路，新增游戏内容版本不改搜索或复制 RPC。首版仍不把教师微课作为来源。普通教师不能改变来源基底，只编辑叠加层或前后自建页；教研对原生 `page-doc-v1` 保留逐元素编辑能力，爱学习与空间页等导入基底仍只编辑叠加层。

## H5 与验收边界

H5 首版只有 HTML 编辑框、防抖预览、运行错误和本地素材 data URL。草稿进入作者域隔离的私有对象路径，发布时按规范化内容 SHA-256 提升到 `cw-h5/packages/<sha256>/`；iframe 只使用 `sandbox="allow-scripts"`，CSP 禁止网络、表单、外层导航、子 iframe 和插件，仅开放内联脚本/样式与 `data:`/`blob:` 媒体，并继续注入课堂指针桥和内存 storage shim。

目录查询只返回 `enabled`、至少一讲且全部有效讲次均存在正式 release 的班级级微课，支持发布教师、课程/讲次标题、主题、关键词、年级及学期筛选。验收至少覆盖既有课程唯一性、同班多课节归为一门课程、同课节并行方案不重复成讲、他人草稿拒绝、审核快照不可替换、4/6/9 宫数独四种结果、游戏校验凭证不可替换、H5 CSP、自由课次冻结、退回重提发布、多讲建班、旧课次钉死旧 release、新班使用新版，以及普通教师/教研 zh/en 固定账号旅程。首轮旧模型生产迁移和开关启用已在 2026-08-27 取得明确授权并完成；本次班级级 schema、已有数据归并、release 与开关变更必须重新取得生产授权。

# 十四、DEV-TMC-2 课次课件多方案协作与选用

## 当前状态与证据

| 子门 | 状态 | 证据范围 |
| --- | --- | --- |
| 本机实现与机器检查 | **PASS** | 两份 migration 回滚断言、TypeScript、双语键、全库 ESLint、定向 Vitest 8 文件 64/64、固定账号新旧 Playwright 2/2、CI 16/16、全量 Vitest 108 文件 761 通过/1 条件跳过及 production build 通过 |
| 生产迁移与应用 | **DEPLOYED / PENDING USER ACCEPTANCE** | 明确授权后，候选 `087b497…` 同时纳入 `20260827000800_classroom_reactivation` 与两条多方案 migration，明确排除班级课型/活动类型增量；全量备份、完整 rollback/零残留/formal、同 schema 双 release 与独立 postflight 通过，ledger=`211`，current/previous=`20260828-071313` / `20260828-071024` |
| 产品负责人生产写态验收 | **UNKNOWN** | 登录态只读浏览器已打开生产 4:3 审核工作区并确认新流程与正常空状态；未创建生产方案、未选择/冻结方案、未点击班级重新启用 |

2026-08-29 的生产验收截图暴露普通课次页仍把“编辑课件”绑定到任课教师 `canPrepare`，与本节“制作权和教学运营权分离”的合同冲突。app-only `bf81aa4…` 已允许教研从该课次页直接进入独立方案工作区，同时保留备课档案只读并隐藏试讲、完成备课和最终选用；本机固定账号旅程与生产机器 postflight 通过，产品负责人刷新页面确认前仍保持 pending。

## 产品动作与对象

一个自由课次可以关联多个课件方案。任课教师和具备 `courseware.review` 的教研都能从空白开始制作；打开本人方案直接续编，修改他人方案时由系统在一次事务内复制当前元数据、页面 revision、绑定、H5 内容引用与游戏校验事实，生成带来源关系的新方案后进入编辑，不覆盖来源 head。方案是并行教学设计，方案内部的连续输入仍只产生自动保存 revision，不把每次保存暴露成新方案。

课次使用权与内容制作权分离：任课教师可以查看所有方案并设置 `本节使用`，教研可以跨班进入课件内容工作区但不能改变课次选用、花名册、点名、上课状态或其他教学运营事实。只有一个方案且课次尚未选择时，创建事务可自动选中该方案；新增第二个及后续方案不得自动抢占既有选择。

## 选用、冻结与共享

常规课次工作区和微课编辑器读取同一选用事实。开课时冻结所选方案当时的 metadata/page/resource/H5/game revision；冻结记录保存方案 ID 与来源关系，后续编辑或切换其他未冻结课次不影响当前和历史课堂。已开课或已冻结课次拒绝改变选用方案。

课次内制作、派生、选择、试讲和上课不依赖审核。班级保存教师微课时读取各课节当前“本节使用”方案；每讲分别进入 DEV-TMC-1 的提交、审核、发布、撤回和 immutable release 流程，课程在全部有效讲次可用后进入共享目录。发布不会改变课次当前选用，也不会把自由班转换成正式课程班。

## 首个开发验收

- 老师和教研均能从空白建立首个方案；教研访问只暴露课次标题、班级名称、排期和课件方案，不暴露花名册与课堂控制。
- 修改他人方案自动派生；来源方案和派生方案的 head、页面顺序与后续自动保存互不覆盖，来源方案 ID 和基准 metadata revision 可追溯。
- 只有任课教师能设置本节使用；单方案可自动选中，多方案保持既有选择，已冻结/开课后拒绝切换。
- 常规开课入口与编辑器开课入口都冻结同一已选方案；无选用方案时继续沿用既有空白课堂降级，不引入新的硬阻断。
- H5 私有内容引用、上传图片 CAS、来源课程页面与 `game-page-v1` 校验事实在派生后仍可预览、编辑、提交共享审核并保持 RLS 隔离。
- zh/en 固定开发账号覆盖普通教师制作/选用、教研创建修改方案、来源 head 不漂移和课堂冻结；本轮生产迁移与应用已取得明确授权并完成，后续 schema、release、权限或开关变更继续要求新的明确授权。
