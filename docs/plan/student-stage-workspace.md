# 学生阶段工作台 · 开发交付

> 2026-09-08：开发端已交付，待人工视觉与交互验收。本增量属于 DEV-SCHOOL-OPS-1，不改变 doc 04 的当前施工阶段、生产状态或 Gate。

学生页按人展示永久名单，复用既有线索、沟通、测评和课程关系。线索关联学生后统一以学生身份展示，姓名和手机号搜索覆盖当前负责范围内的所有阶段。

## 五个主阶段

| 标签 | 归类依据 | 常见当前情况 |
| --- | --- | --- |
| 待首联 | 尚无有效首联，也未发生更后续的正式事实 | 尚未联系、未接通、待分配、号码无效 |
| 待测评 | 已有效首联，尚无完成的专业测评结果 | 尚未预约、协调中、已约待测、未到待重约 |
| 待报名 | 已完成测评，尚无正式报名关系 | 测后未报、待回复、考虑中、有意报名、缴费待核实 |
| 待续班 | 至少一项有效的报名或在班关系仍在继续 | 等班、在读、续班考虑中、本轮不续 |
| 历史学员 | 曾有报名关系，当前所有有效课程关系均已结束 | 已退课、课程已结束 |

主阶段互斥，正式课程关系优先于测评和首联事实。单科退课后仍有其他有效课程的学生继续留在待续班；表达不续班但本期仍在读的学生也继续留在待续班。只有退款备注不构成退课事实。已确认但缺少结构化课程结束事实的来源记录继续保留服务状态，待实际业务关系补齐后归类。

测评预约、未到、取消、空测评记录和报名意向均不代替相应完成事实。未到后的重约创建新尝试并保留原记录。当前情况在数据库分页前筛选；批量归类承担计数和分页，当前页再读取个人摘要。

## 行内登记

- 点击行展开登记，姓名打开既有学生全景。可以单独记情况，或选择首联、测评／活动邀约、报名／续班／重新报名。
- 使用既有登记字段和邀约日历。报名意向保存到课程机会，具有报名权限的员工核实缴费并填写依据后，可调用既有正式报名流程；班级安排继续在分班办理。
- 备注、所选业务安排和回执在同一事务保存。请求 ID 处理重试幂等；邀约保存检查原安排的 ID 与更新时间。
- 保存失败时保留会话草稿。保存成功后保留当前行的位置，更新当前情况并标记“本轮已处理”；支持保存并下一位。刷新名单或切换范围后按正式归类重新加载。
- 回收站沿用原入口。学服过程页和原有学生全景继续承担各自业务记录。

## 本次验证

本地新增迁移为 `20260908000100_student_stage_workspace` 和 `20260908000110_student_stage_workspace_index`。执行入口 `scripts/student-stage-workspace.mjs` 每次核对本机、实际 Supabase origin、监听进程、Docker 网络和数据库指纹，应用前核对已通过检查的迁移校验和与 ledger。业务夹具始终在保存点回滚；应用迁移前后核对既有业务表摘要。

已通过的窄检查覆盖首联流转、身份去重、跨阶段搜索、独立备注、重试幂等、未到重约保留原尝试、预约冲突回滚、测评完成、意向与正式报名区分、多课程退课、不续班仍在读、全部退课、匿名／学生／其他员工范围拒绝和回执表权限。

`tests/student-stage-workspace.test.ts` 覆盖 URL 过滤、身份转换后行位置、双语枚举、备注提交、参数与身份权限、重试冲突、服务端分页与响应合同；新增 7 项通过。共享分页测试通过，本次文件 ESLint 通过。全库 TypeScript 检查仍存在首页和既有测试文件的其他错误，本次文件未报错；既有 `school-support-quick-entry.test.ts` 的一条断言仍要求测评详情直接包含旧组件，与现行测评实现不符。未改写这些其他任务的文件。

固定主管账号的五个阶段 API 和 zh/en 页面启动、固定教师页面启动、固定学生角色拒绝均通过。真实名单查询本地耗时约 0.12～0.26 秒。检查只输出汇总和状态，身份凭据、业务明细、cookie 与私有日志保留在本机。首屏按需加载登记面板；构建体积报表因当前没有生产构建产物未形成可比较结果，留在批次构建时核对。

人工验收重点：五个阶段的业务归类、行内日历与报名表单、保存后行位置、保存并下一位、失败后的草稿恢复、窄屏信息密度。机器检查与开发交付均不表示用户已验收或生产已部署。
