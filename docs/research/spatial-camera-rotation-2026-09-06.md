# 空间工作台旋转方式调研与本轮决策

日期：2026-09-06。范围：独立空间工具的观察相机、视角过渡与可选吸附；不改变空间课件阶段、课堂协议或生产状态。

2026-09-07 最终范围：用户重新测试后认为原版操作足够，要求恢复此前的手动旋转方式，本轮只做视角切换平滑修订。已恢复原有 drei OrbitControls 及手势参数，撤回自由 Arcball 和顶部限制实验；共享相机过渡保留。状态：开发端已交付，待人工视觉验收。

## 1. 行业方法及适用边界

| 方法 | 优点 | 主要取舍 | 本轮判断 |
| --- | --- | --- | --- |
| 固定上方向的 Turntable / Orbit | 保持地平线，容易辨认上下 | 接近正上、正下时，绕世界上轴的动作容易表现为画面打转；需要极点约束或特定策略 | 按用户复测后的决定恢复既有控件，本轮不修改极点规则 |
| 增量自由 Trackball | 屏幕相对拖动，可以越过顶部和底部 | 旋转次序会影响结果，绕一圈回到鼠标起点未必恢复姿态；实现还可能受事件采样和更新频率影响 | 不直接把替换控件当成解决方案 |
| 起点锚定的 Arcball | 一次手势从按下姿态计算，回到指针起点可恢复原姿态；可自由翻转 | 球面映射的半径、边缘区域与侧倾行为仍需调校；不等于自动好用 | 已撤回，不纳入本轮交付 |
| 固定轴旋转环 / 精确角度 | 可预测地完成指定轴的精确调整 | 增加操作步骤，轴环重叠或接近侧视时难选 | 保留为未来有明确精调需求时的辅助方式 |

Blender 将 Turntable、Trackball、旋转中心与 Smooth View 分别设置；保持方位感和自由观察是不同取舍，视角动画并不要求手动拖动具有惯性。[Blender 导航手册](https://docs.blender.org/manual/en/latest/editors/preferences/navigation.html)

Fusion 同时提供自由轨道和约束轨道，说明两者服务于不同的观察需求，并非某个控件可以通吃所有场景。[Fusion 导航栏](https://help.autodesk.com/cloudhelp/FRA/Fusion-GetStarted/files/GS-NAVIGATION-BAR.htm)

Three.js OrbitControls 保持相机上方向，并支持限制水平及垂直角度。锁住极点附近水平角度可以避免画面打转，但会让横拖失去响应；这是对实现的推断和已撤回候选的验证结果，不作为当前操作规则。[Three.js OrbitControls](https://threejs.org/docs/pages/OrbitControls.html)

Shoemake 的原始 Arcball 论文重点处理旋转路径相关性；Three.js 的 Arcball 文档也明确说明同一拖动回到起点时姿态应恢复。算法名称容易混用，实际选型需要检查起点基准、映射区域与松手行为。[原始 Arcball 论文](https://tommyhinks.com/wp-content/uploads/2012/02/shoemake92_arcball.pdf)、[Three.js Arcball](https://threejs.org/docs/pages/ArcballControls.html)

2016 年的对比研究招募了 46 人（最终分析 45 人），没有发现所测 Arcball 与双轴控制之间的显著差异；精确轴控制在简单定向任务中更准，但较慢。该研究使用的控件还有具体实现变体，受试者也不是小学生，因此不能据此宣称 Arcball 对本项目一定最顺手。[Graphics Interface 2016 研究](https://graphicsinterface.org/wp-content/uploads/gi2016-12.pdf)

另一项 32 人研究发现：固定轴与对象本身暗示的旋转轴一致时，任务表现、可用性和负担更好。对本项目的推断是：展开图的根面、观察目标和旋转中心要稳定，不能只调旋转速度。[固定轴与用户预期的研究](https://link.springer.com/article/10.1007/s00371-021-02394-x)

## 2. 本轮交互约定

- 手动操作：恢复原有 OrbitControls，保留默认旋转速度、鼠标/触摸映射及无阻尼设置；恢复展开图既有缩放距离边界。本轮不继续改造顶部拖动或引入自由侧倾。
- 视角按钮：共用 720ms 平滑过渡，从当前实际姿态接续；从实际首帧开始计时。再次点击当前书签也能恢复其方向，用户开始拖动时接管动画。
- 辅助吸附：默认关闭，各工作台共享当前页面内的偏好；仅在旋转松手且距正交姿态不超过 10° 时，以 240ms 过渡对齐。
- 吸附恢复原轨道操作的六个标准观察方向，不保留 Arcball 的 24 姿态扩展。
- 旋转中心：手动旋转期间稳定；平移后继续绕实际中心观察；双击不自动重定位。
- 投影：保持原有体素正交投影合同；手动旋转不自动改为透视。显式书签的投影参数由共享相机层处理。
- 减少动态效果偏好：教学视角切换使用 240ms 简短过渡，保留空间对应关系；辅助吸附仍可立即对齐。
- 编辑预览：异步重建时保留上一对有效页面和运行状态，以保持 Canvas 连续；本地观察不写入编排、课堂状态或数据库。

## 3. 本轮落实的实现风险

原体素与展开图维护了两套相机组件；编辑预览还会在切步重建期间卸载 Canvas。现已共用 `SpatialCameraRig`，并保留连续的预览实例。

正立书签之间沿方位角和仰角过渡，使侧视转俯视的中间帧也保持竖直方向；已有侧倾姿态使用四元数连续接续，避免接管时跳变。这只约束按钮过渡，手动操作沿用原版。

真实 React/R3F 相机组件测试复现了一条瞬跳路径：点击后首帧延迟超过 720ms 时，原实现会直接应用终点。计时起点移到首帧后，真实 demand 帧调度能够持续渲染中间姿态。该测试证明这条代码路径，尚未在用户浏览器中确认它是唯一原因。

已移除 `spatial-arcball-controls.ts` 及自由旋转专用测试。原控件通过公开 target 接入共享相机；新相机挂载时恢复当前旋转中心，动画帧由共享层最终应用，避免默认原点和控件更新覆盖中间姿态。

## 4. 验收边界

机器检查：五个相关测试文件的 37 项测试通过，本次文件 ESLint 通过。真实 React/R3F 逐帧和 demand 调度测试覆盖首帧延迟、连续及重复点击、手势接管、非原点模型及投影切换、短过渡与精确终点。全库类型检查仍有其他业务测试的 `NextIntlClientProvider` children 类型报错，本次空间文件无类型报错。

恢复验证直接对照同版本原 Orbit 控件：正交/透视、鼠标/单指触控、普通及顶部后的拖动、默认参数、缩放和平移；另覆盖平移后平滑恢复、共享吸附、编辑预览接入和课堂交互合同。该结果证明操作实现与旧控件一致，不替代人工体验。

本轮人工验收重点仅为：正、侧、俯视按钮是否看得见连续旋转；连续点击是否从当前画面接续。按用户最新决定，手动旋转改造不再列为本轮待办。

未执行生产部署、数据库写入、浏览器自动验收或阶段 Gate 收口。
