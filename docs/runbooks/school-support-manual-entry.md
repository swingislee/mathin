# 学服工作表补入与资料修订

> 2026-09-09：行旁补入与分班空位录入已随本批[生产发布](../evidence/r1/school-placement-production-20260909.md)上线，机器 postflight 通过，待生产实际交互验收。阶段 Gate 保持原状态。

产品负责人确认五张学服工作表、现场测评／体验和老生报名／续班都有手工补入需求，并授权开始施工和热修补。本轮按本地开发目标实施。

## 可验收范围

- 首联、沟通、测评、续报和学生表左侧提供悬浮“+”，点击后在选定记录及其详情下方插入空白行与填写详情；空表也提供入口。姓名、电话、年级、备注按当前表的列填写，家长姓名、家长电话、学校、微信、档案备注和安排字段在详情补齐。姓名、电话、家长和微信自动检索权限内档案，候选在详情中展示年级、家长、学校和负责人；选择后使用该档案，同一电话的不同孩子分别呈现。
- 选中已有档案后读取完整基本资料，姓名、年级、联系电话、家长、学校、微信与备注可以直接更正，随本次办理在同一事务保存。新资料至少填写姓名或电话；缺项保持为空。确认是新学生时可以建档，也可以保存为待确认登记。
- 同号候选可选择“关联家庭”，核对两名学生和目标家庭后明确勾选确认。支持两名已有学生，以及新学生与已有学生的关联；保留各自身份与业务记录。已有单一家庭时加入该家庭，已有不同家庭时提示核对原关系。
- 沟通补入接到持久工作单；现场测评可选择现有场次，或直接安排一对一测评。只有明确勾选实际到场才登记到场。
- 分班表的可用空位提供“+”，面板带入班级、课程、学期和座位。确认已有学生或新学生后沿用班级加人流程，保存花名册与指定座位；该动作不产生付款或正式课程报名确认。重复请求返回原结果，座位被占用、身份未确认及越权时整笔回滚。
- 其他报名和续报补入接到课程机会；课程或身份未齐的事项保留在原工作表的待办行。补齐后使用既有登记表办理，确认报名后由正式记录继续流转，手工待办自动移出。
- 点击学生姓名打开共用档案侧栏，通过“资料与修订”编辑姓名、年级、电话、家长、学校、微信和备注。保存使用资料版本校验，记录修改前后值、操作者和时间。并发冲突后保留草稿，重新读取后显示最新资料与草稿差异。
- 待确认登记可关联到已有学生，或明确确认为新学生。原参与记录、课程机会和办理事项沿用既有身份重挂逻辑；两边已有同轮待办时返回关联冲突，保留原记录供核对。
- 当前业务记录和导入历史的更正继续使用工作表原有的业务修订入口。已关联正式身份的调整继续沿用既有身份变更流程。

补入只保存资料与当前安排。首联、邀约结果、付款和正式报名由各自实际登记产生。原始导入凭据保持不变。

## 数据与权限

迁移：`20260908160000_school_support_manual_entry.sql`；行内与空位增量：`20260909000100_school_support_inline_intake.sql`。

详情字段增量为 `20260909000300_school_support_profile_intake.sql`。手工待确认身份通过 `leads.manual_profile` 保留家长、学校和微信，档案备注沿用线索备注；确认建档时完整带入。已有档案的资料更正可在补入行直接办理，也可使用“资料与修订”。

`leads` 新增手工登记与身份待确认标记；只为手工登记扩展缺项合同。新增 `school_support_work_items` 保存尚待办理的事项，`school_support_entry_receipts` 防止同一请求重复写入，`school_support_change_log` 保存修订轨迹。

写入函数逐一检查功能权限、主体范围、资料版本和业务状态。客户端不能直接写新表。普通首联的成功沟通后建档行为保持原合同；未确认的手工身份先保留待核对状态。

数据库类型由本地 pg-meta 重新生成。生成结果同时补齐了此前已存在但类型文件尚未收录的业务视图和函数；其实现没有在本轮修改。

## 2026-09-09 增量验证

行下空白行、自动匹配、候选选择、失败草稿与幂等重试、空位上下文及原工作表交互的定向测试通过；TypeScript 和受影响文件 ESLint 通过。本机空位合同覆盖实际座位持久化、重复提交、抢占冲突、未确认身份、学生角色隔离和失败零残留。校验与本机应用均验证原业务内容不变。

本增量使用 `scripts/school-support-inline-intake.mjs --preflight / --check / --apply`；`--check` 整体回滚迁移和演练数据，`--apply` 只提交已检查的函数更新。

详情字段增量使用 `scripts/school-support-profile-intake.mjs --preflight / --check / --apply`，覆盖即时建档、待确认资料保存与修订、稍后建档传递、家长电话／微信匹配、可能重复、幂等重试和失败回滚；原业务内容与原函数权限保持不变。迁移规范化摘要为 `007b7591ff8cdfb6959c2007eb356b9b88e9c41741206c41f12d70a28ed07420`。此增量与档案合并的验收说明分别保留于本文件及 [学生档案合并](student-profile-merge.md)。

行旁入口按后续反馈改为共用一个悬停“+”，定位于表格外侧已有的页面留白，表格滚动容器采用原列宽。鼠标离开后隐藏；分班空位的“+”同样在悬停时显示。键盘聚焦保留可见入口，行旁入口支持上下键选择可见插入点。此调整仅涉及界面，待人工视觉验收。

后续修正：行旁入口离开时立即隐藏，消除坐标重置造成的顶部闪现。空座位号恢复居中，悬停加号位于数字旁；分班表增加窄难度列，读取当前班级课程的难度字段。电话不足 9 位时暂停自动匹配并提示补全，第 9 位起动态检索。此批定向交互、工作表、类型与静态检查通过，开发端待人工验收。

## 基础资料修订与家庭关联增量

`20260909000400_school_support_profile_family.sql` 将可选资料修订与家庭关联纳入补入事务。资料更正保持所选学生 ID，完整读取后编辑，按原版本校验并记录前后值。并发修改后保留草稿，可读取最新档案、核对差异后继续保存。办理过程只复用当前操作者可办理的待办，其他负责人的事项保持原归属。

基础资料更正与家庭关联使用既有学服办理权限（`followup.view`、`followup.write`）和 `school_support_subject_access(..., true)`，分别核对两名学生。教师可办理范围内的学生无需完整学生管理权限即可更正基础资料。身份确认继续使用原 `student.edit` 权限，并通过独立的 `canResolveIdentity` 控制入口；档案合并的权限合同保持原实现。

家庭关联检查双方当前电话或家长电话的规范化结果、对方档案版本、家庭成员关系版本和明确确认。操作复用 `families`／`family_students`，记录 `school_support.family_linked` 审计事件；家长登录绑定仍由原身份流程办理。已有多个不同家庭时整笔返回核对提示，保留原家庭结构。重复请求复用原办理结果；任一资料、家庭或后续办理失败时整体回滚。

本机配方为 `scripts/school-support-profile-family.mjs --preflight / --check / --apply`。规范化迁移摘要：`950b764a904f35d0475f9757f42fde51386e3cebabb0786264e8c59026f219af`。定向事务断言覆盖教师范围内更正与缺失年级补齐、同号多子女、已有／新学生关联、旧家庭复用、不同家庭冲突、版本过期、未确认关联、请求重试、失败整笔回滚、其他负责人及学生角色隔离、家长绑定保持不变。只应用于已核对的本机目标，演练数据全部回滚；开发端待人工验收。

该增量的表单交互、Action 输入与权限映射、TypeScript 和受影响文件 ESLint 通过。固定开发账号的 zh/en 页面启动、家庭预览 RPC、基础资料／身份确认权限字段及学生角色隔离检查通过；机器结果不替代人工交互与视觉验收。

## 本地验证与应用

执行前由共享目标检查器核对执行主机、Supabase origin、开发端口监听进程、Docker 上下文、网关绑定和数据库指纹。

```powershell
node scripts/school-support-manual-entry.mjs --preflight
node --experimental-strip-types scripts/school-support-manual-entry.mjs --check
node --experimental-strip-types scripts/school-support-manual-entry.mjs --apply
$env:SUPABASE_META_DOCKER = 'supabase-meta'
node scripts/generate-database-types.mjs
node --experimental-strip-types scripts/verify-school-support-manual-entry.mjs <local-dev-url>
```

`--check` 在事务内执行迁移与合同断言，随后整体回滚；验证现有业务表内容及原函数权限未变。`--apply` 要求匹配的迁移、断言校验和与账本位置，再在事务内应用。测试数据始终回滚。

本轮机器检查结果：

- 数据库合同通过：最小登记、同电话多子女、重复提交、请求内容冲突、资料版本冲突、身份关联及冲突回滚、到场登记、课程机会接续、续报正式登记、待办移出、普通首联基线、负责人范围及非员工／匿名隔离。
- 定向单元与交互合同通过：补入失败保留草稿和请求编号、选择主体与权限、手工续报使用目标课程／学期，以及受影响工作表原有交互。
- 本次文件 lint 与 TypeScript 检查通过。
- 固定开发账号验证通过：管理员六个页面的 zh/en 启动、老师沟通及学生页启动、工作事项读取、学生角色检索隔离。页面检查只验证启动与入口渲染。

本地受控日志保留于 `.tmp/school-support-manual-entry/`，账号与会话信息不进入日志或提交。

## 人工验收重点

1. 在每张工作表搜索已存在的孩子，核对同电话的多个候选并补入。
2. 用缺电话的姓名登记临时到场，或先保存缺课期的老生续报；刷新后从待办继续，核对保存后的定位与展开体验。
3. 从档案侧栏更正资料，检查列表、侧栏和修订记录；并发编辑时核对冲突提示与草稿保留。
4. 确认身份后继续报名，检查原参与和办理记录，以及正式登记后的名单归属。

若需暂停该入口，先回退应用入口变更并保留新增资料与审计表，再按具体问题修复。已发生业务写入后，保留身份、报名和历史凭据，按对应领域流程处理更正。
