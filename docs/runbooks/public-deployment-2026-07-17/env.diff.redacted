# PENDING — the pre-change .env is backed up with owner-only permissions on xiaomi.
# Planned non-secret changes: SUPABASE_PUBLIC_URL, API_EXTERNAL_URL, SITE_URL,
# ADDITIONAL_REDIRECT_URLS, PROXY_DOMAIN, and possibly KONG host bindings. Never
# record JWT secrets, service-role keys, database passwords, or DNS API tokens.
