# 学生再联系读取性能 · 2026-09-14

状态：**2026-09-15 已获单独发布授权并部署生产，机器 postflight 通过，待用户体验验收**。用户要求同时处理学生全部子路由；[前两条读取修复](school-route-performance-20260914.md)已部署生产。本页跟进单独发现的再联系分组瓶颈，不更改 R1-Live-2 或 Gate 状态。新增[Dashboard 逐路由核查](dashboard-route-audit-20260915.md)逐项记录页面、查询状态及剩余问题。

## 问题与变更

生产管理员再联系“未接通／已测评／往期／沉默”四组完整 RPC 读取分别约 11.47 / 12.39 / 10.86 / 15.78 秒。开发端一次沉默分组读取调用 `has_perm` 15,580 次、`is_staff` 20,215 次；`student_record_index_with_enrollments`、`student_record_list_rows` 和 `student_recontact_candidates` 在两环境的函数体摘要一致。

新增 `20260914003000_student_recontact_read_path`：

- 两个旧摘要读取函数复用当前请求已经计算的员工与岗位权限，已具备全量可见权限、本人负责或未分配的线索直接通过原有可见条件；其他线索继续使用原协作范围函数。
- 最新联系记录的关联拆为线索和学生两条等价查询，继续使用原先的 `DISTINCT ON`、发生时间、记录时间与 ID 排序。同一记录命中两个分支时事实相同。
- 四个入口按本次实际参数生成执行计划，沿用已有学生列表的 `force_custom_plan` 方式。

原有联系人去重、无效号码、近期联系、拒绝再联系、提醒、邀约、工作单、历史接续、业务分类和操作权限字段均保持。迁移严格核对三个读取函数及两条底层权限函数的已知正文；未知版本即停止，重复应用保持同一实现。owner、ACL、SECURITY DEFINER、固定 search_path 和业务表不变。

迁移 LF SHA-256：`ce54d5fa60d87055636ce2d6ca09026d856e14fa57280a9af8e199ca83660155`。

## 验证与边界

验证脚本 `scripts/student-recontact-read-path.mjs` 仅接受已核对的 Windows 本机 loopback 开发库。覆盖固定管理员／教师、四个再联系原因、全部／我的／同组／未分配范围、家长与学生拒绝；比较完整返回 DTO、数量和分组计数。停用、待改密与匿名身份继续拒绝。事务后独立核对业务、函数及权限目录和账本零残留，再允许本地 apply。

第一次本地检查的待改密状态模拟被档案保护触发器拒绝，事务已回滚；测试随后复用仓库现有配方，在运维上下文切换固定账号状态，再以该账号读取，并由子事务恢复，不改保护触发器。性能候选迭代完成后重新运行受影响对照，不复用旧代码的通过结论。

一次独立候选试验中，开发沉默分组完整结果约 6.01 秒降到 1.53 秒，DTO 摘要一致。这不是生产完整页面耗时；最终验证、开发应用和生产结果分别登记。

最终候选完成 16 项返回结果对照，合法范围的完整 DTO 及拒绝结果均相等；停用、待改密及匿名拒绝通过，函数属性、owner/ACL、其他函数、策略、索引、业务与账本回滚零残留。随后本地 apply 仅登记该迁移，业务摘要保持。2026-09-15 生产演练进一步明确：两项同组范围在旧接口中均为 `VALIDATION`，家长／学生为拒绝结果；这些负向结果不计作同组功能可用。

| 开发管理员完整数据库读取 | 修复前 | 修复后 |
| --- | --- | --- |
| 未接通 | 3,305 ms | 551 ms |
| 已测评 | 3,947 ms | 2,457 ms |
| 往期 | 3,353 ms | 647 ms |
| 沉默 | 5,807 ms | 1,501 ms |

窄权限教师继续执行逐条协作检查，部分范围仍约 5 秒；这次针对用户报告的管理员页面，不把管理员收益推广为所有角色同等提速。

## 开发页面与发布候选

第三条应用到开发库后，固定管理员完整 HTTP 响应：未接通约 1.14 秒、往期约 1.38–1.91 秒、沉默约 2.43–2.54 秒。已测评一次约 1.91 秒，另一次出现 16.67 秒（首字节 14.90 秒）的异常等待；随后两次定向复测为 1.93 / 1.92 秒、均返回 200。异常没有在定向复测重现，原因尚未确认，不能据此承诺所有请求耗时上限。开发完整响应与生产 RPC 耗时分别报告，不互相替代。

独立生产候选为 `1ad14a81f7da2f5906df5ec65ba9899f5546c047`，分支 `codex/student-recontact-isolated-20260914`，父提交 `93e049ec10844e81f7b2224e66f60babd791fb77`。本次最初提交 `11a54b60` 发生共享暂存区并发碰撞，包含另一任务已暂存的立方体文件；保留该提交及现有工作树，用独立索引从父提交生成只含本补丁五个文件的候选，已核对变更清单。生产发布绑定独立候选和上述迁移摘要。

发布前第三条只读 preflight 通过：生产主机 Xiaomi、运行中应用 Supabase origin 与配置均为 `https://supabase.mathin.club`、监听 `127.0.0.1:3131`，应用仍为 `3d9892bb` / PID `3825682`；账本 `384 / 20260914002000_workflow_subject_read_path`，四个目标函数均符合已知版本。只读核对输出运维事实、聚合摘要与布尔结果。

发布执行脚本在本机受忽略目录准备，包含新候选绑定的备份、同事务完整 DTO 对照、回滚零残留、正式应用及独立 postflight。用户于 2026-09-15 明确要求“单独发布”，随后重新核对实际目标和账本，并完成下述执行。

## 2026-09-15 单独生产发布

发布只执行独立候选 `1ad14a81f7da2f5906df5ec65ba9899f5546c047` 的一条迁移，应用继续使用 `3d9892bb`，无需重建、重启或切换应用。

候选绑定备份为 `mathin-db-prechange-20260914T162158Z-student-recontact-1ad14a81f7da`（名称使用 UTC），350,655,461 bytes，TOC 6,503 项，SHA-256 `9e83ab1df91358d43be5b0b63207597d7af4d732aa692bd9d0f982d260c1923b`。候选、迁移、备份和恢复目录摘要均核对通过。

首次演练在迁移前读取 `scope=group` 时遇到既有 `VALIDATION`，事务中止；独立连接确认函数目录、业务摘要和账本零残留。原因是旧协作迁移的枚举字符串顺序与该 RPC 不同，未加入 group 校验值。迁移内容和摘要保持，演练将该既有失败明确作为负向合同；备份仍绑定同一候选，没有把它记为同组可用。

随后 SERIALIZABLE 回滚演练和正式事务完成四类既有身份的 10 项对照：7 项合法完整返回、同组既有参数拒绝、家长及学生拒绝；匿名执行继续拒绝。所有结果相等，三个函数正文和四个计划配置之外的函数属性、owner/ACL、固定 search_path、策略、索引和 43 项业务事实摘要保持。

| 生产管理员完整 RPC | 正式事务修复前 | 修复后 |
| --- | ---: | ---: |
| 未接通（全部） | 11,431 ms | 1,458 ms |
| 已测评（全部） | 12,500 ms | 2,768 ms |
| 往期（全部） | 11,362 ms | 1,177 ms |
| 沉默（全部） | 15,932 ms | 4,800 ms |
| 未接通（我的） | 11,141 ms | 3,678 ms |
| 未接通（未分配） | 11,124 ms | 1,415 ms |

既有 staff 沉默查询约 16,175 → 4,882 ms；这是本次选定身份的结果，不推广为所有岗位和数据范围。沉默及“我的”范围仍有数秒耗时，尚未达到所有页面快速完成的状态。

正式账本为 **385 / `20260914003000_student_recontact_read_path`**，账本登记时间 `2026-09-14T16:33:37.47918+00:00`（北京时间 9 月 15 日）。独立数据库 postflight、43 项 zh/en 健康及匿名入口检查通过，业务摘要保持、错误增量为 0，应用 PID 保持 3825682。

受控发布证据 `/home/swing/services/mathin/staging/student-recontact-1ad14a81f7da-20260914/evidence.tar`，225,280 bytes，SHA-256 `2924728446ed08ca770aa8dfcac39b4c0b5a2d79536deb594778ed8c1255a51c`。包含首次演练中止及独立零残留记录。owner-only，生产运维维护者可访问，保留到 `v1.0.0 + 365 天`。后续 168 项完整路由匿名检查及开发逐项结果见[路由核查记录](dashboard-route-audit-20260915.md)。
