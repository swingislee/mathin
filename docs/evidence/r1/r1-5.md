# R1-5 · 学生与家庭门户（阶段中证据）

## 状态

R1-5 仍在施工，当前证据覆盖学生班级入口、课前工作区、正式课堂响应式布局、课件逐生检查边界、学生媒体预载、三类课后发布底座，以及家庭门户按稳定学生 ID 切换、成果草稿隔离与特殊状态、监护关系自助撤回和请假→审批→补课状态链。多子女与未绑定家庭的 zh/en 浏览器旅程已关闭；完整请假/补课浏览器旅程、跨家庭负向 E2E 和正式浏览器自动化尚未关闭，因此本文件不得作为阶段通过或进入 R1-6 的依据。

## 应用、课堂与浏览器证据

| 字段 | 值 |
| --- | --- |
| `gate_id` | `UX-R1-5-INCREMENT-20260730` |
| `domain` | 学生班级入口 / 家庭多子女、成果可见性与请假补课 / 备课工作区 / 正式课堂 / 逐生检查 / 媒体同步 / 课后独立任务 |
| `result` | partial |
| `measured_value` | zh/en messages 3357×2；家庭课表/考勤投影携带 `student_id`，同名子女不再按姓名合并；家庭课评只读取已发布家庭摘要，保存摘要或修改逐生课评会自动撤回可见性；家庭成果状态投影只返回课次元数据与 `pending/published/withdrawn`，不返回草稿分数、点评或摘要，未绑定/待审核/撤回/财务关闭四类状态均有 zh/en UI 合同；监护人可自助撤回自己的平台关系，立即失去孩子访问权并在需要时顺延主监护人；家庭共享请假记录，审批后显示补课待安排，补课安排进入家庭课表并自动关闭跟进任务、通知学生和有效监护人；开发库事务断言验证草稿 0 可见但状态为待审核、发布后可见且状态为已发布、修订后正文重新隐藏且状态为已撤回，拒绝外部作业/提交/家庭摘要/监护操作及跨家庭请假/关系撤回，关系撤回即时失权，请假→审批→补课安排→家庭课表/通知可见，课表/考勤/学习摘要/账户/作业/课评/成果状态/视频/请假投影均限定当前家庭；R1 定向回归 52/52；本轮 R1 课堂连续性定向回归 12/12；production build 308/308 静态页生成；1600×900 教师课堂课件 1020×765，右侧副板书 288px、名录 240px，双折叠后课件 1072×804 且两条窄栏位于屏幕右侧；1024×768 教师/学生课堂课件 896×672，副板书与名录位于可滚动后续区域；学生端无副板书/名录折叠、页面列表或课件操作按钮；学生视频完成预载后 `controls=false`、`pointer-events=none`；备课工作区移除重复决策栏和开始按钮，复制备课贴近左侧阶段导航，右侧按试讲→完成备课排列；三项生产材料自动保存→待审核→通知→审核决定合同已固化；共享页目录/预览组件与 H5 manifest 入口已接线，备课使用 17rem 宽目录，课前外层固定视口且只允许流程栏/页目录内部滚动；共享翻页按钮与键盘入口已合并，course 讲次预览弹窗外层滚动已移除；4:3 定格的底部书写区复用主题 `bg-card`，正式课件默认恢复保留一次撤销快照；冻结后课前页保留完整只读备课档案并从本课快照渲染；已标记页跨设备切换检查项，未标记页保留当前检查项；课后主操作与阶段导航同行，三类发布先于学生卡，作业和视频分别审阅；最终响应式浏览器复测 pending |
| `threshold` | 本增量的 typecheck、lint、messages、数据库类型、规划审计、定向合同和人工响应式走查通过；R1-5 最终门仍要求学生/家长 E2E、跨家庭与跨学生负向查询 100% 拒绝、所有状态 zh/en UI |
| `commit_sha` | `6a9c793c09ca83235faabfa5fb042daa0841f294`（当前未提交工作树的基线；最终 SHA 待提交） |
| `migration_head` | `20260730010500_doc26_teacher_workflow.sql` |
| `environment` | Windows 本地 Next.js 16.2.11 开发服务；小米自托管 Supabase 开发库；Codex in-app Browser |
| `dataset_manifest` | 复用 `.claude/test-accounts.local.md` 固定 teacher/student/parent 账号、测试班-P4、既有模拟课与既有逐生检查课次；新增开发专用 `test-parent-unbound` 家长账号且保持 0 条监护关系，把现有 `test-parent` 绑定到现有第二学生形成稳定双子女夹具，未新建学生；家庭门户断言只在事务内改写后回滚；家庭成果浏览器复测临时写入带 `R1_BROWSER_EPHEMERAL_*` 标记的摘要/课评各 1 行，验收后删除并确认 0/0；浏览器保存验证将一条既有测试结果登记为 `independent` |
| `started_at` | pending（R1-5 阶段仍施工） |
| `finished_at` | pending（R1-5 阶段仍施工） |
| `actor` | Codex desktop agent（实现与验证） |
| `approver` | `swingislee`（以本次“继续”指令授权当前增量） |
| `command_or_runbook` | `R1_DEV_TEST_FIXTURES=1 pnpm r1:family-fixtures`（连续执行确认幂等）；`pnpm typecheck`; `pnpm lint`; `pnpm messages:check`; `pnpm db:types:check`; `pnpm plan:audit`; `pnpm build`; `pnpm r1:test`; 通过 SSH/psql 顺序执行 `run-r1-db-audit.mjs` 清单内六项断言（均事务回滚）；teacher/student 浏览器 1600×900、1024×768 人工走查；family 浏览器 zh/en 待审核、撤回、财务关闭、未绑定与多子女切换人工走查；开发库只读清理查询 |
| `artifact_url_or_path` | not_applicable（本轮为阶段中本地人工走查；正式截图/视频进入 R1-14 CI artifact） |
| `artifact_hash` | not_applicable |
| `retention` | 本摘要随 Git 历史永久保存；正式浏览器 artifact 待 R1-14 |
| `access_roles` | 仓库读权限持有者 |
| `failure_ticket` | pending：R1-5 仍需关闭完整请假/补课浏览器旅程以及跨家庭/跨学生正式 E2E；本轮数据库负向断言和人工 UI 走查不能替代最终 Playwright 证据 |

## 合并数据库审计增量

R1-5 新增 `assignment-submissions` 后，旧 R1-2 断言把“基础 6 项文件策略必须存在”误写为“文件策略总数必须恰好为 6”；开发队列存在到期业务任务时，旧租约断言也误要求第二 worker 不得领取任何任务。当前断言逐项核对基础策略，使用最高优先级测试任务并只拒绝重复领取同一任务；定向合同 5/5、最新 R1 合同 52/52 与 R1-1～R1-5/doc 26 六项数据库断言均通过，所有数据库断言最终 `ROLLBACK`。


## 课后统一工作区增量

- “还有 N 项待完成”压缩为单行高对比状态条，只展示必需任务计数和完成进度；补登记出勤、完成或重新打开本次课移动到“课前 / 课堂 / 课后”阶段导航同行区域。
- 页面按“知识总结 / 作业 / 视频三类独立发布 → 学生课堂记录 → 作业审阅 → 视频审阅”排序。旧通用任务列表移除，任务完成或跳过动作落到对应业务面板。
- 学生记录按响应式网格呈现，每张卡同时包含出勤、星数、逐题学情、课评输入和跟进输入；保存课评只完成 reviews，不再误把独立知识总结标为完成。
- 作业审阅复用现有提交列表、照片/PDF 签名访问、评分与反馈能力；多份本课作业使用页签切换。视频仍在所属课次面板审阅，不恢复无入口的全校队列。
- 本轮最新验证为 zh/en messages 3357×2、R1 定向回归 52/52、production build 308/308。1280×720 深色模式浏览器走查确认发布区、学生卡和两个审阅面板的层级；1024×768 下 scrollWidth=clientWidth=1024，阶段同行的两个主操作保持在视口内，学生卡宽 328px 并进入双列网格。最终刷新后无新增应用控制台错误。

## 已固化的逐生检查边界

正式课件只保存稳定 `page_doc_id + track` 上的“需逐生检查”布尔标记，发布时固化到 release。备课课次默认继承当前 release，允许本课删除默认页或临时加入其他课件页；额外项目通过新增空白课件页实现，不另建脱离页面的自定义项。每次点击自动写入 `session_learning_checks`，`class_sessions.learning_checks_configured_at` 使明确保存的空清单刷新后仍保持为空；所有课次调整不反向修改正式课件。

## 本轮媒体问题结论

学生从中间页加入时，页面文档会先于绑定资源 URL 到达；旧实现又按整套课件顺序串行预载，导致教师端已缓存可播放、学生端长时间显示“资源不可用”。当前页对象现在排在队首并采用 4 路有界并发，其余对象后台预载；绑定未到时显示明确加载态，完成后视频无原生控制条且不接受学生触摸。

## 课前工作区与材料审核增量

- 进入课前阶段自动开始备课，不再提供“开始备课”按钮；复制备课贴近左侧阶段导航，右侧按“试讲→完成备课”排列。
- 左栏只保留解析记录、标准教案、磨课链接三步实际生产流程。有效上传/链接保存会为对应材料创建或递增审核 revision，状态进入 `pending`，并向有效课程审核人发送实时通知；未配置审核人时管理员审核队列兜底。
- 审核面板按课次和材料显示签名附件/链接，支持通过或退回修改。三项材料必须全部 `approved`，并满足课件 release 与逐生检查门，教师才能完成备课。
- 右侧工作区使用 17rem 页栏和 4:3 预览；正式文档页书本图标直接切换本课“需逐生检查”，选中后显示绿色确认图标，再次点击可取消。点击后串行自动保存并显示保存状态，写入仅影响本课，不反向修改正式课件。
- 完成备课、课件冻结、开课或下课后，课前页继续显示三项生产材料、审核状态、逐生检查标记、页面目录和课件定稿，所有改写入口转为只读。预览直接展开 `class_sessions.courseware` 冻结数组，未从当前 track head 重建；顶部正式修订入口进入 Courseware Studio，新 release 不改写当前或历史课次。1280×720 已结束课次浏览器复测显示 59 页目录和 376×282 的 4:3 预览，页面根节点 `scrollHeight=clientHeight=720`，只保留左侧流程内部滚动，控制台无本轮错误。
- `20260731000500_r1_preparation_archive_unlock.sql` 增加默认关闭的组织级临时解锁开关；开启时只放行任课教师对备课材料、Vector 板书/解析、结构化教案、审校人与本课逐题检查的补改，课件页结构、冻结快照和正式 release 继续不可变。数据库写入与页面只读状态共用同一开关，关闭后立即恢复拒绝。
- 正式课堂登记面板按共享事件流中的稳定 `pageDocId` 自动切换检查项，副设备已打开的面板随主设备翻页。已标记页切换到对应检查项；未标记课件页、媒体页和临时白板页保持当前检查项，不再回退第一题；教师仍可手动切换，翻页时只清除批量学生选择，避免误批。
- `20260730000700_r1_fix_learning_check_mark.sql` 消除了写入 RPC 中 `classroom_id` 的变量/列歧义。开发库以固定教师身份完成事务回滚验证，浏览器实际登记后显示“已检查 1/1”，数据库结果为 `independent`。
- H5 HTML 在包脚本前注入不透明源存储兼容层和嵌套媒体桥，并用运行时版本参数淘汰旧缓存；教师端内置视频控件初始化与学生端加载后补发播放状态共用该入口。
- 本轮学情跳转修改对应的 lint、messages、规划审计与 R1 定向回归 44/44 已通过；后续合并家庭/备课增量后，全量 typecheck、数据库类型检查与 production build 308/308 均通过，最终响应式浏览器复测仍保持 pending。

## 家庭门户发布与跨子女隔离增量

- `get_my_schedule` 与 `get_my_attendance` 返回稳定 `student_id`；`/dashboard/children` 和家长首页按 ID 过滤，不再以可重复的姓名拼接多子女数据。
- 家庭课评投影只联接 `published_at is not null` 的家庭摘要，删除了对内部 `class_sessions.knowledge_summary` 草稿的回退。
- 保存家庭摘要会清空发布时间；逐生课评 insert/update/delete 由触发器自动撤回当前家庭摘要。教师显式重新发布后家庭端才再次可见。
- `get_my_session_review_states` 只投影家庭可见的课次元数据和 `pending/published/withdrawn`；孩子详情与家长首页显示待审核或已撤回，不再把两者伪装成“暂无课评”，发布正文仍由独立 RPC 控制。
- `get_family_session_brief` 对家长要求 `grades` scope；关系或 scope 撤回后 RPC fail-closed。
- `revoke_my_guardian_relationship` 只删除调用者自己的关系；同一学生上的操作串行化，当前同意记录追加 `withdrawn`，主监护人离开时按绑定时间顺延下一位。
- 家庭页先区分学生本人档案与监护绑定；只有监护绑定显示解除入口，并用 `AlertDialog` 明确即时失权后果。
- `list_my_session_leave_requests` 按家庭关系共享申请，并返回补课待安排、已安排、已完成或已取消状态；家庭页展示目标课次、时间和班级。
- 最新补课安排会加入 `get_my_schedule`，即使目标课次属于另一个班级；安排成功同时完成 `makeup_followup`，并向学生与当前有效监护人写入通知。
- 旧请假通知触发器的 `classroom_id` 歧义会让提交事务失败；新触发器使用限定变量名，并保持提交→员工、决定→全家庭的角色定向。
- `r1_family_portal_assertions.sql` 在开发库事务内创建最小外部课室 fixture，验证草稿→发布→修订正文隔离及三态投影，外部作业正文/提交/家庭摘要/监护同意、列表、权限与邀请全部拒绝，跨家庭请假/关系撤回拒绝，关系撤回即时失权与主监护人唯一性，请假→审批→补课→任务关闭→家庭课表/通知可见，以及所有带 `student_id` 的家庭投影只包含当前家长可访问学生；事务最终 `ROLLBACK`。
- Codex in-app Browser 以固定测试教师的家庭环境验证中文/英文孩子页：财务关闭分别显示“财务未启用”与“Finance is not enabled”；临时成果在 `pending` 时显示“待审核”与“Pending review”，发布后撤回则显示“已撤回”与“Withdrawn”，两种不可见状态均未渲染带唯一标记的点评正文，应用控制台无错误。验收后临时摘要/课评删除计数为 0/0，账号使用环境恢复为“工作台”。
- 开发专用账号夹具显式要求 `R1_DEV_TEST_FIXTURES=1`，并验证 Supabase 域名解析后的全部地址属于私网/回环网段；重复执行返回 `created=false`，没有重复账号或关系。Codex in-app Browser 以 `test-parent` 验证 zh/en 孩子页同时列出两个稳定学生 ID，并切换到第二学生查看独立课表、星数、监护关系与空状态；以 `test-parent-unbound` 验证 zh/en 家长首页和孩子页只显示绑定引导且侧栏不出现孩子入口。两个账号验收后均退出，最终浏览器 error 日志为 0。

## 家庭请假、审批与跨班补课浏览器闭环

| 字段 | 值 |
| --- | --- |
| `gate_id` | `UX-R1-5-FAMILY-LEAVE-MAKEUP-20260731` |
| `domain` | 家庭请假 / 教师审批 / 跨班补课 / 双语家庭课表 |
| `result` | pass（R1-5 家庭子门；不代表 R1-5 总阶段关闭） |
| `measured_value` | 固定家长在 zh/en 均看到 Pending；固定教师在课前工作区看到同一学生与原因并批准；课表抽屉选择教师管理的另一班目标课次后，固定家长在 zh/en 均看到 Approved、Make-up session scheduled 与目标班级/时间，目标课次同时进入家庭未来课表；检查最近 100 条浏览器日志，error 0 |
| `threshold` | 家长提交、教师批准和跨班补课写入真实 Server Action；结果在 zh/en 家庭投影一致；外部通知通道保持 fail-closed；浏览器 error=0 |
| `commit_sha` | `9cfa15eaa74bb53116dcd187f0f736a1987ef876` |
| `migration_head` | `20260730010500_doc26_teacher_workflow.sql` |
| `environment` | Windows 本地 Next.js 16.2.11 开发服务；小米自托管 Supabase 开发库；Codex in-app Browser |
| `dataset_manifest` | 复用固定 parent/teacher/student；`scripts/ensure-r1-family-journey-fixture.mjs` 幂等创建或核对一个关联正式讲次 release 的未来的源课次和一个教师管理的跨班未来目标课次；email/sms/wechat/webhook 均为 disabled；验收产生 append-only 领域事件与站内通知，作为开发/RC 数据保留至 R1-15 统一清理 |
| `started_at` | 2026-07-31T00:40:00+08:00 |
| `finished_at` | 2026-07-31T00:49:00+08:00 |
| `actor` | Codex desktop agent |
| `approver` | `swingislee`（以“继续”授权当前增量） |
| `command_or_runbook` | `R1_DEV_TEST_FIXTURES=1 pnpm r1:family-journey-fixture`；家长 zh→en、教师 zh、家长 zh→en 的 in-app Browser 真实操作；再次执行夹具命令核对 `approved` 与 makeup `to_session`；跨家庭/跨学生负向查询沿用本文件已登记、同迁移头通过且最终 `ROLLBACK` 的 `r1_family_portal_assertions.sql` 证据 |
| `artifact_url_or_path` | not_applicable（本轮为本地人工走查；正式视频/截图由 R1-14 CI artifact 承载） |
| `artifact_hash` | not_applicable |
| `retention` | 本摘要随 Git 历史永久保存；开发验收数据到 R1-15 清理 |
| `access_roles` | 仓库读权限持有者 |
| `failure_ticket` | not_applicable（家庭子门通过；正式浏览器自动化仍归 R1-14） |


## doc 26 教师备课生产链增量

- 开发库已应用 `20260730010500_doc26_teacher_workflow.sql`：四个新对象均启用 RLS，authenticated 角色不能直接改表，写入经 teacher/session/page/version/frozen guard RPC。
- 课次左侧三步备课流程切换研读试做、教案设计和磨课定稿；右侧课件目录与预览始终常驻。只有教师首次进入左侧教案步骤时才创建 `mathin-teaching-plan-v1` 模板，避免浏览课件即产生空教案。
- 课件页复用现有白板 Canvas 保存 Vector Stroke；“生成解析”把当前笔迹快照写入统一 `solution_records` 并进入既有解析审核。原上传入口保留并投影为 upload source；迁移会补投影既有上传记录，只改磨课/教案材料不会误增解析 revision。
- 结构化教案复用 BlockNote、700ms 自动保存、乐观 revision 冲突检测、页面级教学备注与显式提交；审核页只读渲染 BlockNote/Vector，决定同步教案状态。
- 开发库事务断言覆盖模板固定、RLS/执行权限、教师写入、陈旧版本拒绝、学生越权拒绝、板书解析、上传 projection revision 隔离、教案提交/审核同步与回滚；定向 Vitest 覆盖模板、zod Vector 合同、迁移与 UI 接线。
- 2026-07-31 以固定教师账号在 1280×720 实测目标课次：左侧默认研读试做，可切换教案设计、磨课定稿并返回研读；教案步骤在左侧加载 BlockNote 与页面教学备注，右侧 73 页课件目录、可视化预览和板书始终可见，旧工作模式节点为 0。三种步骤下页面 `clientHeight/scrollHeight` 均为 720/720，浏览器 error 日志为 0；走查产生的草稿按 ID、session、revision、status、creator、created_at 精确删除，复核该课次教案剩余 0，走查前已存在的 `session_preparations` 与审核记录未触碰。
- 独立教案管理页面和 WebP/PDF 派生输出尚未实现，doc 26 保持 `partial`；R1-5 仍需其既有浏览器 E2E 才能关闭。

## 学生门户与真实认证隔离浏览器闭环

| 字段 | 值 |
| --- | --- |
| `gate_id` | `UX-R1-5-STUDENT-AUTH-BOUNDARY-20260731` |
| `domain` | 学生门户 / 家庭投影 / authenticated RLS / 双语 UI |
| `result` | pass（R1-5 学生与认证隔离子门；不代表 R1-5 总阶段关闭） |
| `measured_value` | 固定 student-1/student-2/parent/unbound-parent 分别通过 publishable key 登录；10 个家庭投影只返回允许的稳定 `student_id`，student-1/student-2 各 1 个本人档案、parent 2 个子女档案、unbound-parent 所有投影 0 行；外部作业/提交 4 个 RPC 与学生、监护、提交、课评 6 个直表查询共 10/10 拒绝。固定 student-1 在 zh/en 分别走查学生首页、测试班详情、作业列表和作业详情，28 个关键标题、区域、输入与操作合同均唯一命中；未提交作业或上传文件；干净复测页最近 100 条浏览器日志 error 0；R1 定向回归 54/54 |
| `threshold` | 学生和家长只能看到本人或有效监护关系覆盖的学生；未绑定家长投影 0 行；跨学生 RPC/直表查询 100% 拒绝；学生首页→班级→学习任务→作业详情在 zh/en 均可达且 UI 双语；浏览器 error=0 |
| `commit_sha` | `0d4c15d3084852699d16108e547936c8902ddae8` |
| `migration_head` | `20260730010500_doc26_teacher_workflow.sql` |
| `environment` | Windows 本地 Next.js 16.2.11 开发服务；小米自托管 Supabase 开发库；Codex in-app Browser |
| `dataset_manifest` | 复用 `.claude/test-accounts.local.md` 固定 student/student-2/parent 与开发专用 unbound-parent；student-1=`3172e2bd-c18b-40b9-8139-03923ccda653`，测试班-P4=`92028e9e-e349-4c04-88ff-79a23702185d`；认证边界脚本只读，未 insert/update/delete；凭据、token 与可识别未成年人信息未写入仓库 |
| `started_at` | 2026-07-31T07:45:00+08:00 |
| `finished_at` | 2026-07-31T08:17:46+08:00 |
| `actor` | Codex desktop agent |
| `approver` | `swingislee`（以“继续施工”授权当前增量） |
| `command_or_runbook` | `$env:R1_DEV_TEST_FIXTURES='1'; pnpm run r1:family-boundary-check`；`pnpm run r1:test`；固定 student-1 在 in-app Browser 顺序走查 `/zh|en/dashboard`、`/zh|en/classroom/92028e9e-e349-4c04-88ff-79a23702185d`、`/zh|en/dashboard/assignments` 与带本人 `student` 参数的作业详情；使用全新页复跑后读取最近 100 条 error 日志 |
| `artifact_url_or_path` | not_applicable（本轮为本地人工走查；正式视频/截图由 R1-14 CI artifact 承载） |
| `artifact_hash` | not_applicable |
| `retention` | 本摘要随 Git 历史永久保存；开发账号与业务数据到 R1-15 清理 |
| `access_roles` | 仓库读权限持有者 |
| `failure_ticket` | not_applicable（学生与认证隔离子门通过；正式浏览器自动化仍归 R1-14） |

## 学生自助学习记录与身份感知通知链接

| 字段 | 值 |
| --- | --- |
| `gate_id` | `UX-R1-5-STUDENT-LEARNING-NOTIFICATIONS-20260731` |
| `domain` | 学生考勤 / 成果状态 / 请假补课 / 身份感知通知链接 |
| `result` | pass（R1-5 学生自助与通知子门；不代表 R1-5 总阶段关闭） |
| `measured_value` | 固定 student-1 在 zh/en 学习任务页各唯一显示考勤、近期课评和课程请假区域；中文 `#learning-results` 与英文 `#leave` 均滚入 720px 视口，`scrollWidth-clientWidth=0`。现有“补课已安排”通知点击后实际进入 `/zh/dashboard/assignments#leave`，未再跳向 family-only 孩子页；最近 100 条浏览器日志 error=0。authenticated 家庭边界再次通过，跨学生/未绑定家长 10/10 拒绝；R1 回归 55/55。 |
| `threshold` | 学生能查看本人考勤、发布状态成果、请假与补课状态并提交未来课次请假；学生学习通知不得进入 family-only 路由；家长通知继续带稳定 `student_id` 进入对应孩子；zh/en 区域唯一、无横向溢出、浏览器 error=0。 |
| `commit_sha` | `8b0e95dc697fa1edcb659101008106d4acae3098` |
| `migration_head` | 仓库新增 `20260731000300_r1_family_notification_links.sql`；本地浏览器验证使用应用层身份归一化，DDL/backfill 运行时验证留给正常迁移重放门禁。 |
| `environment` | Windows 本地 Next.js 16.2.11 开发服务；小米自托管 Supabase 开发库；Codex in-app Browser 1280×720。 |
| `dataset_manifest` | 复用固定 student-1 与既有 R1 家庭旅程课次/通知；点击一条既有补课通知使其进入已读，未提交新请假、作业或视频，未新建账号；凭据、token 与可识别未成年人信息未写入仓库。 |
| `started_at` | 2026-07-31T08:24:00+08:00 |
| `finished_at` | 2026-07-31T09:02:20+08:00 |
| `actor` | Codex desktop agent |
| `approver` | `swingislee`（以“继续施工”授权当前增量） |
| `command_or_runbook` | `pnpm run r1:test`；`pnpm lint`；`pnpm run typecheck`；`pnpm run messages:check`；`pnpm run db:types:check`；`pnpm plan:audit`；`$env:R1_DEV_TEST_FIXTURES='1'; pnpm run r1:family-boundary-check`；固定 student-1 在 in-app Browser 走查 `/zh|en/dashboard/assignments#learning-results|leave` 并点击既有补课通知。 |
| `artifact_url_or_path` | not_applicable（本轮为本地人工走查；正式视频/截图由 R1-14 CI artifact 承载） |
| `artifact_hash` | not_applicable |
| `retention` | 本摘要随 Git 历史永久保存；开发通知与业务数据到 R1-15 清理。 |
| `access_roles` | 仓库读权限持有者 |
| `failure_ticket` | pending：迁移 DDL/backfill 尚未在本轮开发库单独执行；由空库重放/迁移流水线验证，不影响已由应用层完成的学生通知直达子门。正式 Playwright 仍归 R1-14。 |

## 学生与家长 Dashboard 信息架构纠偏

| 字段 | 值 |
| --- | --- |
| `gate_id` | `UX-R1-5-CUSTOMER-DASHBOARD-IA-20260731` |
| `domain` | 学生课务 / 作业 / 学习记录 / 家长多子女概览 |
| `result` | pass（R1-5 客户门户信息架构子门；不代表 R1-5 总阶段关闭） |
| `measured_value` | 学生侧栏拆为课务、作业、学习记录与班级；作业页 DOM 中 `#attendance/#leave/#learning-results` 均为 0，只保留待交作业、视频任务与上传；课务页集中未来课次、近 60 天考勤和请假→补课状态；学习记录集中课堂星星、作业成绩和成果发布状态。英文侧栏使用 `Class schedule` 与 `Classes` 消除同名入口。家长首页只渲染 2 张稳定学生 ID 孩子卡与绑定卡，旧自定义布局缺任一孩子时回落为全孩子默认布局；游戏成绩、个人 Notebook 与家长自己的教室磁贴及查询均移除；最近 50 条浏览器 error 日志为 0。authenticated 边界复核 parent 投影 2 个孩子、student-1/student-2 各 1 个本人档案、unbound-parent 0 行，跨学生/未绑定家庭 10/10 拒绝；R1 定向回归 57/57。 |
| `threshold` | 学生按“接下来上什么课 / 要交什么 / 学得怎样”完成任务；考勤与请假不进入作业入口；家长首屏全部孩子强制可见并只呈现孩子级概览、风险与动作；zh/en 入口无同名歧义；浏览器 error=0；认证负向边界 100% 拒绝。 |
| `commit_sha` | `501684e`（学生端分区）、`6a721c2`（家长首页收敛）、`c854c8d`（英文标签消歧）、`8a72aca`（迁移序号唯一）、`a2454d9`（旧布局多子女强制可见） |
| `migration_head` | 仓库新增 `20260731000600_r1_student_information_architecture.sql`；开发浏览器由应用层身份归一化验证，DDL/backfill 留给正常迁移重放门禁。 |
| `environment` | Windows 本地 Next.js 16.2.11 开发服务；小米自托管 Supabase 开发库；Codex in-app Browser。 |
| `dataset_manifest` | 复用固定 student-1、parent 与现有两名学生档案；只读边界脚本确认 parent 的 2 个学生投影。未新建账号、未提交请假/作业/视频、未改业务数据；凭据、token 与可识别未成年人信息未写入仓库。 |
| `started_at` | 2026-07-31T09:05:00+08:00 |
| `finished_at` | 2026-07-31T09:38:48+08:00 |
| `actor` | Codex desktop agent |
| `approver` | `swingislee`（以本次继续施工指令授权当前增量） |
| `command_or_runbook` | `pnpm r1:test`（57/57）；`pnpm lint`；阶段提交前 `pnpm typecheck`、`pnpm messages:check`、`pnpm doc22:audit`、`pnpm plan:audit`；`$env:R1_DEV_TEST_FIXTURES='1'; pnpm r1:family-boundary-check`；固定 student-1 与 parent 在 in-app Browser 走查 zh/en 首页、`/dashboard/coursework`、`/dashboard/assignments`、`/dashboard/progress`。 |
| `artifact_url_or_path` | not_applicable（本轮为本地人工走查；正式视频/截图由 R1-14 CI artifact 承载） |
| `artifact_hash` | not_applicable |
| `retention` | 本摘要随 Git 历史永久保存；开发账号与业务数据到 R1-15 清理。 |
| `access_roles` | 仓库读权限持有者 |
| `failure_ticket` | pending：通知迁移 DDL/backfill 尚未在本轮开发库单独执行；最终空库重放和浏览器自动化分别归数据库门禁与 R1-14。末态 `pnpm typecheck` 已通过；并发迁移使数据库类型摘要暂时滞后，需由当前数据库迁移任务完成重放与 `pnpm db:types` 后关闭；本子门的 lint/R1 定向回归已通过。 |

## 共享白板尺规与公式识别回补

| 字段 | 值 |
| --- | --- |
| `gate_id` | `UX-R1-5-WHITEBOARD-CONSTRUCTION-20260731` |
| `domain` | 共享课堂/解析白板 / 圆规 / 公式 OCR / 工具栏分组 |
| `result` | pass（R1-5 教师工作区子门；不代表 R1-5 总阶段关闭） |
| `measured_value` | 工具栏将形状、尺规、手写转公式与填充色收敛为独立构造组，画笔、橡皮、颜色与线宽保留为绘制组。圆规移动/绘制句柄命中直径分别提升到 44px/48px，并按最近控制点路由窄画布重叠区域；固定教师浏览器实测圆规中心移动约 40px，拖动期间与保存后使用同一椭圆弧几何，生成真实 SVG 弧线。项目本地 Pix2Text 1.1.6 服务健康检查通过；用户提供截图直连返回 HTTP 200 与可编辑 LaTeX，应用内框选手写内容后返回 LaTeX 并成功插入 MathML 公式对象。验收后清空本轮测试对象，弧线与公式对象均为 0，浏览器 error 日志为 0。 |
| `threshold` | 圆规两类控制点均可稳定命中，拖动预览与落笔结果一致；公式服务存在可执行启动入口、健康检查与应用内插入闭环；构造工具不得与自由绘制属性混为同组；浏览器 error=0。 |
| `commit_sha` | `a96c451`（弧线几何、句柄与分组）、`ae1ff07`（OCR 启动与运维入口）、`cb83ddc`（窄画布命中与图层修复） |
| `migration_head` | not_applicable（本轮未新增迁移；共享 BoardItem 合同沿用 `20260731000700_doc26_annotation_board_items.sql`） |
| `environment` | Windows 本地 Next.js 16.2.11 开发服务；项目 `.tmp/formula-ocr` Python 3.12 / Pix2Text 1.1.6 CPU 服务；Codex in-app Browser。 |
| `dataset_manifest` | 复用固定教师与既有开发课次；OCR 只读取用户提供截图及本轮临时手写对象，未新建账号或提交业务审核；浏览器创建的弧线与公式对象已清除。凭据、token 与可识别未成年人信息未写入仓库。 |
| `started_at` | 2026-07-31T09:55:00+08:00 |
| `finished_at` | 2026-07-31T10:39:14+08:00 |
| `actor` | Codex desktop agent |
| `approver` | `swingislee`（以本次明确修复指令授权当前增量） |
| `command_or_runbook` | `pnpm exec vitest run tests/whiteboard-geometry.test.ts`（7/7）；定向 ESLint；`pnpm typecheck`；`pnpm formula:ocr:check`；对用户截图调用 `/pix2text`；固定教师在 in-app Browser 走查圆规移动/绘制、两组工具栏与公式框选/插入，随后清理测试对象。服务启动见 `docs/runbooks/formula-ocr.md`。 |
| `artifact_url_or_path` | not_applicable（本轮为本地人工走查；正式视频/截图由 R1-14 CI artifact 承载） |
| `artifact_hash` | not_applicable |
| `retention` | 本摘要随 Git 历史永久保存；本地 OCR 模型缓存与 `.tmp` 环境不入库。 |
| `access_roles` | 仓库读权限持有者 |
| `failure_ticket` | not_applicable（本轮子门通过；正式浏览器自动化仍归 R1-14） |
