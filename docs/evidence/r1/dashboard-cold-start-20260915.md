# 大屏首次加载与学科运营响应 · 2026-09-15

状态：**已单独部署生产，机器发布核查通过，待人工性能验收**。生产“主要数据可操作在 2 秒内”的目标仍待人工验收；本记录不关闭 R1-Live-2 或正式 Gate。

## 修复与候选

- 本轮实现：`9db92b98ede3c40ad9d0594fd09d6ac56c7ca035`，10 个文件。
- 独立组合候选：`2ded3c75ef984cedcb594a97ce63ebc459c850e3`，在生产 `f019ce9d` 上包含上一轮传输／测评补丁 `ec73d498` 和本轮修复，共 19 个文件；各文件 Git blob 与对应实现逐一一致。
- 源码包 53,227,520 字节，SHA-256 `4065703b8254b1607328b99254f8760f080b0de38759fd05e95ce8656062a61f`。源码包不包含本机环境、诊断脚本或运行日志。
- 发布前生产只读核对为 `f019ce9d` / `20260915-064839`，previous=`8bd17893` / `20260915-062847`；账本 `392 / 20260915007000_teaching_live_observations`，数据库指纹 `10e3f97e32b018403c9074efa4e258d699530a487c47de89b5d307ab7ff21a0c`。

本轮追踪从 HTTP 入口、首个响应头到正文结束，补充模块加载、Auth/REST 响应头及正文消费时间。新记录中客户端计时与 HTTP 入口相差约 6–8 ms；上一轮未覆盖的约 1.27 秒不再直接归因给鉴权。CPU 采样会扰动首次模块读取，采样运行的 9.75 秒单列为诊断值，不进入性能对比。

具体改动：

1. 关闭 `experimental.preloadEntriesOnStart`，首个请求按需加载路由。旧时间线显示全站课件、公开内容、学生详情等无关路由在首次请求期间加载；该开关及其取舍见 [Next.js 官方说明](https://nextjs.org/docs/app/guides/memory-usage#preloading-entries)，实现也核对了固定版本 16.2.11 的加载路径。
2. 管理员数据总览、今日工作、家长及学生首页分别按已通过身份检查的分支加载。
3. 将完整中英文客户端消息拆为按语言加载的静态消息块，保留 SSR、所有消息键及上下文；HTML/RSC 不再逐次携带整份消息对象。服务端翻译配置继续使用原始目录，语言切换和客户端弹层继续取得完整文案。嵌套 Provider 的配置继承规则参考 [next-intl 官方配置](https://next-intl.dev/docs/usage/configuration)。
4. 大屏统计与今日工作列表同时读取；当前学期的班级范围在学期查询完成后立即读取，与其他业务事实并行。
5. 同一次月统计只计算一次本期、上期月份，减少逐条历史事实的重复时区格式化。周／月、来源确认月份、去重、顺序和未知值口径保持一致。

## 时间与传输对照

计时范围是服务已经监听后，首条已登录 HTML 请求到完整正文；未提前请求页面或执行业务预热。下表两个候选使用同一本机隔离 Supabase、固定管理员和本机 Node 22 Alpine 镜像。Windows standalone 中的目录链接在临时归档中转换为 Linux 相对链接，运行文件内容保持原样；容器创建和归档展开耗时单列，不计入页面 HTTP 时间。它是本机 Linux 对照，尚未替代生产主机或浏览器测量。

| 本机 Linux | 上轮候选 ec60f97a | 本轮候选 2ded3c75 |
| --- | ---: | ---: |
| 新进程首次大屏完整 HTML | 2023、2008 ms | 1487、1441、1434 ms |
| 后续大屏完整 HTML | 1099、1004 ms | 952、961、935 ms |
| 大屏完整 RSC（无路由状态头） | 1002、968 ms | 925、907、870 ms |
| 大屏 HTML，gzip | 212,688 B | 51,396 B |
| 页面引用的 JS/CSS，gzip | 556,530 B | 713,803 B |

消息块从正文移到可缓存静态资源：首次无缓存访问的 HTML 加引用资源合计约 769 KB → 765 KB，主要传输收益发生在后续刷新复用静态资源时。不能将 HTML 单独缩小约 76% 解释为首次全部网络传输下降 76%。两种语言分别加载自身消息块。

Windows 下新构建文件首次读取仍测到 **3044 ms**，后续两次重启首次请求为 **1379、1369 ms**，该慢样本保留。Windows 与 Linux 的差异集中在模块文件读取；本记录没有将其进一步断定为杀毒、磁盘或 JIT 的某一项，也没有声称各平台的冷启动都低于 2 秒。

带 Next 自身编码、校验的路由状态树，从数据总览发起侧栏形式的 RSC 读取：

| 本机 Linux 页面／状态 | 完整响应 |
| --- | ---: |
| 今日工作 | 622 ms |
| 线索 | 837 ms |
| 沟通 | 749 ms |
| 测评 | 490 ms |
| 学生全部记录／待测评 | 1505 ms |
| 分班 | 546 ms |
| 续班 | 316 ms |

这些请求使用真实路由状态，但未运行浏览器点击、绘制或水合计时。另在 Windows 核查了学生当前工作、全部记录、再联系，线索、沟通、测评、分班、续班、续班信号／成长、活动、协作、经营分析与中英文大屏，完整 HTML 约 254–1758 ms；财务沿用上一轮未启用状态的闸门证据。

## 正确性与检查

- 固定管理员、教师分别对照本期／上期、周／月，共 8 组完整 DTO 和顺序哈希一致；本轮没有修改鉴权、MFA、RLS、业务状态、写入或缓存账号／业务结果。
- 50 项定向检查通过，覆盖消息 Provider SSR、统计口径、数据读取与下钻；TypeScript、限定 ESLint、独立生产构建通过。
- 额外 8 项 HTTP 检查通过：匿名登录闸门，管理员中英文 MFA 闸门，固定家长／学生中英文首页，以及实际消息资源的 `immutable` 缓存响应头。这些检查验证服务端响应，不替代浏览器交互验收。
- `pnpm bundle:report` 已对照。消息加入静态块的体积变化与正文移除一起评估，未用清单中所有动态引用冒充实际首屏下载量；表中引用资源来自真实 HTML。
- 之前的 `assessment-unified-workbench.test.ts` 8 项历史失败见[上轮记录](dashboard-server-transport-20260915.md)，本轮未修改其实现或把历史失败记作通过。
- 临时容器通过任务标签核对后停止并自动移除。本机固定管理员的临时 MFA 因子用后撤销。一次诊断脚本的路由头编码失败已修正，异常留下的单个临时因子已精确移除，并补齐脚本异常清理；未变动原有因子或生产身份。

## 发布范围与回退

候选包含应用、Next 启动配置及上一轮的服务端网关配置 `SUPABASE_SERVER_URL=http://127.0.0.1:8000`；公开 Supabase origin 保持 `https://supabase.mathin.club`。无数据库迁移、业务写入或 Storage 变更。

产品负责人明确回复“上线生产”后，重新核对生产 current、指纹、账本和网关，备份 owner-only 环境配置及 current/previous 指针，完成 Linux 构建后原子切换应用，短暂重启应用与已有随动 worker。回退同时恢复原环境配置与原 release。旧候选 `ec60f97a` 已随本候选一起发布。

## 生产发布 · 16:12 北京时间

- 2026-09-15 16:12:11 完成应用切换：current=`2ded3c75` / `20260915-080520`，previous=`f019ce9d` / `20260915-064839`。应用和既有 worker 均使用新 release；同机网关环境已在真实应用进程核对，公开 Supabase origin 保持一致。
- 发布绑定的配置／指针备份为 `mathin-app-prechange-20260915T080508Z-dashboard-deep-2ded3c75ef98`，仅所有者可读。隔离目录完成配置和指针恢复演练，原版本与候选均通过独立 loopback 启动、双语登录页和匿名权限检查；临时进程用后停止。Linux 产物与源码密钥扫描通过。
- 41 项 HTTP 检查和 2 项同机网关检查通过，覆盖公网及 loopback 健康、双语登录、大屏、学生和学科运营相关路由的匿名权限。已核对生产产物的启动预加载设置。已登录页面的绘制、水合和主要数据可操作时间仍属于人工性能验收。
- 账本、函数、RLS、索引和错误基线保持一致，`operational_errors` 增量为 0。业务表哈希与保护对象计数中仅 `domain_events` 增长：构建期间原版本追加 661 条课次操作事件，全部早于应用切换；原 2300 条事件哈希一致，其余业务摘要和 Storage 计数一致。
- 发布后日志记录了 33 次旧接口标识请求错误。逐条核对标识仅存在于上一版 Action 清单，新版本服务异常为 0；已提示刷新此前打开的页面。该日志记录与匿名路由检查分别保留。
- 后续用户反馈已将上述 33 次请求定位到 `saveSessionReviewsAction`，确实阻止了旧编辑页保存和逐生发布；目标课次随后于 16:29:31 发布成功，详见[课评失败诊断](session-review-deployment-skew-20260915.md)。新版页面检查通过不能覆盖跨发布的旧编辑页，课评草稿恢复保护仍存在缺口。
- 最终状态为 `DEPLOYED_PENDING_USER_ACCEPTANCE`。本次没有数据库迁移或 Storage 写入；构建 staging 的源码目录已精确清理，运行 release、配置备份和可执行回退脚本保留。

生产私有证据位于 `.tmp/dashboard-deep-production-20260915/`，同时归档至上述受控生产备份目录。归档包含 30 个证据／操作文件及摘要清单，共 294,912 字节，SHA-256 `4fd75ecce852c5bbae8c136d9485ff05a99da2bf88ad205eaddc3b47707d9cd0`；维护者可访问，至少保留至人工验收后 30 天。

私有证据位于 `.tmp/dashboard-deep-latency-20260915/`：完整脱敏时间线、CPU 诊断、Windows/Linux HTTP 与路由状态请求、完整数据哈希、资源体积、源码包和候选清单；维护者可访问，至少保留至人工验收后 30 天。生产只读目标记录仍在 `.tmp/dashboard-latency-20260915/production-readiness.json`。开发体验入口为 `http://192.168.5.213:3130/zh/dashboard?view=overview`；开发模式编译速度不用于生产性能对比。
