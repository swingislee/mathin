# Base 用户运营资料生产增量 · 2026-09-14

状态：**生产增量已提交，机器核对通过，待人工业务验收**。本批保持 R1-Live-2 及现有 Gate 状态。

后续 [Base 大屏复核](base-dashboard-reconciliation-20260914.md) 发现获客入口仍读取旧文件、2 条来源诺访未进入业务统计，以及在读集合和日期口径差异；本页的增量持久再读通过不代表所有大屏指标已经对齐。

## 来源与执行范围

用户要求将 `2026-09-14【思维】用户与产品运营表.base` 与生产比较后增量更新，并补充确认姓名写法、班级沿用生产。生产对应姓名已经正确，因此本批学生身份和正式班级关系保持原值。

新版来源 SHA-256：`60c36cf906ec15fc6d3f4d4dc515d34287db391c83b6ea631efbb359a01fe6dd`，3,651,037 字节。生产旧主表为 9 月 7 日版，5,212 行；旧原档的相关业务字段与旧文件逐项相同。新版增加 46 行、修改 189 行的 535 个显示字段，另有 2 行未出现在新版中。缺少的旧行继续保留。4,703 行的原始编码变化未改变对应字段的显示内容，未据此重建业务。

本批完整保存新版原文件，并追加 235 份新增或变更的来源记录及第三版结构化资料，共 3,747 个非空业务字段。旧文件、旧原档及旧字段版本保留。业务更新复用既有对象主键，以“旧来源生成值、新来源生成值、生产实际值”逐字段合并；独立生产修订保留，推定归属继续保留推定标记。

## 实际增量

| 对象 | 新增 | 更新 |
| --- | ---: | ---: |
| 线索 | 25 | 87 |
| 线索沟通 | 69 | 76 |
| 活动 | 16 | 3 |
| 活动登记 | 16 | 35 |
| 测评结果 | 14 | 11 |
| 课程机会 | 1 | 0 |
| 课程报名 | 7 | 0 |
| 报名分班描述 | 7 | 3 |

报名分班描述属于来源资料；正式班级成员关系没有变化。保留 3 条人工业务修订和 8 处存在独立编辑的线索备注。1 条没有联系方式、存在同名候选的到访来源已保存为待核对资料，尚未关联到具体身份或生成其业务投影。

新来源复制 72 条历史范围标记，有本期依据的 22 条主体范围恢复办理；原历史事实期间限制及人工范围决策保持。新增 15 条带来源依据的推定关联和 89 条由生产既有触发器产生的业务参与人记录。

## 目标、备份与验证

- 主机：`xiaomi`；配置及运行进程的 Supabase origin：`https://supabase.mathin.club`；应用监听：`127.0.0.1:3131`，进程 `3825682`。数据库指纹：`10e3f97e32b018403c9074efa4e258d699530a487c47de89b5d307ab7ff21a0c`。
- 应用保持 current `20260913-155934` / `3d9892bb87c2361e6662e685b606ac77a3e82e29`，previous `20260911-153257`；ledger 保持 382，head `20260913000200_school_source_collaboration_alignment`。本批只更新业务资料，未执行迁移、发布、服务重启或 Storage 写入。
- 写前备份：`/home/swing/services/mathin/backups/mathin-db-prechange-20260914T053535Z-base-incremental-20260914`；custom dump 346,954,795 字节，TOC 6,504 行；dump SHA-256 `000e9f3b8ea9007ee596ca4d2c33cd671a5617083fea3bccb4827694ccb062ff`。正式执行前再次核对 dump 摘要。
- 最终事务 SHA-256：`6e379c75cb4bc04cccea8ec67876f9d595d6bcec80cc2786284812bc5aa972e5`，13,408,026 字节；计划摘要 `28e17a4e3778fbb4efce92fd1bb0bfec9e7073a6decf91c0d141b29a317ae67c`。同一 SQL 先以 SERIALIZABLE 完整执行后回滚，独立连接核对零残留，再正式提交。早期演练的保护表主键、生成列问题均在回滚后修正；一次保护核对耗时异常的演练已取消并确认零残留，临时表索引及统计修正后通过。
- 正式提交完成：`2026-09-14T05:43:13Z`。新连接逐项核对插入及更新字段，235 份原文和 3,747 个结构化字段均一致。现有管理员的 `authenticated` 角色调用应用 RPC，再次逐条读取 235 份最新字段，全部一致。
- 5 项增量合并定向测试及相关 ESLint 通过，覆盖生产独立修订、字段冲突、统计子字段、清空、数组和重复应用。批次唯一键及事务内旧值比较防止重复写入或覆盖并发变更。
- 应用健康检查通过，业务错误仍为 2,017 条，本批新增 0。机器结果不替代人工业务验收。

| 受保护对象 | 写前／写后 |
| --- | ---: |
| profiles | 41／41 |
| 学生 | 1,469／1,469 |
| 正式班级 | 34／34 |
| 正式班级成员关系 | 171／171 |
| 课次 | 429／429 |
| 考勤 | 1／1 |
| 学生跟进 | 200／200 |

事务内逐行保护既有来源、字段、关联、家庭联系方式、年级、课次和考勤等对象；只允许精确清单中的字段更新及明确的范围恢复。上表对象还通过独立连接的完整行摘要对照。

## 受控记录与保留

批次：`base-incremental-20260914-60c36cf906ec15fc`。生产原文、目标快照、逐项差异、SQL、备份清单、演练及应用回执保留在生产 owner-only 目录 `/home/swing/services/mathin/staging/base-incremental-20260914/`，至少保留 90 天，访问限管理员及本次获授权执行人。

自动审批拒绝了最初的完整生产表外带方案；改为在生产机内计算后执行。生产个人资料没有导出到本机，本机 gitignored `.tmp/base-incremental-20260914/` 仅保存用户提供的来源、操作代码、数据库结构及非个人资料汇总。Git 保存本摘要、通用合并函数和定向测试。

业务验收入口：[生产线索表](https://mathin.club/zh/dashboard/followups/leads)、[生产学生表](https://mathin.club/zh/dashboard/students)及学员来源资料。完整业务回退应使用本批备份和逐项清单，在核对提交后的新增业务后按事故恢复授权执行；本批未执行数据恢复或清理。
