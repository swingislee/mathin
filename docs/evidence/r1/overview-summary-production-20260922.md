# 数据总览汇总优化生产发布

> 状态：2026-09-22 14:02（Asia/Shanghai）生产已部署、机器检查通过，待管理员浏览器验收；首次 1 秒可操作尚未证明。阶段保持 R1-Live-2。

用户先明确要求“改动推送生产”，随后明确允许推送 `swingislee/mathin` 公开仓库并发布生产；过程中追加了小程序 AppID 与连接脚本隐私修复。

## 发布范围

- 生产候选 `176600ec4c4389050ffe00342c8db2f7a22315b3`，分支 `codex/deploy-overview-summary-20260922`，release `20260922-055726`。
- 以原生产 `f6c41abf` 为基线，保留其 PostgreSQL 兼容与私有目录执行身份修复；登记已上线的 `d40f694f`，加入总览补丁 `b5e2cdbc`、隐私保护 `b4923d77` 和视图守卫修正 `3b856263`。本次应用不包含开发分支新增的小程序业务、0.2.0 版本号等其他任务提交。
- 新增生产迁移仅 `20260922002000_overview_summary_shared_candidates`，规范化 SHA-256 为 `9c7d8fc8bcbd08561f80357ca2805345af823460dae62b855cdac1ff87de769a`；账本 431 → 432。更新两处汇总函数，保留 owner=`postgres`、ACL、SECURITY INVOKER、RLS、其他函数及全部视图。
- 数据总览实际默认读取获客/沟通汇总，省去三个历史明细入口；详情沿用按需读取。完整数据合同及本机内部请求量见[实现记录](overview-summary-integration-20260922.md)。
- 实际 AppID 从共享配置移入已忽略的本机配置；提交和推送防护见[脚本隐私记录](script-privacy-guards-20260922.md)。历史 AppID 仍存在，未发现高置信 AppSecret 或其他密钥泄露，未重写历史。

## 发布验证与阻断处理

1. 只读核对 `xiaomi`、实际应用 PID/cwd、`127.0.0.1:3131`、公网 Supabase origin、内部 `127.0.0.1:8000`、数据库指纹、旧函数及迁移校验和；配置 SHA-256 全程一致。
2. 最初的生产演练由 `OVERVIEW_BUSINESS_SCOPE_CHANGED` 拦下：新迁移再次使用了强制 `r.` 别名的旧文本守卫，生产 PostgreSQL 省略了该无歧义别名。独立连接证明回滚零残留后，将守卫修正为只接受有/无 `r.` 的同一完整过滤条件；四个真实生产视图及“缺失过滤、错误列、额外 OR”负例通过。修正已同步开发分支，未改变函数业务体。
3. 修订候选重新提交、推送、打包和创建最终备份。应用构建来自 `14fd66c2`；与最终提交只差迁移守卫及类型文件摘要，应用输入和函数体逐字核对相同，复用构建并在 release 元数据记录 `artifactBuiltFrom`。未把旧候选备份当作最终备份。
4. 生产 `SERIALIZABLE` 事务执行相同迁移、12 组管理员/有效员工的完整事实与四种周月汇总对照；管理员→员工→管理员 prepared statement 三组一致。执行备份中的显式回退 SQL、ROLLBACK，随后独立连接确认账本、函数、ACL、16 张业务/冻结表及 Storage 零残留。
5. 正式事务执行同一文件、校验和与结果断言，刷新 PostgREST cache；提交后独立复核通过。此前拒绝的抽样员工处于待修改初始密码状态，检查改用已就绪身份，没有改变账号或业务权限。
6. 新旧应用临时端口验证、430 个旧 Server Action ID 保留、密钥未进入产物（5,036 个文件）、指针往返演练通过。原子切换应用并重启原本运行的 worker；线上健康、zh/en 登录、匿名保护与网关共六项公网检查通过，切换后服务错误日志命中为零。

## 生产测量

| 测量范围 | 发布前 | 发布后 |
| --- | --- | --- |
| 管理员获客/沟通汇总 SQL，独立会话三次 | 2663 / 1624 / 1529 ms | 842 / 742 / 761 ms |
| 固定受限员工，中文完整 HTTP 三次 | 1047 / 987 / 1001 ms | 1545 / 901 / 1032 ms |
| 同一受限员工，英文完整 HTTP | 939 ms | 1002 ms |

SQL 使用服务端选择的有效真实角色、RLS 与固定期间，只输出摘要和耗时。HTTP 使用已授权的固定测试账号，其生产角色实际是受限员工；凭据从本机忽略文件读取，仅经 SSH stdin 传入内存，不保存 Cookie 或页面业务内容。HTTP 请求从生产主机 loopback 发起，发布后的第一个样本包含应用重启成本；发布前已预热，不能把两者当作等条件冷启动对照。

这些数据证明汇总 SQL 减少开销、页面兼容通过；不能据此宣称管理员浏览器首屏 1 秒已达成。其他指标仍有约 3.2 MB 内部读取，后续继续按汇总、数据库分页和展开详情优化。

## 备份、回退与证据

- 最终备份：`/home/swing/services/mathin/backups/mathin-prechange-20260922T055804Z-overview-summary`；目录 0700、文件 0600，数据库 dump 356,000,909 字节，TOC 6,788 行。完整数据和函数回退定义只在生产保存。
- dump SHA-256：`b495136e620368cb0694118dd546916c7db04aaf0ee1f1c3025b94f30bcca03e`；回退 SQL：`8354fa9f866630a5843fbab80f68a26db747847924314461dec65cf6d42bc934`。
- `previous` 保留 release `20260921-192226` / `f6c41abf`。需要完整回退时先恢复旧应用，再执行备份的两函数回退与准确匹配 checksum 的账本删除，刷新 schema cache 并核对摘要；不恢复整库覆盖正常业务新增。
- 生产归档 `deployment-receipt.json` SHA-256：`0e8d07fc1255e14d1ac044875781d0a9c094a33173e64584a19b1484c152e86d`；保留至 2026-12-21，访问角色为项目负责人和授权运维。
- 本机发布摘要 `.tmp/overview-summary-release-20260922/`；守卫修订后开发证据 `manifest-post-guard.json` 的规范化 SHA-256 为 `2b7684ace75d4a384e00976b4d3ee4cd784971bec8a2102fae593004494e4f88`。共享 Git 只保存本摘要。

生产验收：[数据总览](https://mathin.club/zh/dashboard?view=overview&period=month&date=current)。
