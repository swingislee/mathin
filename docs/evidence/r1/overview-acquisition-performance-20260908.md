# 获客明细读取性能 · 2026-09-08

状态：开发端已交付，待人工交互验收。当前生产阶段及 Gate 保持原状态；本批只修改本机隔离开发库。

点击学服完整周期的姓名行，默认打开该人员的获客明细。此前此路径读取整批来源、沟通、报名与测评对象，再计算总览和筛选人员。已移除 Server Action 传输开销后，该路径仍需约 3.4～4.1 秒；另观察到一次 20.8 秒异常延迟，其原因未单独定位。

本批增加来源文件、来源表和记录 id 的联合索引，并提供 `list_staff_overview_acquisition_sources`。数据库每行只展开一次单元格，保留原顺序、缺失及重复字段语义；以游标分页返回计数、姓名和署名字段。3,634 条来源由原先 22 次请求变为 4 次。函数为 stable / security invoker，沿用原表 RLS；未安装迁移的目标继续使用既有投影路径。

获客明细另外采用专用读取模块：沟通、报名、测评只读取来源 id 与线索 id；使用现有 `buildOverviewAcquisitions` 和范围筛选合同，跳过其他指标的汇总。全部来源关联仍参与历史归属判断。来源依赖读取不完整时显示不可用。

| 固定开发管理员，同一人员 | 变更前完整 HTTP | 变更后完整 HTTP | 明细条数 |
| --- | --- | --- | --- |
| 2026 年 8 月 | 4,052 ms | 1,191 ms | 131 |
| 2026 年 9 月 | 3,435 ms | 1,058 ms | 32 |

以上为本机开发服务的具体请求样本，包含接口与网络开销，不是浏览器点击到绘制的延迟保证。专用读取模块在七组日期／人员场景中为 844～910 ms，约 3.81 MB UTF-8 响应数据。尚未将全部关联事实和分页下推数据库。

机器验证：

- 新旧模块在机构、指定人员、其他人员、未归属人员、本期、上期和周范围下逐条对比，记录内容及时间范围一致；搜索、姓名补充和分页通过。
- 3,634 条数据库投影逐行一致，游标无遗漏；固定非管理员读取为空，匿名无函数执行权限；事务回滚恢复函数和索引的原始存在状态。
- 获客投影、游标、来源日期和去重、范围及接口错误合同的定向测试通过；受影响文件 ESLint、定向 TypeScript 与生成类型摘要检查通过。开发页面返回 200、中英文接口及未登录拦截通过；人工验收待完成。

本机 migration：`20260908130000_overview_acquisition_reads`，规范化 SHA-256 为 `597b6a2315581658fe76a6b78d95d5174e30ae1a0d37af0c18c34cabfe872e37`。本机目标核对包含执行主机、实际 Supabase origin、监听进程、Docker 网络及数据库指纹；没有 SSH、生产数据库、业务数据写入或服务重启。

数据库验证可通过 `node --experimental-strip-types scripts/overview-acquisition-reads.mjs --check` 重跑。应用迁移使用同一脚本的 `--apply`，要求已通过同一迁移和断言版本的检查。仓库摘要保留至 Git 历史，访问角色为仓库读权限持有者；开发机临时计时日志不作为发布 Gate 证据。
