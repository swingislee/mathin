# Dashboard 查询优化生产发布与资源首屏检查 · 2026-09-20

状态：**已部署生产，机器检查通过，待登录后的实际首屏验收**。本轮不关闭 R1-Live-2 或正式 Gate，也不宣称生产主要数据已在 2 秒内可操作。

## 发布范围与结果

- 用户明确授权：“推送生产，同时检查生产端资源首屏问题”。
- 2026-09-20 09:06:26 北京时间完成切换：`1eaab0fbe3285d44f89924b8c1f771c5417ef657` / `20260920-005221`；previous 保留 `2ded3c75` / `20260915-080520`。应用和既有 worker 均运行新 release。
- 独立候选从实际生产 HEAD 构造，移植 [9 月 19 日查询补丁](dashboard-query-audit-20260919.md)，不包含后续学生入口重组、空间教具和画笔开发。源码包 SHA-256：`b066ce867302f8a9554279614e48bbbc01c9b6f1b865c7bb000dcefac9527f69`。
- 发布请求内身份与目录复用、批次并行读取、学生详情并行、管理员续班权限去重，以及下列三条查询迁移。数据库账本从 392 增至 395。
- 定向测试 6 文件 / 44 项、类型检查、迁移类型摘要、源码密钥扫描和 Linux 构建通过。发布包密钥扫描通过，环境文件未进入产物。
- 上线后 25 项健康、双语登录和匿名权限闸门检查通过；运行日志错误及切换后的 `operational_errors` 均为 0。所查业务表摘要、受保护资源计数和 Storage 对象摘要保持一致；迁移 checksum 与发布包一致。

| 迁移 | SHA-256（LF 归一化） |
| --- | --- |
| `20260919100000_dashboard_asset_query` | `a364f977c5189ea489622bbe7b7f3a17bb1b349c029057f36a75f1ce7ecdc55f` |
| `20260919101000_session_event_student_lookup` | `f92b9e07132a96cb8c488070b0cf969b42491bc58dd462fab3c4695eec5dc991` |
| `20260919102000_courseware_progress_summary` | `8ddfad4a015b4a08043890811b2704b80354ec1b03618f817bcf87a071121555` |

## 备份、权限与回退

写前重新核对主机、真实应用进程、监听端口、公开 Supabase origin、同机网关、数据库指纹和账本。原函数及事件表 owner 均为 `postgres`，DDL 使用该 owner。原资源 RPC 的 ACL、固定 search_path 与 definer 属性保持；新增汇总 RPC 使用 invoker，匿名调用被拒绝，authenticated 可执行。

最终候选绑定备份：`mathin-prechange-20260920T005104Z-dashboard-query-1eaab0fbe328`，仅所有者可读。数据库 dump 为 351,481,706 字节，SHA-256 `793d84db50483dbafb67db67b9ff1955e4241e64e89c566f938a22a15010d401`，TOC 6566 行；同时备份生产配置和 current/previous 指针。TOC 校验不等同于完整恢复演练。

三条迁移在 SERIALIZABLE 事务中完整执行并回滚，独立连接确认函数、策略、ACL、账本和核查的数据摘要零残留；正式提交使用相同迁移字节及断言。核对包含四类现有角色对 10 条真实事件的可见集合、资源首页完整行与顺序、课件年级汇总与全量源表结果。开发端完整异常参数及历史名册合同沿用前一天证据。

演练脚本先遇到旧事件策略全表检查超时，随后改用明确 UUID 数组定位样本；SQL 展开曾因 JavaScript 替换语义破坏美元引号，已改为回调原样插入。失败连接均回滚并独立确认无残留，候选源码和迁移 hash 未变。最终演练通过后才执行正式迁移。

隔离目录完成配置、指针的恢复演练，新旧应用分别在临时 loopback 端口启动，双语登录与匿名闸门通过，临时进程已清除。切换脚本保留失败回退及独立应用回退入口。三条迁移与旧应用兼容，应用回退不恢复或覆盖业务数据库。

## 已打开页面的发布兼容

针对[上次课评保存失败](session-review-deployment-skew-20260915.md)，本次构建在生产主机内复用当前 Server Action 加密密钥，并验证旧版 428 个 Action 标识全部存在于新版；保留 3 个新产物没有的旧静态文件。密钥未传到本机，也未出现在客户端静态文件中。

这证明本次接口标识兼容，不替代真实课评写入验收，也没有增加跨版本草稿恢复功能。生产发布检查未提交真实课评或其他业务数据。

## 资源首屏的分段测量

资源库按 `p_limit=11` 读取，显示前 10 行并判断下一页；6 个图片单元格对应 5 个唯一对象。预览元数据已批量读取，签名地址已批量生成。

| 生产测量 | 发布前 | 发布后 | 边界 |
| --- | ---: | ---: | --- |
| 原版轨道资源 RPC | 600.351、600.400 ms | 612.911、663.871 ms | 相同首页参数，完整函数 EXPLAIN ANALYZE；本轮没有测到耗时改善 |
| 适配轨道资源 RPC | 565.706、591.296 ms | 528.342、519.952 ms | 完整函数时间，不是页面时间 |
| 原版轨道 shared buffer hits | 236703–237639 | 9919–10004 | 重复块访问约减少 96%，不等于页面快 96% |
| 预览 revision / object 元数据 | 1.836 / 1.692 ms | 沿用原查询 | 返回 6 条 revision、5 条 object，无逐图串行查询 |
| Storage 同机 HEAD | 首条 2797 ms，之后 10–53 ms | 10–59 ms | 只检查对象元数据，使用运维服务权限；不是浏览器用户下载或签名接口计时 |
| Windows 公共登录页完整 HTTP | 385 ms | 209 ms | 冷暖状态不同，不能作为应用加速对照或 Dashboard 指标 |
| 公共页面所引用 JS/CSS | 26 个 / 584726 B | 相同 | 均 200、immutable；25 个 gzip，203 B 的微小文件未压缩 |

5 个唯一图片合计 **382678 B（约 374 KiB）**，6 个图片单元格合计 480328 B。列表中包含的视频等文件并未自动下载；不能把这页 10 个资源的原始总容量约 150 MB 当作首屏流量。

保留两个慢样本：Windows 首次健康检查连接的累计 TLS 建连约 846 ms；生产 Storage 第一次 HEAD 约 2.8 秒，空闲后的复测未再出现。当前没有证据将后者确定为数据库、鉴权、连接池或对象读取中的某一项。数据库迁移期间的负载和首次请求状态都可能影响该样本。

同日[专项审查](storage-first-request-audit-20260920.md)补充：该 HEAD 接口不读取图片文件，七天日志中的浏览器签名读取最长约 454 ms；2.8 秒具体阻塞点仍未知。另确认服务端签名绕公网造成额外等待，局部补丁已通过机器检查，尚未部署。

**尚未完成的体验证据：**浏览器连接返回不可用，未取得已登录 Dashboard 的 FCP/LCP、绘制、水合、侧栏切换或主要数据可操作时间。运维 HEAD 不包含图片内容传输，公共登录页资源也不是 Dashboard 的全部首屏资源。最初的私有图片本机下载检查被自动审批拒绝，已改用生产主机内 HEAD；未下载私有图片或签发该检查的短效地址。

生产入口：[管理员数据总览](https://mathin.club/zh/dashboard?view=overview)、[课件资源库](https://mathin.club/zh/dashboard/courseware-assets)。本轮证据支持发布健康、查询结果和权限保持；资源库首次进入与刷新是否达到 2 秒仍待登录后的真实测量。

私有证据目录：`.tmp/dashboard-query-production-20260920/`。生产备份内保留发布操作、回退脚本及校验后的证据归档，限维护者访问，至少保留至人工验收后 30 天。

归档包含 47 个证据／操作文件，372224 字节，SHA-256 `c42a132e0a4456f63c67eadec99dfdbf6d540a3c6b8434d1d2c862631555c85d`。远端逐项摘要复核通过；本轮 staging 构建源码已精确清理，运行 release、previous、备份及回退入口保留。
