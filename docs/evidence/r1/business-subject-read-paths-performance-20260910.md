# 学生列表与当前线索读取提速 · 2026-09-10

状态：本机迁移已应用，定向机器检查通过，开发端已交付、待人工交互验收。生产未写入，R1-Live-2 和 Gate 状态保持原记录。

## 本次调整

- 为 `history_workflow_scopes.student_id / lead_id` 补充覆盖全部非空身份的索引。原有索引仅覆盖 `resumed_at is null`，而 `business_subject_is_current` 还需要检查恢复后明确归档的范围；原查询会按线索反复扫描范围表。
- `student_list_facts` 在工作列表中先构造候选身份：保留未暂停身份，以及由未完成工作单恢复的学生或关联线索，再执行原有权限、阶段和资料汇总。后续 `subjects` 继续按最终选定的可见线索检查完整条件。
- 全量档案保留全部身份；当前记录和历史记录的查询口径分别保持。权限函数、RLS 策略、RPC 参数、结果字段、筛选选项、分页和写入流程沿用原实现。
- 新增迁移 `20260910005000_business_subject_read_paths`。函数调整按实际定义中的明确读取片段定位，保留已有协作分组实现和函数元数据；定位失败时事务报错。

## 测量结果

同一本机隔离数据库、固定开发身份、同一事务时间内对照。下表为完整学生列表 RPC 的数据库执行耗时，含精确总数及筛选选项，每页 20 行。

| 查询 | 调整前 | 调整后 |
| --- | ---: | ---: |
| 管理员：当前待首联、全部可见范围 | 977 ms | 220 ms |
| 教师：当前待首联、全部可见范围 | 2,833 ms | 460 ms |
| 管理员：当前待续费、全部可见范围 | 966 ms | 245 ms |
| 教师：当前待续费、全部可见范围 | 2,732 ms | 379 ms |
| 管理员：当前待首联、本人参与 | 1,922 ms | 209 ms |
| 教师：当前待首联、本人参与 | 3,327 ms | 316 ms |

独立 `EXPLAIN ANALYZE` 对照中，当前线索 ID 查询（相同排序、上限 1,000）从管理员 472 ms / 教师 790 ms 降为 33 ms / 357 ms。另一项事务检查对两类线索视图的全量 ID、排序、参与/分组/编辑标记进行了完整对照，结果一致。

本机 Next 开发服务的预热后完整 HTTP 响应，路由为 `/zh/dashboard/students?scope=all&stage=awaiting_first_contact&pageSize=20`，两次请求均为 200、包含 20 行：

| 身份 | 调整前，两次完整响应 | 调整后，两次完整响应 |
| --- | --- | --- |
| 管理员 | 1,601 / 1,460 ms | 790 / 662 ms |
| 教师 | 3,285 / 3,342 ms | 865 / 792 ms |

这些数据是开发端读取测量，未代替人工交互验收或生产测量。首次页面编译另有额外等待，未计入预热对比。

## 检查与本地应用

- `node scripts/business-subject-read-paths.mjs --check` 通过：35 个学生列表场景覆盖五个阶段、当前/全量档案、本人/分组/未分配范围、搜索、中英文排序、复合筛选和超界分页；另比较两个身份下的完整线索视图。每个场景返回的完整 JSONB 均相等，包括行字段、顺序、阶段计数、筛选选项和权限标记。
- 固定管理员、教师、销售、多岗位身份的读取结果保持；教研的原拒绝结果保持，学生/家长及未登录身份继续得到相应拒绝。枚举暂停与工作单组合，确认预筛选保留原结果所需的候选学生。
- 对照整个 public 函数目录（仅允许目标函数正文变化）、策略目录、表/视图权限和既有索引：无额外变化，目标函数的签名、owner、ACL、security definer、search path 和 plan cache 配置保持。
- 学生、线索、档案工作范围、工作单、参与人、分组、课程报名及班级成员等 12 张相关表摘要保持；事务回滚后的完整目录和业务摘要恢复一致。
- `node scripts/business-subject-read-paths.mjs --apply` 在重新核对执行主机、Supabase origin、监听进程、Docker 网络及数据库标识后，复核通过记录与实际状态一致，保存实际原函数定义，原子应用 DDL 和规范化 checksum 账本；业务与权限检查通过。
- 检查脚本 ESLint、变更文本检查通过。没有应用代码或类型合同变更，本次以实际数据库对照和 HTTP 页面检查覆盖风险。

大查询计划、HTTP 计时、读取对照及回退原函数保留在本机受控临时目录，仓库仅记录无个人资料的摘要。

## 后续已定位的热点

全量历史档案需要保留历史身份，本次没有套用工作列表的提前排除条件。它仍存在较重的逐身份权限及协作/资料汇总：例如教师全量待测评约 7.2 秒，管理员全量待首联约 4.4 秒。后续优先考虑对关联信息进行批量汇总，继续以完整字段、筛选选项和权限结果等价为前提。

本批是独立开发增量；生产发布仍需按实际版本完成目标核对和迁移发布流程。
