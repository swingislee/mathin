# 2026-09-20 画笔极细档与线宽滑块

状态：开发端已交付，待人工视觉与书写验收。当前阶段保持 R1-Live-2，生产未部署。

粗细菜单新增“极细”档，基础归一化线宽为 0.0015，是原“细”档 0.003 的一半；原细、中、粗档与默认档保留。原弹层内复用 Slider，支持 0.0005～0.012、步长 0.0001 的自定义线宽，拖动时更新当前数值、线宽预览与工具栏圆点。四个预设保留直接选择和选中态，中英文标签完整。

界面线宽标尺显示 0.5～12.0，不标注为屏幕像素；保存时仍为归一化线宽。实际显示沿用画布尺寸、共享参照宽度及 PF 压力算法；基础笔宽仍有现有的 1 CSS px 下限，在较窄画布上最细的几个值可能落在同一下限。主、副板书现有工具态双向同步包含 `sizeNorm`，自定义值沿用该链路。此次没有调整笔迹算法、历史笔迹、保存协议或数据库。

验证：

- Toolbar/store 的 ESLint、全库 TypeScript 与本次 diff 空白检查通过。
- `classroom-board-checkpoint`、`whiteboard-palm-input`、`client-messages` 三个相关测试文件共 23 个用例通过；新增中英文标签读取通过。
- 开发端局域网健康接口返回 `ok / development`；复用已有开发服务，没有重启或业务数据写入。
- 未执行浏览器自动操作或截图验收。教师应在课堂原“粗细”菜单中对比“极细”与“细”，再拖动滑块书写，检查主、副板书、触摸滑动和翻页后的线宽设置。

本次仅提交线宽菜单、预设、中英文及上述记录；工作树中既有教具与空间操作修改保留给原任务。
