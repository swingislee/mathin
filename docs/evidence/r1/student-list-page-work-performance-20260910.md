# 学生档案分页读取提速 · 2026-09-10

状态：本机迁移已应用，定向机器检查通过，开发端已交付、待人工交互验收。生产未写入，R1-Live-2 和 Gate 状态保持原记录。

## 本次调整

- 筛选和排序只传递所需字段，逐行聚合筛选值，减少整行资料在字段展开及哈希聚合中的复制。
- 可见身份继续在分页前按原条件判断；可编辑、可联系标记使用原表达式，只为最终页计算。2,329 条匹配记录返回 20 行时，标记函数实际调用 20 次。
- 原私有 `student_list_facts` 保留完整返回合同，底层事实查询只有一个实现；新增两个私有 helper 与原函数同 owner，撤销 PUBLIC、anon、authenticated、service_role 的直接执行权限。
- 新增迁移 `20260910007000_student_list_page_work`，保留 RPC 参数、结果字段、行顺序、精确总数、筛选选项及既有权限函数。

## 测量结果

本机 Next 开发服务预热后的完整 HTTP 响应，固定开发身份，路由 `/zh/dashboard/students?population=records&scope=all&stage=awaiting_assessment&pageSize=20`。每次均为 200，返回 20 行。

| 身份 | 调整前，两次完整响应 | 调整后，两次完整响应 |
| --- | --- | --- |
| 管理员 | 5,036 / 4,798 ms | 2,672 / 2,487 ms |
| 教师 | 7,742 / 7,760 ms | 4,191 / 4,108 ms |

同一事务中的完整列表 RPC 对照：管理员全量待测评 4,336 → 1,969 ms，教师 6,958 → 3,630 ms。全量待首联分别为 4,329 → 1,863 ms、4,178 → 2,949 ms。当前工作列表延续上一批约 0.2～0.4 秒的数据库耗时。

教师全量档案仍有逐身份可见性判断的开销，本次保留权威权限实现。原私有全量事实包装器为保留旧合同仍计算全部操作标记，不能据此推断所有内部调用均提速。

上述为开发端读取测量，首次编译另有等待；未代替人工验收或生产测量。

## 检查与本地应用

- 首轮 8 个完整列表对照覆盖两类身份、当前/全量档案的待首联和待测评；正式脚本另覆盖 24 个列表场景，包括其他阶段、本人/分组/未分配范围、50/100 行、中英文、排序、组合筛选、未知选项和超界分页。完整 JSONB 相等。
- 两类身份、五个全量档案阶段的完整私有事实摘要均一致，包括全部字段、权限标记及阶段归类；匿名、学生、家长拒绝断言通过。
- 对照 public 函数目录、策略、表/视图权限和索引，仅目标函数正文与两个私有 helper 有预期变化。原函数签名、owner、ACL、security definer、search path 和 plan cache 配置保持。
- 12 张相关业务表完整摘要保持；事务回滚后目录和业务摘要恢复一致。
- `node scripts/student-list-page-work.mjs --check` 与 `--apply` 通过。本地应用前重新核对主机、实际 Supabase origin、监听进程、Docker 网络、数据库标识、迁移头和摘要，保存实际原函数定义，原子应用 DDL 与规范化 checksum 账本。
- 检查脚本 ESLint 与变更文本检查通过。没有应用代码或类型合同变化，使用数据库实际对照及 HTTP 页面检查覆盖本次风险。

详细计时、完整对照和回退原函数保留在本机受控临时目录，仓库仅记录无个人资料的摘要。本批是独立开发增量，生产发布另行执行目标核对和发布流程。
