# 正式课程初版 4:3 release 补齐 · 2026-09-08

> 状态：`DEVELOPMENT BACKFILL COMPLETE / INDEPENDENT POSTFLIGHT PASSED`。开发库新增 1,135 个初版 4:3 release；生产库只读盘点完成，无缺失初版，未执行生产写入。本记录不关闭 R1-Live Gate，不代表粗版已经通过人工视觉验收。

## 授权与范围

- 产品负责人明确要求绕开前端长时间生成，在开发库与生产库一次性补齐初版 4:3 release，并确认范围为“全部正式课程中尚无 4:3 release 的讲次”。已有发布版本保留。
- 生产只读 preflight：稳定数据库指纹为登记的 `10e3f97e…`，migration head 为 `20260907000500_aixuexi_incremental_source_snapshots`。1,309 个正式讲次均已有 4:3 release/head，缺失初版与有历史但缺 head 的讲次均为 0，因此本次生产写入为 0。
- 开发只读 preflight：本机 Windows、loopback Supabase、固定隔离数据库指纹与 Node/Docker 监听进程通过核对。目标为 1,135 个正式讲次、71,553 页；71,545 页缺少 4:3 页面版本，8 页已有草稿。另有 1 个旧格式教师微课来源测试讲次，排除且不清理。

## 执行合同

- 工具：[本机初版补齐脚本](../../../scripts/courseware-initial-4x3-local.mjs)，候选提交 `07077d64`，备份目录校验修正 `1a39f808`；只接受已登记的本机隔离目标，没有生产写通道。
- 复用编辑器的严格文档解析、A～F 映射和粗版物化器；没有分类的来源页采用既有默认策略。已有 4:3 草稿/当前版本不重建。此次不上传 Storage、不改变来源页或冻结课次。
- 使用现有且已启用的 `emergency_publish_cw_review`，记录产品负责人授权的一次性初版补齐理由与“粗版待视觉检查”发布说明；不伪造人工逐页审核通过。
- 每讲在独立事务内锁定讲次、按来源 revision 校验并只创建缺失草稿，然后发布且断言 release 编号为 1。发现已有 4:3 release 时跳过；失败停止，已完成讲次可保留并续跑。

## 写前备份与验证

- 样本讲次 69 页：自动生成 65 页，保留 4 页已有草稿，完整初版发布成功；事务回滚后 revision 数量、已有 release、native 页面头、冻结课次与 Storage 摘要均恢复。
- 最终备份：`.tmp/initial-courseware-4x3/before-2026-09-08T01-56-34-072Z.dump`，251,978,580 字节，`pg_restore --list` 校验 6,121 项。
- dump SHA-256：`3210b2268f55651138d29f5983eb32e427a7531f9216706ce7da6ed6a6a59aa9`；执行代码摘要：`b6f67ecbf283c4a9915da29525a8b559f1aed20f6f9c6bf1ee9e1c1bc0db7a51`。
- 备份包含开发库业务数据，仅保存在开发主机受控目录，不进入 Git；访问限该主机维护人员。保留至本次数据验收完成，至少 30 天；恢复需单独授权。
- 首次备份目录校验遇到 `pg_restore --list` 读取完成后提前关闭 stdin 导致的 EOF，执行停在业务写入前；修正后重新绑定执行清单、回滚演练和最终备份，旧备份保留。

## 最终核对

- 批处理于 `2026-09-08T02:14:37Z` 完成：1,135 / 1,135 讲，新增 71,545 页粗版，原有 8 页草稿内容保留并纳入发布；失败为 0，支持范围内剩余正式讲次为 0。
- 独立连接确认本批初版 release 为 1,135 个，快照覆盖 71,553 页，当前 track head 未指向本批 release 的数量为 0。
- 已有 native/4:3 release 快照、native 页面头、冻结课次内容与 Storage 对象计数的执行前后保护摘要完全一致。没有修改历史发布内容、16:9 草稿/当前头或冻结课堂。
- 独立来源盘点只剩 1 个已明确排除的旧格式测试讲次（2 页），不是正式课程缺失；该测试数据未删除或改写。
- 生产复核仍为 1,309 个正式讲次、缺失初版 0、历史 release 无 current head 0；生产业务、schema、Storage、服务和应用均无本轮写入。
- 当前样本讲次的中英文开发页面均返回 200，直接呈现编辑器与独立发布操作行，不再呈现自动生成等待入口。此为只读 HTTP 启动检查，实际画面仍待产品负责人验收。
- 开发库 postflight migration head 为 `20260908007200_student_stage_shared_query_facts`（其他业务任务的迁移；本批只写课件业务数据，没有新增 migration）。
- 结果摘要 `.tmp/initial-courseware-4x3/result.json` 的归一化 SHA-256：`e49a9452b4c9927db898238d26dacf871fe6db601da12220e9f47f46542070c1`。该摘要不含账号与课件内容；仓库保留本记录，受控开发目录保留执行清单与备份。
