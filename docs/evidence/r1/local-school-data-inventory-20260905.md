# 本地学辅数据来源盘点

盘点日期：2026-09-05。数据库统计来自 2026-09-05 15:08:47 开始导出的同一只读快照（北京时间）。其他开发任务可继续工作，本报告不代表后续实时数量。

状态：只读盘点完成；2026-09-05 已补录产品负责人对测试沟通、误升年级和漏导班级的确认。未清理业务数据、未运行导入、未修改业务代码、未接触生产。分类用于决定保留与复核范围，不构成删除清单。

## 1. 本次交付

- 原始资料位于 `docs/test_material/`：9 个文件，7 份 Excel 共 20 张工作表，另有 1 份飞书 `.base`、1 张图片。
- 9 个源文件已另存本地 ZIP 快照，并核对文件 SHA-256 与 ZIP 完整性，后续源文件变动不影响本轮保留版本。
- 本地数据库逻辑备份已保存，大小 230,676,377 字节（约 220.0 MiB）；备份和盘点查询共用同一 PostgreSQL 导出快照。
- 数据库 migration ledger 共 286 条；按版本排序最高为 `20260905000900_renewal_health_policy`。这里只描述本地快照。
- 导入账本共 16 批：7 批完成、9 批预览。9 个登记了源文件名的批次引用都已找到对应文件，5 个不同源文件的字节 SHA-256 全部相符。
- 人工可读的待确认明细、字段差异、来源 ID 映射和备份保存在本次 `.tmp/local-school-data-inventory-20260905-150632/` 忽略目录中；本报告只保存匿名汇总。

## 2. 源文件

| 文件 | 实测格式 | 内容规模 |
| --- | --- | --- |
| white-board.png | PNG | 图片素材 |
| 【思维】用户与产品运营表.base | JSON container | 19 张独立表，5,144 条记录（4,928 条有内容） |
| 员工管理.xlsx | OOXML ZIP | sheetName：52 条数据行 |
| 子2：报名学员信息与2024课表【数据系列2】.xlsx | OOXML ZIP | 14 张工作表；含横向名单、报名、暂课、退班历史 |
| 学生管理-学生列表.xlsx | OOXML ZIP | Sheet1：1375 条数据行 |
| 班级列表导出数据.xls | OLE BIFF XLS | bill：132 条数据行 |
| 班级列表导出数据.xlsx | OOXML ZIP | bill：132 条数据行 |
| 格致思维——市场活动-202692164614.xlsx | OOXML ZIP | Worksheet：1071 条数据行 |
| 格致思维教育预约信息登记表-202692095828.xlsx | OOXML ZIP | Worksheet：157 条数据行 |

说明：两份班级 `.xls`/`.xlsx` 的 133×23 个单元格逐格相同，按同一份来源处理。复合历史工作簿存在一行多个孩子的横向名单，因此网格行数不能作为学员人数。

历史工作簿的“学员报名信息”有 346 行姓名、343 行标准手机号；“4退班学员表”有 40 条非空网格行，另有暂课与多个往期名单页。本地完成的名单导入只登记了“26年暑秋在读学员”，不能据此宣称整本历史工作簿已经迁入。

源文件读取兼容性：部分 OOXML 的范围错误标为 A1，且有空页边距；本次依据实际 XML 单元格读取。复合工作簿有 511 个公式单元格且均无缓存值，本次未将这些未计算的派生计数当作业务事实，也未修改源文件。

## 3. 已完成的导入与待处理预览

| 来源 | 账本状态 | 核对结果 |
| --- | --- | --- |
| 魔法校学生 | 1,375 行已完成 | 1,375 个学生目标均存在；全部与源外部编号对应 |
| 魔法校班级 | 123 行已完成 | 源 132 条；4 个体验课和另 5 个正式班未在本批导入 |
| 魔法校秋季名单 | 163 行已完成 | 89 行关联已有学生、74 行建立学生；27 个来源班级映射 |
| 小地推第一份 | 1,071 行已完成 | 保留 1,071 条来源行与 1,140 个意向选项 |
| 小地推第二份 | 157 行仅预览 | 有两次预览，不能累计为 314 条已导入；每批 8 条匹配待判断 |
| 员工 | 11＋1＋52 行已完成批次 | 52 行批次目标均存在、姓名均与员工源相符；历史 11 行批次目标已不在 profiles |

魔法校学生/班级导入完成后原行 payload 已清空，但外部来源键到目标的账本仍保留，源文件也完整存在。名单批次仍保留 163 行选择结果及来源单元格；这部分应作为未来可重放迁移材料保全。

小地推的匹配决定表当前没有 `decided_by` 的显式人工决定；不要把自动匹配建议记成人工复核通过。班级来源映射 27 条存在，`setup_completed_at` 均为空，该标记只说明未登记设置完成，不代表来源映射丢失。

员工批次没有记录文件名或文件 hash。52 人批次的目标和源姓名逐一对应，但仅 33 个手机号键与文件中可抽取的 48 个手机号相符，需结合脱敏、替换或手工录入过程核对。历史完成的 11 行员工批次，其 11 个目标不在当前 profiles，可能涉及开发重建或替换；保留账本，不将它直接复用为生产员工映射。

## 4. 本地数据分类

| 对象 | 总数 | 外部来源可追溯 | 明确测试记录或其依赖 | 来源/操作真实性待确认 |
| --- | --- | --- | --- | --- |
| 学生档案 | 1515 | 1449 | 59 | 7 |
| 线索 | 1074 | 1071 | 2 | 1 |
| 班级 | 150 | 136 | 9 | 5 |
| 班级名单关系 | 207 | 163 | 44 | 0 |
| 线索沟通 | 30 | 0 | 30 | 0 |
| 学员跟进 | 29 | 0 | 24 | 5 |
| 活动参与 | 20 | 0 | 8 | 12 |
| 测评汇总 | 4 | 0 | 0 | 4 |
| 课程意向 | 57 | 0 | 57 | 0 |
| 课程报名 | 49 | 0 | 49 | 0 |
| 续班资格 | 8 | 0 | 8 | 0 |
| 家庭 | 2 | 0 | 2 | 0 |
| 联系人 | 2 | 0 | 2 | 0 |

“外部来源可追溯”只证明来源关系，不能证明全部为正式业务。源学生里已有 2 条测试类名称，源班级里有 5 条备课类名称且都在导入范围，需另行确认用途。

三批证据充分的虚构学生共 59 名：`P5-DEMO-20260905` 11 名、`DEMO-ENROLLMENT-20260905` 12 名、`ROSTER-ACCEPTANCE-20260905` 36 名。部分验收姓名没有测试前缀，部分测试班 `purpose=production`；本次按来源、创建脚本和精确关联分类。

样例引用的既有课程、学期、教师属于共享基础，不能随样例一起归入清理范围。其余缺少精确来源的对象保留待确认，不按名称、环境或 purpose 推定可删除。

## 5. 确认结论与后续处理范围

1. **21 条外部来源线索上的 30 次电话沟通**：17 次未接通、6 次无效号码、4 次接通、3 次拒绝；备注均为空。产品负责人已确认全部属于测试操作，迁移时排除这 30 条操作事实，保留其关联的来源线索。相同线索上的 5 条活动参与、15 条活动环节记录仍需分别核对来源；不能仅凭对象重叠就判为测试。因测试沟通而改变的线索状态、下一动作和邀约状态需追溯影响范围后单独处理。
2. **31 名学员的年级变化**：重用当前学生导入解析器比对 1,375 名学生，全部映射存在；14 类来源字段中仅年级有差异，31 人均增加一级。产品负责人确认属于历史学年切换误操作。只读快照进一步证实这 31 人的当前学年均有 `source=promotion` 且等于误升后值的记录，当前学期的年级也与误升后值一致；上一学年的 31 条记录均与源文件年级一致。修正候选范围为 31 个学生当前年级、31 条对应学年年级、31 条对应学期年级，按精确来源键及目标 ID 复核后恢复源基线，并保留修正前值。源年级用于撤销此次已确认误操作，不据此推定其他年份的年级。当前 grade 更新触发器会写入执行时的当前学年/学期，实际修正前须重读周期和记录状态。其余导入字段、上一学年与原有学期历史继续保留。
3. **5 个源正式班未进入已完成班级批次**：3 个短期班、2 个长期班，源状态均为开课中。产品负责人已确认并非有意排除。离线重放现有解析器读到 132 行，其中 128 行为支持的正式班；5 个漏导行均缺少可匹配的学期，128 行扣除这 5 行后与已完成批次的 123 个来源键完全一致。批次本身只收到 123 行且全部成功，因此缺口发生在提交前；“选择已就绪行”与该结果一致，但实际点击无法从已清空的 payload 还原。5 行均可匹配唯一 active 员工，快照中没有同规范名班级；课程、校区与教室目录未纳入本次 JSON 快照，映射可用性仍需复核。
4. **163 行名单归属选择及 27 个班级来源映射**：其中 74 行创建学生，89 行关联已有学生；这是可以保留和转成跨环境映射的已保存结果。
5. **当前工作状态单独交接**：外部来源线索关联 200 个下一动作，其中 179 个 open、21 个 completed。外部来源班级关联 105 个课次均未开始、未结束、未冻结课件、未完成课后；按排期和配置复核，不视作已发生课堂历史。

漏导班级的学期细节：2 个长期班由 2024 年 9 月开课日和秋季推导为 2024 学年秋季；3 个短期班源字段写“春季”，开课日却在 2024 年 11 月，现有算法机械推导为 2023 学年春季。本地只有 2025/2026 学年，5 行均无法匹配。2023 是算法结果，尚不能作为真实业务学年；先复核这 3 行的历史学期，再建立历史周期映射并准备独立 5 行补导。另 4 个体验课按现有类型规则保留在活动资料中。补导保留原 123 行账本，重新核对源外部 ID 与目标映射，按已确认历史状态承接，不根据汇总表虚构课次、考勤或名单。

代码依据：`src/features/school/mofaxiao-class-import.ts` 的 `inferMofaxiaoSchoolYearStart`、`src/features/school/MofaxiaoClassImportPanel.tsx` 的学期解析与已选行提交逻辑；导入前提交 `a97d6873` 的相关逻辑与当前行为一致。逐行诊断保存在本地 `class-import-gap-diagnosis.json`。

## 6. 历史沟通资料所在位置

飞书 `.base` 的 20 份快照中有 1 份完全重复；去重后 19 张表、334 字段、52 视图、5,144 记录，其中 4,928 条有内容。快照未包含完整修改历史，已被覆盖的旧沟通不能据此恢复。

获客表有 2,578 条确认信息备注、666 条跟进信息；到访表有 550 条学员情况、539 条家长情况，其他家长/学员补充字段也需保留。它们不是相同口径的独立沟通次数，迁移时按来源片段保存与复核。

魔法校学生表及两份小地推 Excel 的备注字段均为空；当前库中的 30 条线索沟通也没有备注。因此，第一批历史文字查询的核心资料仍是飞书文件与复合历史工作簿。本地没有 `feishu` 完成导入批次，不能把现有名单导入视为历史跟进迁移完成。

飞书两列关联指向缺失目标表，9 个附件字段无有效附件条目；原始值需保留。无日期、年份不完整、作者未匹配的内容以历史摘要承接，后续结构化时继续追溯来源。

## 7. 核对与边界

- 盘点前核对了本机执行主机、loopback Supabase origin、Docker Desktop endpoint、监听进程及数据库指纹。
- 备份 SHA-256：`8e6c8dba6abef1dff68f1ec13d75949c577e1144998723c4fca79b4c3477f624`。已成功读取 custom archive 的 TOC；没有做恢复演练，未备份 Storage 文件或 Docker 卷，所以不将其表述为全系统恢复验证。
- 本次检查 71 条外键引用路径、8,470 个非空引用，发现 0 个缺失；另核对 201 次跨表身份对应，发现 0 个不一致。这不包含多态导入账本 target_id；历史员工目标缺失已另列。
- 本轮不运行生产导入、删除、purge、reset、迁移或服务重启，不关闭任何产品阶段或正式 Gate。

## 8. 下一增量

保全本次快照、原始文件和已保存的归属选择。已确认的 30 条测试沟通进入迁移排除项，31 名学生进入精确年级修正候选，5 个正式班进入漏导排查与补导候选。上述确认及精确对象范围另存本地 `user-confirmed-decisions.json`，作为只读盘点快照上的决定补充；尚未执行数据库修正、删除或补导。开发工作可并行建设飞书历史资料的来源存档、全量查询及归属复核。正式迁入使用来源标识重新解析生产目标 ID，继续排除明确的虚构验收记录。
