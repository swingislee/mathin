# 管理员总览加载回退 · 2026-09-14

状态：**已获本次授权并部署生产，机器 postflight 通过，待人工体验验收**。用户反馈生产管理员数据总览加载约 10 秒，并追加线索页、学生及学服工作台全部子路由的性能排查。本批保持 R1-Live-2 和 Gate 状态；本页记录已获批准的一条读取策略修复，追加页面排查单独记录。

## 定位

生产当前应用为 `20260913-155934 / 3d9892bb87c2361e6662e685b606ac77a3e82e29`，数据库账本 `382 / 20260913000200_school_source_collaboration_alignment`。只读检查确认应用 active、NRestarts=0，loopback health 200，约 24 ms。

生产先于协作功能部署了 `20260910003000_dashboard_read_paths`。0.1.4 后补协作迁移 `20260910001000_school_subject_collaboration`，把 `leads_select_pool_scope` 改回逐条调用 `can_view_school_lead`，覆盖先前优化。开发库依时间编号执行，保留优化策略；两者的权限语义一致，执行成本不同。

使用现有管理员的数据库权限上下文，在 `REPEATABLE READ READ ONLY` 事务内测量当前页面所用查询，仅输出耗时、行数和大小：

| 生产读取 | 数据库耗时 |
| --- | --- |
| 工作项列表 | 74 ms |
| 线索首批 1,000 行 | 632 ms |
| 当前线索 | 3,645 ms |
| 沟通记录首批 1,000 行 | 2,304 ms |
| 来源记录首批 1,000 行 | 2,360 ms |
| 活动报名首批 | 56 ms |
| 课程报名首批 | 20 ms |

这些是查询耗时，部分请求并行，不能直接相加作为完整页面时间。本次生产未获取或伪造用户登录会话，10 秒是用户实际观察。

## 修复与验证

新增 `20260914001000_dashboard_collaboration_read_path`，恢复兼容当前协作合同的账号级直接可见分支。其他逐条参与、分组和学生关联条件继续调用原权限函数。迁移核对原策略与两个权限函数的完整已知定义，遇到漂移即停止；已优化的开发库也可应用。原账本和旧迁移保持。

LF SHA-256：`0c97963481cdc3f818b9ac75df177180f12bd1947afff45b17d788df6a3bd6bd`。

在已核对的本机 loopback Supabase，`scripts/dashboard-collaboration-read-path.mjs --check` 先复现生产策略覆盖，再应用补丁。11 个固定开发身份与匿名的 48 项完整可见行数及身份摘要一致；停用和待改密状态的原、新策略逐行结果一致；重复应用保持原表达式。独立连接确认回滚后函数、策略、账本及业务内容摘要零残留。随后 `--apply` 登记迁移，业务摘要保持。

开发管理员完整可见行读取对照：线索约 735 → 3 ms、沟通约 727 → 24 ms、来源约 698 → 2 ms、当前线索约 1,481 → 626 ms。正式权限、协作范围、业务内容和页面结构保持。

固定开发管理员访问数据总览，页面均为 200 且包含大屏正文。首次请求含开发编译约 5.74 秒；预热后的完整 HTTP 响应为 1.59、1.49 秒，首字节为 251、236 ms。这是开发端响应测量，生产完整页面仍需发布后验收。

第一轮诊断误将全部 64 个本地档案纳入读取对照，耗时超出必要范围；已取消该事务并独立确认原策略和账本恢复，再以固定开发身份完成上述检查。期间一次并行开发登录受验证事务锁影响返回 504；正式 HTTP 测量在事务结束后完成。

## 交付

补丁只变更一条 SELECT 策略，无需重建或重启应用。生产执行前按[写目标策略](../../runbooks/r1-write-target-policy.md)和[发布手册 §7](../../runbooks/r1-production-deployment-preflight.md#7-函数rpc-热修执行补充)冻结候选、核验备份、演练回滚零残留，再提交并执行生产只读核对。

### 生产结果

用户明确回复“现在发布生产”。冻结候选 `219eb3e190f498222abdd612ad682dde96571378`，私有 bundle 与 LF 迁移上传本轮专用 staging；正式提交时间 `2026-09-14T06:04:49.58488Z`。应用仍为 `3d9892bb…`、原进程，数据库账本由 382 增至 383，新增版本及 checksum 与候选一致。

写前备份：`mathin-db-prechange-20260914T055829Z-dashboard-collaboration-219eb3e190f4`，350,657,950 bytes，TOC 6,511 项；SHA-256 `c26680608e666f486ff8dd02208924644af08ba3be9491d430eced7025ad3387`。备份位于生产受控 backups 目录，摘要及 TOC 已核验；本次不恢复数据库、不写 Storage、不重启应用。

生产 `SERIALIZABLE` 回滚演练与正式事务均核对 11 类既有权限上下文、18 项可见 ID/DTO 摘要与匿名拒绝，前后结果一致；39 张业务表完整摘要和 4 项受保护对象计数保持，除批准的 SELECT 策略外，函数、ACL、策略、表权限和索引目录保持。独立连接确认演练回滚零残留后才正式提交。

| 管理员生产读取（同一回滚演练） | 修复前 | 修复后 |
| --- | --- | --- |
| 线索首批 | 586 ms | 10 ms |
| 沟通首批 | 2,363 ms | 75 ms |
| 来源首批 | 2,290 ms | 7 ms |
| 当前线索可见 ID | 4,081 ms | 1,672 ms |
| 协作线索可见 ID | 4,044 ms | 1,755 ms |

正式提交后，发布脚本把含 `applied_at` 的账本整行与仅含 version/checksum 的对象比较，导致回执校验报错。SQL 已完成 COMMIT，未重复迁移；独立只读快照重新确认账本、checksum、策略分支、业务及其他权限目录正确，恢复发布回执。正式事务的计时 JSON 未保留，上表仅引用已保留的回滚演练计时。

独立数据库 postflight 无新增业务变化和错误；运行时 31 项健康与 zh/en 匿名入口检查通过，应用 PID 保持，loopback health 6 ms，服务错误日志增量为 0。上述结果证明发布合同，不代表管理员生产完整页面耗时或人工体验已通过。

受控证据：`/home/swing/services/mathin/staging/dashboard-collaboration-219eb3e190f4-20260914/evidence.tar`，184,320 bytes，SHA-256 `73e1e77cfd91b2c70811c67f61eedcda6537402c2e89ec462f4c918f51f4a3bd`。文件清单摘要校验通过，owner-only，访问角色为生产运维维护者，保留到 `v1.0.0 + 365 天`。

生产验收：[管理员数据总览](https://mathin.club/zh/dashboard?view=overview)。开发对照：[管理员数据总览](http://192.168.5.213:3130/zh/dashboard?view=overview)。
