# 学服五表字段与分页开发交付（2026-09-07）

状态：开发端已交付，待人工视觉与业务验收。本记录是独立增量说明，不关闭阶段或正式 Gate。

## 本次范围

- 测评默认按 Supabase `assessment_workbench_read_order` 的 `assessment_at desc nulls last, id` 读取。邀约、报名、公开课环节统一成只读行键，DTO 按这些键组合，不再执行同一默认排序，也不按当前/历史记录重新分组。
- 线索、沟通、排班、续班接入同一字段查询合同：文本搜索、实体 ID、多选 OR / 跨字段 AND、日期范围、数值范围及空值。
- 排班的班级、教师、星期和起止时间命中同一班级/课次；姓名与电话命中同一学生；沟通记录的方式、结果和备注命中同一有效事件。
- 五表共用默认 50 条/页及 20/50/100 选项。切换条件、调整页数及结果缩小时回到有效页；共用紧凑分页栏，缩小表格底部留白。
- 已有服务端分页的线索/沟通先在权限与业务范围内完成字段筛选、候选、计数，再返回一页 DTO。排班、续班、测评继续复用既有完整工作集，在筛选后分页显示；本次不宣称这三页已经改成 Supabase 仅取 50 条。
- 保存、继续下一位、隐藏选择、排班已占座位和历史记录写保护沿用现有业务合同；本次没有造数或生产操作。

## 本地数据库增量

- 迁移：`20260907001200_assessment_workbench_read_order.sql`。
- `security_invoker = true`，仅向 authenticated 授予 SELECT，沿用底层 RLS；没有基础业务表 DML。
- 已按 `scripts/assessment-read-order.mjs` 完成本机隔离环境 preflight、事务内权限/行键检查及 rollback 后检查，再应用迁移和本地迁移账本。
- 使用固定开发身份验证校长、教师、学生可见范围；原三类业务表行数未改变。
- 生产环境尚未应用该迁移；后续部署按生产目标策略单独授权与 preflight。

## 机器检查

- 定向测试覆盖新字段/分页合同及真实服务端读取器、共享菜单、五表关键交互：跨 50 条边界、筛选前计数、权限范围候选、日期/季节/数值排序、同关联事实匹配、筛选切换与保存后行保留。
- 本次变更文件 ESLint 通过，`git diff --check` 通过。
- 全仓 `tsc --noEmit` 仍报告 `tests/autumn-identity-review.test.ts` 和 `tests/source-reconciliation.test.ts` 的既有类型错误；本次应用代码和新增测试没有类型错误，未修改这两份其他任务文件。
- 按 verify 技能复用现有开发服务，使用固定校长/教师账号检查中英文页面启动、主筛选与默认 50 条分页。校长覆盖五页，教师覆盖其可访问的四页，全部 HTTP 200。
- 没有运行浏览器控制、Playwright 或截图，也没有把机器检查当作人工验收或正式 Gate。

## 人工验收重点

1. 测评默认从最新测评时间向前排列；同时间顺序稳定，未知日期在最后。
2. 五页表头按真实字段组合条件，跨页筛选能命中原第二页以后的记录。
3. 分页栏高度和底部留白符合预期，默认 50 条；筛选、换页及 20/100 条切换顺畅。
4. 当前行保存、继续下一位、批量分配及排班占座仍符合实际业务使用习惯。
