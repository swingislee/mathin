# 2026-09-08 工作区全量快速内部发布

> 状态：**已部署；必要机器检查通过；待内部业务验收**。本次为公司内部 14:30 使用前的快速发布，不关闭 R1-Live Gate 2 或 Production 1.0 Gate。产品负责人本次授权完整工作区上线，随后明确要求跳过新备份和迁移回滚演练；执行按该范围完成。

| 发布事实 | 结果 |
| --- | --- |
| 时间 | 2026-09-08 13:25 开始核对；13:46（Asia/Shanghai）必要 postflight 已完成 |
| 应用提交 | `3dca80786ed25956d0750181ca957b81867d07ce`，包含开始时两个已修改文件、一个未跟踪方案文档及发布所需的类型修复 |
| current | `20260908-053849`；构建完成时间 `2026-09-08T05:40:53Z` |
| previous | `20260904-103900` / `c913ea2f63353b8647c1b83e66d83be5e7c6dc91` |
| 数据库 | ledger `261 → 356`；95 条未应用迁移正式提交；既有 migration 校验摘要零差异 |
| 数据库 head | `20260908140000_student_record_unassigned_access` |
| 生产目标 | `xiaomi`；应用 `https://mathin.club`，Supabase `https://supabase.mathin.club`；应用监听 `127.0.0.1:3131` |
| 数据库指纹 | `10e3f97e32b018403c9074efa4e258d699530a487c47de89b5d307ab7ff21a0c` |
| 备份、迁移演练 | 按本次用户明确指令跳过；没有执行新备份、恢复或迁移回滚演练 |

## 执行范围与数据保护

按[生产写目标核对规则](../../runbooks/r1-write-target-policy.md)完成主机、实际 origin、监听进程、数据库指纹、current/previous、磁盘和迁移账本核对。写前未来两小时已排课次为 0。

完整 Git archive 在生产独立目录构建。初次构建在 TypeScript 阶段达到默认 2 GiB 堆上限；核对主机可用内存后，将本次构建进程的 `NODE_OPTIONS` 设为 `--max-old-space-size=4096`，重试通过。该设置仅用于构建命令；373 个页面生成完成，再封装独立 release。

95 条迁移使用原文件内容及 LF 归一化 SHA-256，移除文件外层事务后置于同一次正式事务。事务包含目标与原账本守卫、5 秒锁等待上限、正式对象计数及原字段摘要核对。生产写前不存在五张历史试验表；这五张表在本批先创建，后在确认总行数仍为 0 时按原迁移退役。执行没有删除既有正式记录，也没有复制开发身份或开发数据库内容。

年级名称文字按迁移统一为阿拉伯数字。正式身份、学生、报名、点名等原字段摘要保持一致；班级与课次摘要仅排除本批明确规范化的名称/标题及对应更新时间。课件内容与 Storage 未执行批量导入或适配。

| 计数 | 发布前后 |
| --- | --- |
| Auth 用户 / profiles | `14 / 14` |
| 学生 / 班级 / 课次 | `10 / 5 / 38` |
| 报名 / 点名 | `11 / 0` |
| 页面 / revision / release | `77282 / 121792 / 3774` |
| Storage 对象 | `126534` |

迁移成功提交后，原子切换应用并重启 `mathin.service`。服务为 active/running，`NRestarts=0`、`ExecMainStatus=0`。上一应用 release 已保存为 previous；本次没有执行回切。

## 必要检查与已知验证缺口

- 本机类型检查、受影响文件 ESLint、密钥扫描通过；密钥扫描覆盖 2688 个已跟踪文件，高置信命中为 0。
- 正式生产构建通过。10 个定向测试文件的 93 项合同通过，覆盖登录身份、正式对象保护、生产写目标、课堂同步及本次类型修复相关逻辑。
- 另运行的一次全量现有单测**未全绿**：20 个文件 / 34 项失败，295 个文件通过、1 个文件跳过；2237 项通过、4 项跳过。失败包含旧 UI 源码断言、历史规划/fixture 合同，以及测评测试仍 mock `activities` 等原表、实现已读取 `business_activities` 等视图的不匹配。本批未逐项修复或将全量检查登记为通过。
- 生产 loopback、Caddy 与公司开发主机访问实际域名的健康探针均正常；zh/en 登录 HTTP 200，手机号旧入口规范化到统一登录页。学生、学服、测评、活动、课件、月目标及 zh/en 后台入口匿名请求均按预期重定向登录。
- 只读事务以现有正式管理员的数据库授权上下文执行五个学生阶段列表、分班读取和业务视图，全部成功；该检查没有建立浏览器登录会话，也没有模拟人工保存。六个新增读取/写入 RPC 的匿名执行权限核对为全部拒绝，关键新表 RLS 开启。
- 服务 journal 错误增量为 0；`operational_errors` 仍为 1962，最新时间仍为 `2026-09-04T10:14:49.338Z`。

实际员工登录、数据登记、业务闭环与视觉效果继续由公司内部使用者验收。快速发布结果不替代这些产品结论。

## 可复核产物

生产受控目录：`/home/swing/services/mathin/staging/quick-20260908-3dca8078/`。源包、构建日志、正式 migration 文件及其执行日志留存生产主机；访问范围为生产运维角色，保留期按 R1 发布证据政策执行。以下为发布产物的原始字节 SHA-256；各 migration 内容摘要另按仓库 LF 归一化规则登记。

| 产物 | SHA-256 |
| --- | --- |
| `source-3dca8078.tar` | `bd730433bbcebce5e79d8805c887fde8478396a81ba5a67078d00cc650060df1` |
| `migration-plan.json` | `c958bfaf46f8312ded059f2d47e4e6f275452a8babc2586ff88654acf7c87f55` |
| `apply-migrations.sql` | `f8cc48435409c78fdda6e47971d8355d1179fd5f150a84554dcb580be05e99fd` |

生产入口：[后台](https://mathin.club/zh/dashboard)、[学生](https://mathin.club/zh/dashboard/students)、[学服工作台](https://mathin.club/zh/dashboard/followups)。
