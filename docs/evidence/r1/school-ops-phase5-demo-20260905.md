# Phase 5 续报与长期运营：本机验收样例

日期：2026-09-05。状态：开发库种子数据已保存，账号读取与页面响应检查通过，待产品负责人手工验收；未关闭 Phase 5 或正式 Gate。

## 入口与初始数据

使用本地固定校长开发账号。所有虚构人物名称以 `P5验收` 开头，学生来源为 `P5-DEMO-20260905`。

| 入口（接开发端 origin） | 用途 |
| --- | --- |
| `/zh/dashboard/renewals?cycle=c98548d1-b55d-49a1-a613-e9b6f197b8ae` | 选择「P5验收 · 暑期衔接→秋季续报」，先将视图切换为「全部」 |
| `/zh/dashboard/renewals/signals` | 教师建议默认展示待处理；切换「全部」可查看已采纳样例 |
| `/zh/dashboard/renewals/growth` | 重新报读与转介绍；搜索 `P5验收` |
| `/zh/dashboard/opportunities` | 课程意向与正式报名衔接；搜索对应样例姓名 |
| `/zh/dashboard/enrollments` | 查看已报名样例的商业报名记录与入班衔接 |

初始包含 11 名学生、2 个测试班、10 条班级成员关系、8 条周期资格、6 条续报意向、1 条加报意向、1 条重新报读意向、1 条转介绍意向、5 条教师建议和2条转介绍来源。另有一个尚未确认学生身份的转介绍 Lead。两个测试班复用既有课程与学期引用。

## 按顺序人工验收

勾选表示产品负责人实际操作通过。以下数量是开始操作前的基准，保存操作后会随业务变化。

| 完成 | 样例 / 入口 | 操作 | 通过标准 |
| --- | --- | --- | --- |
| [ ] | 续报池，01 林知夏、02 陈星禾 | 勾选两名待准备学生，填写负责人、下一步与时间，批量准备续报 | 待准备人数从 2 变为 0；续报意向从 6 变为 8；刷新仍保留，学生身份保持原有记录 |
| [ ] | 续报池，03 许一诺、04 周予安 | 对比「规划中」与「考虑中」；进入04详情，修改下一步和时间并保存 | 姓名、来源班级、周期、课程、学期对应正确；返回列表并刷新后保留修改 |
| [ ] | 续报池，05 苏沐言 | 查看「待支付确认」；在课程意向入口找到同一学生并完成模拟报名确认 | 原意向变为已报名；报名台账出现对应记录，继续使用同一学生 |
| [ ] | 续报池，06 沈书宁 | 在「全部」查看已报名对照；在报名台账搜索姓名 | 已存在报名记录；续报详情显示已报名且进度编辑锁定，入班状态可与商业报名状态分别识别 |
| [ ] | 续报池，07 顾景行、08 唐可欣 | 对比「未报名」与「长期培育」，阅读备注 | 能区分本轮接送冲突与长期待定；记录仍可追溯，未被当成删除学生 |
| [ ] | 教师建议，03 许一诺 | 查看来源教师、班级与建议，选择采纳并填写课程、学期、负责人和下一步 | 建议变为已采纳，可打开关联意向；同课程、同学期已有续报意向得到复用 |
| [ ] | 教师建议，04 周予安、07 顾景行 | 查看加报建议和流失风险；任选一条作出处理 | 能读懂专业建议与来源，处理结果保留，刷新后状态一致 |
| [ ] | 教师建议，01 林知夏 | 切换「全部」并打开已采纳样例的课程意向 | 关联类型为加报，能追溯同一学生；与01待准备的续报资格可分别识别 |
| [ ] | 重新报读，09 陆思远 | 新建重新报读意向，选择该历史学生、目标课程与学期，填写负责人和下一步 | 创建后进入重新报读列表，沿用既有历史学生身份 |
| [ ] | 重新报读，10 宋小满 | 打开已有「考虑中」意向并更新下一步 | 无需先创建数据即可查看完整详情；保存与刷新一致 |
| [ ] | 转介绍，12 待确认朋友 | 查看推荐学生01、推荐家长与关系；打开 Lead | 尚无课程意向；可继续分配负责人及确认身份的前置流程 |
| [ ] | 转介绍，11 江晨曦 | 打开已建立的课程意向，与12号对照 | 可见已确认身份及关联的转介绍意向，推荐来源关系保留 |

09号同时有待处理的重新报读教师建议。建议先完成手动新建，再采纳相同目标的建议，观察是否复用原意向。

## 已完成的机器检查

- 事务预演成功后才保存；SQL 断言学生、班级和成员关系增量分别为 11、2、10。
- 固定校长账号读取到 11 名学生、8 条周期资格（其中2条待准备）、9 条长期运营意向、5 条教师建议和2条转介绍。
- 固定教师账号能读取自己提交的5条专业建议。
- 续报池、教师建议、重新报读/转介绍、05号意向详情4个页面返回 HTTP 200，响应包含对应样例，未发现 schema cache 或服务端渲染错误。
- 以上为数据与页面响应检查；视觉、交互与业务流程等待人工勾选。

## 维护与复跑

入口脚本为 `scripts/seed-school-ops-phase5-demo.mjs`，SQL 为 `supabase/fixtures/school_ops_phase5_demo.sql`。先完成本地写入目标核对，再用当前只读 preflight 得到的 PostgreSQL system identifier 替换下方占位符：

```powershell
node --experimental-strip-types scripts/seed-school-ops-phase5-demo.mjs --expected-system-id=<当前本机数据库标识>
node --experimental-strip-types scripts/seed-school-ops-phase5-demo.mjs --expected-system-id=<当前本机数据库标识> --apply
```

默认事务回滚，`--apply` 才保存。脚本限制隔离的本机 Supabase origin、Docker Desktop、Compose 项目及目录、网关端口和数据库标识。使用既有固定开发身份，不创建认证账号。已有样例周期时复跑保留用户操作结果；发现半套样例时停止以便检查。未提供清空或重置命令。

本次运行清单位于忽略目录 `.tmp/school-ops-phase5-demo/manifest.json`，账号/API/页面检查结果位于同目录 `verify-results.json`。当前周期链接来自本次运行；在其他开发库运行后使用新 manifest 中的周期 ID。
