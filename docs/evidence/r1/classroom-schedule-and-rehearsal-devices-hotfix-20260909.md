# 非排课时间提醒与试讲设备联动 · 2026-09-09

状态：开发端已交付，待人工视觉与设备验收；本次没有生产操作，R1-Live 阶段和 Gate 保持原状态。

## 交付行为

- 正式课、微课与公开课共用开课提醒。点击首次正式开课时，以排课开始时间和时长判断；公开课接受任一授课环节的有效时间窗口。时间缺失、尚未开始或已经结束时，弹窗显示排课时间，并提供「进入试讲」「仍然正式开课」「取消」。已结束课次重新开课也使用同一提醒。
- 试讲仍先进入候课。「连接试讲设备」提供同一试讲的链接与只读展示窗，同一教师账号可在多台设备操作。课次试讲和公开课试讲复用课堂翻页、板书及已注册的互动/媒体协议；未注册同步协议的组件继续遵守课堂只读合同。
- 试讲频道按教师账号、资源类型和资源 ID 隔离。事件与板书快照用于临时回放，正式课次、点名、课堂 outbox 和业务记录保持独立。逻辑时钟与设备 ID 统一并发事件顺序；各对象只保留最新完整快照，语义命令保留顺序。新窗口使用独立设备身份，晚加入与重新连接时请求回放。

## 机器检查

- `pnpm typecheck`、本任务 TS/TSX 的定向 ESLint、`pnpm messages:check` 通过。
- 排课边界（含跨日和时区）、试讲路由、双向事件、并发收敛、晚加入、快照压缩、媒体回放防回环，以及既有候课、板书、课件、工具与正式课堂连续性定向 Vitest 通过；课堂 interaction sync audit 通过。
- 固定开发教师账号的已有课次试讲候课页通过带登录 Cookie 的 HTTP 200 / 候课组件启动检查。
- 使用固定开发账号和独立 Realtime 客户端，实际验证 `session` / `activity` 两类私有频道：同账号广播送达，另一账号订阅被拒绝。

## 本机数据库策略

已核对 Windows 开发主机、应用 Supabase 回环 origin、Docker 网关监听进程和本机 Docker endpoint；未使用 SSH。只应用：

`20260909002000_classroom_rehearsal_channels.sql`

规范化 SHA-256：`2d6219acda2331f1330f508b258ba992493618348b7c23b70a5f733a020926c8`。

迁移新增两条 authenticated 私有广播策略。先在事务内执行迁移和权限断言，再完整回滚并确认策略为零；随后单独提交本迁移与 ledger。提交后再次执行 [权限断言](../../../supabase/tests/classroom_rehearsal_channels.sql)：本人收发通过、其他账号和匿名被拒绝、畸形频道和未授权 presence 被拒绝；广播探针全部回滚，最终仅保留预期两条策略。未创建身份、课次或活动数据。

## 人工验收边界

- 本机当前没有未删除的公开课活动，因此公开课只完成代码、合同和实际私有广播检查，尚未完成带业务数据的页面验收。
- 本机开发网关仅监听回环地址；平板等其他物理设备需要可达的开发网关，才能执行初次自动配对。此次没有调整网关绑定、系统网络或生产配置。真实跨设备 WebRTC、断网保持、视觉和操作手感仍待验收；本机双窗及私有广播通过不替代这些结果。
- 验收入口：[正式课／微课](http://192.168.5.213:3130/zh/dashboard/classes) · [公开课](http://192.168.5.213:3130/zh/dashboard/activities)。在非排课时间点击正式开课检查弹窗，再进入试讲检查候课与连接入口。
