# 教学班级概览 · 2026-09-15

状态：**开发端已交付，待人工视觉与交互验收**。本批未部署生产，R1-Live Gate 状态保持原样。

## 可验收增量

- 入口：[教学工作台](http://192.168.5.213:3130/zh/dashboard/teaching?view=records)。
- 按所选周／月每班一行，班级名称约占表格五分之一；同行呈现学生人数、已有记录课次、系统下课数、到课与缺迟假、学情进度、课评人次、需关注学生及最近评语和沟通正文。
- 可按老师、班级和需关注学生筛选；默认按需关注人数、已有记录课次数排序。班级可展开课次摘要，继续进入原实际记录详情，返回保留日期、老师和班级上下文。
- 概览分页复用现有表格组件，展开的课次独立滚动，长班名和正文限制两行并保留完整文本提示。

## 统计与权限

- 学生人数在本期班级内去重；出勤和课评按学生课次计数。到课包含迟到，分母为已点名人次，尚未点名不会计为缺席。
- 学情分母为各课次题目数乘对应花名册人数之和。需关注来自本期至少一题的需提示、模仿完成或未完成标记，同班按学生去重。
- 已有记录依据实际考勤、学情标记或课评内容，与系统开课／下课状态分开统计，支持只使用学情记录的老师。
- 日常沟通按本期课次花名册合并学生、按沟通录入时间筛选，同班不重复计数；正文可能涉及其他课次。
- 新增只读 RPC `get_teaching_class_overview`，复用 `get_teaching_workbench` 的教师／主管课次授权和时间范围。沟通独立检查 `followup.view` 与逐生访问权限，不返回联系方式字段。
- 新迁移 `20260915004000_teaching_class_overview` 经本机主机、实际 Supabase origin、监听进程与数据库指纹核对后，仅应用于开发数据库。类型取自该实际数据库，仅合入新增函数定义；迁移摘要纳入已跟踪迁移与本次迁移。
- 应用回退可保留兼容的只读函数。本批没有业务数据迁移或写入权限变更。

## 机器检查

- 定向 Vitest：班级概览与实际记录共 9 项通过，覆盖人数／人次、沟通去重、教师筛选、未记录状态、缺失数据报错及折叠行正文呈现。
- SQL 回滚事务复用固定开发账号，验证实际聚合值、跨课次去重、沟通日期范围、主管／教师／代课范围、学生及匿名拒绝、独立沟通权限与回滚。临时样例和权限对照全部回滚。
- `pnpm typecheck`、受影响文件 ESLint、zh/en 消息键一致性及 `git diff --check` 通过。
- 固定主管和教师的局域网 zh/en 列表均 HTTP 200，返回 HTML 含新概览；匿名请求跳转登录。此检查验证页面渲染，不替代浏览器视觉与业务验收。

## 人工验收关注点

在所选月份按老师查看班级，核对关键数据能否直接阅读、班名和正文截断是否合适，以及展开课次后进入／返回实际记录的体验。当前状态为开发端待验收。
