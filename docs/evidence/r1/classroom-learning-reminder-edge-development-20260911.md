# 课堂学情页边提醒 · 开发交付

状态：**开发端真实课堂已交付，待人工视觉与交互验收**。产品负责人要求在方案 C 上增加课件外缘细框和简洁书签，并明确使用真实课堂页面验收；看过金色效果后，进一步要求尝试站点主红色。

## 实现

- `LiveShell` 的实际课件层增加提醒容器，常规与专注课堂均由现有 `SessionLearningCheckPanel` 将当前页提醒呈现在该容器中。
- 2px 细框和书签使用站点 `rose` 色彩变量，共用原按钮的已保存结果、考勤排除与保存状态；完成后收为绿色，普通页不显示提醒。书签打开同一份本页逐题学情，使用现有 native 输入标记保留控件点击。
- 边框不接收指针，书签保留 44px 触控区域；现有教师控制端权限与学生展示隔离沿用。课堂画布尺寸、课件内容、同步协议和数据接口沿用。
- 临时 demo 页面及相关文案已移除。验收入口使用现有 `/{locale}/classroom/{classId}/session/{sessionId}/live`；试讲也在同一个真实课堂中运行。

## 检查与入口

相关 lint 通过；既有学情提醒、窄屏与输入路由共 3 个测试文件、25 项测试通过。类型检查中本次文件的错误已修正；全库仍有已有的 `tests/teacher-workspace-entry.test.ts:31` TS18048，确认该文件与 HEAD 一致，本次未修改。

玫瑰红换色增量仅调整提醒组件的颜色类，组件定向 ESLint 与开发服务健康检查通过；登记、布局和输入逻辑复用上述已通过结果。实际辨识度由产品负责人在当前真实课堂页面刷新后验收。

后续修复未设置页的误提醒：试讲原先在检查项为空时，为全部文档页生成临时检查项；新增提醒也读取了这些临时项，导致每页都显示待检查。当前改为保留不绑定页面的通用“课堂观察”，页边提醒只匹配已保存的检查页，点名入口继续可用。用户提供的现有课次经本机只读核对，共 31 页，已保存 8 个检查页，release 默认标记为 0；用户确认全页提醒发生在之前已打开的试讲窗口。刷新试讲会重新读取已保存配置。本次没有数据库写入。

修复的定向 ESLint、学情提醒与课堂连续性 2 个测试文件共 32 项通过。回归覆盖未配置、缺少配置、明确取消全部检查页，以及已选检查页的登记进度；验证通用观察不触发边框、书签或待检查胶囊，试讲名单与点名入口保留。课堂视觉与真实操作仍由产品负责人验收。

本机 Supabase origin 为 loopback，监听进程为本机 Docker。只读核对现有三年级秋季课次《周期问题》、62 页已发布课件和报名记录，局域网课堂入口匿名访问按既有规则跳转登录；当前固定开发教师未分配该班，课堂画面与操作由对应任课教师登录验收。没有新增班级、课次、账号、学情标记或业务数据。

进入真实课次后，在备课页确认需要登记的页面，再进入课堂检查玫瑰红细框、页边书签、点击登记和完成态；也可使用真实课次的试讲入口观察布局与临时登记。机器结果不替代人工课堂验收。

本增量未执行生产切换。先前冻结的 `b9437449` 生产候选不包含本次页边提醒；后续发布应按验收后的代码重新冻结候选，并重新核对生产实际版本。
