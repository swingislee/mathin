# 完整来源入库 · 开发端

2026-09-07 用户确认：全部资料先完整入库，冲突与疑点随记录保存，业务实际使用相关资料时再确认归属。秋季名单逐人确认入口已撤下，原暂存数据保留。

本机隔离目标已提交完整来源批次：1,120 个文件、1,152 张表、14,540 行，其中 14,324 行含内容；原文件共 12,087,566 字节。覆盖当前 `docs/test_material` 的全部 `.base` 与 `.xlsx` 文件，包括两个飞书文件、学生/班级导出、各班级明细、历年报名资料、员工、校区教室与小地推来源。空行、空表、原始字段值、链接及文件字节均保留；附件的来源链接按导出原值保存。

资料存入现有 `history_import_records`；文件字节与路径覆盖分别使用 `history_import_files` 和 `history_import_batch_files`。不同来源文件的表 ID 使用命名空间，来源版本有独立记录 ID。姓名或电话不足以确定身份的记录保留候选；员工来源独立归类，不关联为学生或线索。该批次没有创建一线复核待办。

`/zh/dashboard/history-import` 与英文页面现从 Supabase 的完整批次分页读取。此项交付证明完整来源可查询；各业务事实的结构化投影与按需关联按独立增量接入，不能把来源覆盖计数当作全部业务模型已完成转换。

## 验证

- 目标：WhiteHouse、本机 Docker Desktop、实际 Supabase origin 与监听进程及数据库标识通过只读核对。
- 完整入库事务已演练并回滚，随后正式提交本机；逐文件字节、逐行原文及候选关系与输入相等。
- 既有 26 条来源记录与 18 张相关业务表的指纹保持不变，包含学生、班级、课次、考勤、当前业务及人工修订。
- 员工来源归类修正单独回滚验证后提交，原文不变，没有衍生业务引用。
- 36 个定向机器检查通过，覆盖全量超出试导入上限、空行、原值、延后关联、员工隔离、分页参数与鉴权边界。
- 固定开发管理员中英文页面 HTTP 200，实际读取到完整计数；批次最后 40 行可以分页读出，固定教师会话不能读取管理员来源批次。

迁移：`20260907000700_complete_source_import`。完整导入包、目标核对、回滚检查、提交结果及字段修正结果保存在被 Git 忽略的本机私有证据目录。仓库仅保存本摘要与不含真实个人资料的验证代码。

状态：机器检查通过，开发端可验收，待人工验收。此次没有生产写入、部署或阶段 Gate 关闭。
