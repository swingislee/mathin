# 完整来源入库 · 开发端

2026-09-07 用户确认：全部资料先完整入库，冲突与疑点随记录保存，业务实际使用相关资料时再确认归属。秋季名单逐人确认入口已撤下，原暂存数据保留。

本机隔离目标已提交完整来源批次：1,120 个文件、1,152 张表、14,540 行，其中 14,324 行含内容；原文件共 12,087,566 字节。覆盖当前 `docs/test_material` 的全部 `.base` 与 `.xlsx` 文件，包括两个飞书文件、学生/班级导出、各班级明细、历年报名资料、员工、校区教室与小地推来源。空行、空表、原始字段值、链接及文件字节均保留；附件的来源链接按导出原值保存。

资料存入现有 `history_import_records`；文件字节与路径覆盖分别使用 `history_import_files` 和 `history_import_batch_files`。不同来源文件的表 ID 使用命名空间，来源版本有独立记录 ID。姓名或电话不足以确定身份的记录保留候选；员工来源独立归类，不关联为学生或线索。该批次没有创建一线复核待办。

`/zh/dashboard/history-import` 与英文页面从 Supabase 的完整批次分页读取。学生档案的“历史资料”按学生权限展示已关联原文；有全局学生查看及编辑权限的学服可看到可能相关的资料。管理员资料总表显示最新人工归属，原始匹配依据、原文及确认事件分别保留。完整来源覆盖与可确定业务事实的结构化范围分别计量。

## 业务接入与使用时确认

完整批次中可明确归属、含业务事实的记录已写入现有业务表，共新增 2,473 个历史对象：923 条报名与对应 923 条分班记录、168 条续班机会、189 条历史沟通、104 个活动及对应 104 条登记、62 条测评结果。先前 13 个业务对象与人工修订原样保留；历史报名总数为 925。读取层使用稳定分页与分批来源查询，完整批次可以在现有分班、续班及学生档案页面查看。

来源只给出混合学期或未注明年份时保留原标签，未知周期与日期留空；同一学期多个排课块无法确定所属时，课程背景留在原文。员工资料、多人民册及缺少可靠归属的记录完整归档；后续可以结合实际工作校准。历史写入保留字段来源与版本，不自动产生当前考勤、工作待办或首联。

学生档案与资料详情提供“参考此资料记录沟通”。已关联资料直接进入该学生的沟通页面；归属待定时展示原文和已有学生选项，用户确认后保存关联，并把来源引用写入本次真实沟通。暂时保留会继续保留资料。1,022 组待定学生候选关系已入库，查看档案不会产生确认写入。

确认由现有学生权限控制；教师按学生范围读取明确关联的原文。确认接口检查版本，重复请求不重复记录事件；已被真实沟通使用的来源保护已有归属。沟通写入由数据库核对资料与学生相符，来源引用在保存后保持不可变。原始归属判断、原文和既有业务数据不被确认操作覆盖。

## 验证

- 目标：WhiteHouse、本机 Docker Desktop、实际 Supabase origin 与监听进程及数据库标识通过只读核对。
- 完整入库事务已演练并回滚，随后正式提交本机；逐文件字节、逐行原文及候选关系与输入相等。
- 既有 26 条来源记录与 18 张相关业务表的指纹保持不变，包含学生、班级、课次、考勤、当前业务及人工修订。
- 员工来源归类修正单独回滚验证后提交，原文不变，没有衍生业务引用。
- 定向单测、受影响 TypeScript 与 ESLint 检查通过，覆盖超出试导入上限、空行、原值、员工隔离、业务投影、旧版本保留、分页、鉴权与确认后显示；生成的数据库类型与迁移摘要一致。
- 固定开发管理员中英文页面 HTTP 200，实际读取到完整计数；批次最后 40 行可以分页读出，固定教师会话不能读取管理员来源批次。
- 历史业务事务先回滚验证再提交本机，新增 2,473 个对象，原有业务对象与当前工作指纹不变；报名总数 925，第 901 条以后的记录可读取。
- 按需确认迁移与候选关系先回滚验证再提交。固定开发身份下验证了确认、重复请求、版本冲突、来源使用引用、跨学生阻断、原文保留和教师范围；验证中的临时确认及沟通均回滚，没有写成真实业务事实。
- 学生资料中英文页、分班与续班页面、带来源引用的沟通页面均 HTTP 200。资料归属筛选实测分区总数为 14,324，当前归属与相关记录的数据库嵌入查询通过。

迁移：`20260907000700_complete_source_import`、`20260907000800_contextual_source_associations`。完整导入包、目标核对、回滚检查、提交结果、字段修正与 HTTP 结果保存在被 Git 忽略的本机私有证据目录。仓库仅保存本摘要与不含真实个人资料的验证代码。

后续业务验收检查点：在现有学生档案核对跨学期历史，按实际需要查阅来源或参考来源记录一次沟通；仅在遇到归属疑点时作人工选择。界面、实际业务映射及一线操作体验等待产品负责人组织验收，本记录不推定学生档案已全部人工校准。

状态：机器检查通过，开发端可验收，待人工验收。此次没有生产写入、部署或阶段 Gate 关闭。
