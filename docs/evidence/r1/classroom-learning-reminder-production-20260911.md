# 课堂学情登记提醒 · 2026-09-11 发布记录

状态：**实现与机器检查通过，Linux 生产发布包已构建，等待生产切换确认**。2026-09-11 17:38–17:39，自动审批两次拒绝 current 切换与服务重启，理由为可能中断服务且未认可任务目标中的生产授权文本。切换命令未执行，线上仍保留原版本；待取得审批接受的直接确认后，复核目标并继续同一发布包。R1-Live-2 与现有 Gate 状态沿用。

## 功能范围

后续开发增量已增加课件外缘金色细框与页边书签，并改用真实课堂验收，见[开发交付记录](classroom-learning-reminder-edge-development-20260911.md)。本记录中的 `b9437449` 候选未包含该增量，后续发布应按验收代码重新冻结。

- 采用方案 C：翻到本节课实际选入学情清单的页面时，现有登记入口显示月亮黄胶囊和已保存的登记人数。点击打开本页逐题学情。
- 部分登记继续提醒；保存期间显示保存状态，成功保存后才计入完成进度；失败保持原有已保存计数，全部登记后显示安静的“本页已记录”。完成表示登记完成。
- 普通页恢复原入口；缺席、请假学生沿用补登记排除口径；空名单单独显示。常规课堂、专注模式共用提醒，窄屏显示短文案；独立学生展示端沿用现有控制端隔离。

复用现有学情和考勤接口，本次没有修改数据库、Server Action、鉴权、RLS、依赖或生成类型。生产过程没有执行迁移、业务写入或 Storage 写入。

## 固定候选与检查

生产候选为 `b9437449b4008e7e2e650cba197488838766d355`，基于实际原生产 `7855bab6551dfb3ce9ab70856ccaf4ec36a9fca3`，仅增加本任务 9 个文件的变更。源码已通过 `b2c8cdb0`、`b0e7d8c9` 同步到本地 main，保留其他开发成果；六个代码与测试文件与生产候选逐文件一致，双语新增键已合并。

- 5 个定向测试文件、40 项测试通过：页级提醒、窄屏课堂、课堂连续性、交互同步和缺失原生 UUID 的合同。最终测试夹具调整后，10 项提醒测试、定向 lint 与完整类型检查通过；其余未受影响检查复用。
- 完整 lint 通过，0 错误，保留原有构建脚本的 2 项未使用变量警告；生产候选双语文案检查通过，每种语言 9,249 个键。本地 main 合并后额外完成提醒双语检查。
- Linux 构建及 Worker 依赖打包通过。发布包为 `/home/swing/services/mathin/releases/20260911-093430`，commit 与候选一致；构建后检查 2,884 个源码文件及 4,822 个运行时文件，secret 扫描及环境文件隔离通过。

## 生产目标与回退准备

2026-09-11T09:34:06Z 只读 preflight 核对 SSH 主机 xiaomi、配置与运行时 Supabase origin `https://supabase.mathin.club`、站点 `https://mathin.club`、loopback 3131 监听进程及 current 工作目录，配置权限为 0600。

原 current 为 `20260911-031503 / 7855bab6551dfb3ce9ab70856ccaf4ec36a9fca3`，previous 为 `20260911-015309 / 046ba7f1c7039e37ccd968d5ffbdcf7d0bab4c59`。数据库指纹为 `10e3f97e32b018403c9074efa4e258d699530a487c47de89b5d307ab7ff21a0c`，账本 376 条，head `20260911002000_base_activity_source_links`，摘要 `67d3d562f32e639cd2416e2455e727b1`。

本轮基线计数为：身份 41、班级 34、课次 429、冻结课件 release 3,774、页面文档 77,282、Storage 对象 126,535，operational_errors 2,017。已登记恢复点 `/home/swing/services/mathin/backups/mathin-db-prechange-20260911T032929Z-base-review-7855bab6551d` 的 dump、TOC、manifest 与摘要文件存在；应用改动沿用现有恢复点与原 release。

切换脚本已准备目标、版本、账本和路径保护，健康失败时恢复原 current、previous 并重启复核。上线后检查已准备三条 health 路径、双语登录与课堂鉴权入口、新提醒客户端资源摘要、应用与 Jobs 状态、错误增量和业务计数；这些上线后检查尚未执行。

## 证据与验收

冻结源码与 Git bundle 已在受控目录 `/home/swing/services/mathin/staging/classroom-learning-reminder-b9437449b400-20260911` 完成上传与摘要核对：

- source.tar SHA-256：`7233a2a6e1f84442e575097451f0cc4259caa1385e7b04dce032ecdc9490c0bf`，50,421,760 bytes。
- candidate.bundle SHA-256：`2bc8570b736b8aecb90b9c9bed3114c38b683935fa19fe6cb66767b3fb25c709`，8,306 bytes。
- 访问角色为生产运维维护者，至少保留至 v1.0.0 后 365 天。完整构建、preflight、候选与检查摘要暂存本任务运维目录，成功切换后归档最终证据。

开发端入口：[班级与课次](http://192.168.5.213:3130/zh/dashboard/classes)。老师在课前标记页面后进入课堂，翻到标记页、部分登记、保存完成、返回普通页，并在常规与专注模式观察提醒。实际课堂视觉与交互待人工验收；自动化结果只覆盖列出的合同。
