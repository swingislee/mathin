# 老师上周教学记录 · 本地复现

状态：**开发端已交付，待数据展示与业务人工验收**。用户对上一轮整体展开反馈“这个感觉还可以”，本批重点转为实际数据展示价值；不据此关闭完整视觉验收或 R1-Live Gate。本批生产只读提取，没有生产写入、迁移或部署。

## 当前增量：三位老师与分组

2026-09-15 按用户要求加入薛立志老师，首个标签为“全部老师”。默认按年级分组，可切换按老师；选择老师后保留分组方式。普通教学概览也复用同一分组表格与控制样式。

- 生产只读重新提取 9 月 7–13 日记录，捕获于 12:25:41 +08:00。当前包含王成国、李海霞、薛立志共 12 班、12 课次、82 名学生、226 条逐生逐题学情。
- 薛立志范围为 6 班、6 课次、90 条学情；其中仅 2 节有系统开始／结束时间，其余课次仍展示已有学情，保留时间记录缺失事实。
- 年级来自 `classrooms.grade`，当前为 1、2、3、4、6 年级，分别 1、1、7、1、2 个班。缺失字段进入“年级未设置”，不从班级名猜测。
- 按老师分组时，班级内只汇总该老师实际任课课次；代课、共同任课与未分配分别处理。分组行保持单层标题，组内复用既有两级详情，行和详情标识包含组范围。
- 年级批量读取只覆盖当前授权概览的班级，每批最多 100 班；分组在客户端摘要数据上计算。完整详情仍按需加载，缓存与权限边界保持。
- 生产连接继续使用 `default_transaction_read_only=on` 与 `repeatable read read only`，目标主机和数据库指纹与首轮一致。没有生产写入，也没有把真实资料导入本地业务表。

当前固定快照路径沿用下方所列路径，规范化 SHA-256 为 `595e6b5624e210412286e55b15abe0d0b32c80a1aaf4c60d2ebdeaf8f5ac9624`。原双教师快照另保留为同目录 `week-2026-09-07-two-teachers.private.json`，访问范围与保留期沿用首轮约定。新快照、读取 preflight 和 HTTP 结果均位于 gitignored 目录。

机器检查：23 项受影响测试通过，覆盖数值年级顺序、缺失年级、按老师重算、第三位老师、无系统开课记录的学情、分组下 Enter／Esc／方向键、原位加载和权限合同；类型、ESLint、双语一致性通过。固定管理员／主管 zh/en 页面、全部／三位老师各自的两种分组、普通概览两种分组均可读取；24 次授权课次详情与快照逐条一致，教师／学生／匿名隔离继续通过。

开发端完整概览响应约 486–933 ms，详情约 356–553 ms。开发引用清单：教学概览 32 chunks / 1,724,482 gzip B，复现页 32 / 1,713,591；相对本轮前分别增加约 1.4 KB，无新增引用 chunk。该开发引用清单不等于生产首屏下载量。

状态仍为开发端已交付、待人工验收；本次没有浏览器自动化或生产发布。以下保留首轮双教师捕获时的证据。

## 首轮入口与范围

- [教学记录本地复现](http://192.168.5.213:3130/zh/dashboard/teaching/review)，使用现有开发管理员或主管身份；顶部可切换两位老师，复用教学班级表、共用详情与字段菜单。
- 上周按组织时区 Asia/Shanghai 的周一至周日：2026-09-07 00:00 至 2026-09-14 00:00，右端不含。捕获时间 2026-09-15 11:19:48 +08:00，展示当时保存的上周课次记录，不假定内容在上周内最后修改。
- 用户明确授权提取王成国与李海霞的生产记录；以正式课次任课关系／代课关系选择，6 个正式班、6 个课次、40 名学生，全部使用课次冻结花名册。只提取本页需要的记录字段；本地业务数据库及正式身份关系没有复制或改写。
- 生产目标核对为 Xiaomi，数据库指纹 SHA-256 `10e3f97e32b018403c9074efa4e258d699530a487c47de89b5d307ab7ff21a0c`；连接设置 `default_transaction_read_only=on`，导出使用 `repeatable read read only` 事务与 30 秒语句超时。
- 生产尚无开发端新增的教学概览／实际记录 RPC。本次只读查询既有业务表，按课次冻结花名册和任课范围整理为现有页面合同；没有向生产安装开发函数。

## 从实际内容得到的展示调整

| 观察 | 页面呈现 |
| --- | --- |
| 六节课共有 136 次学情观察，但每题记录覆盖不同 | 班级行显示“独立 / 讲解 x/y”，分母为实际逐生逐题观察；另列有记录题数。展开后每题显示已记录学生数／花名册人数。 |
| 王成国四年级班的练习4.1，6 次记录均为需提示；另有 2 名学生未记录该题 | 优先题目直接显示“需支持 6/6 次”，详情保留 6 条提示与 2 个未记录状态。 |
| 李海霞三年级班的练习4.1，8 次记录中 4 次需提示、2 次模仿完成 | 该题作为班级优先查看项；展开核对对应学生及其他题目的变化。 |
| 10 行原始课评中 3 行全空，其余 7 行有文字；没有数值评分 | 概览与详情共同剔除全空课评；只写“已填课评”，不把短评数量当作反馈质量。 |
| 六节课均无考勤，五节有系统下课时间，一节已开课但未记下课 | 课次／考勤列显示缺失事实；未记录不推定为缺席、未授课或仍在实际授课。 |
| 1 条学生沟通在上周录入，但实际沟通日期与署名均缺失 | 展示为学生背景与沟通参考，明确“沟通日期未注明”、录入时间和未记录姓名，不归为任课老师当周沟通。 |

课堂表现、覆盖和优先题目由快照适配器提供；现有线上读取尚无这些聚合字段时仍走原概览展示。本批用于本地数据价值验收。班级名称不链接至本地数据库中的生产对象 ID；实际内容继续在共用组件内展开。

## 加载与数据边界

- 原始快照存于 gitignored 的 `.tmp/teaching-production-review/week-2026-09-07.private.json`，56,478 B，SHA-256 `5ce4216681eca2b2adbd102484388823b7de15cd69e6d2b773a89d2542e1951f`。本机工作区受控保存；验收使用期 30 天，后续保留由负责人决定，未建立自动清理。
- 页面与详情在读取文件前同时检查 development、精确本地 Supabase origin、`class.view.all` 与 `followup.view`，并沿用员工环境／身份闸门。文件路径固定；客户端只能选择已登记快照和其中课次。生产环境、普通教师、学生、匿名访问均不能通过此入口读取快照。
- 首屏仅传概览与短摘要，沟通摘要最多 96 字；完整学情矩阵和沟通正文在展开时读取。原位请求保留取消、过期结果丢弃、失败重试、最多 8 页／30 秒实例内缓存；缓存键区分快照与普通记录。
- 明细复用现有配色及详情布局。每格重复的作者与时间转为状态提示信息，逐题记录保留原始值。没有增加截图、外部存储或公开快照下载入口；Git 证据只含汇总，没有学生姓名与正文。

## 机器检查

- 23 项定向 Vitest 通过：快照环境与权限限制、上海周边界、观察与题目／花名册分母、空课评及零分、正文摘要与来源日期、未知课次／快照、分页、缓存隔离、原列表与原位展开行为。
- `pnpm typecheck`、受影响文件 ESLint、zh/en 消息一致性通过。首次类型检查遇到并行骰子任务的测试接口暂态报错；该任务修正后再次全库类型检查通过，本批未修改骰子文件。
- 本机读取 preflight 核对 WhiteHouse、Supabase `http://127.0.0.1:35421`、数据库指纹及 Docker／3130 监听进程，复用固定开发身份，不创建账号或生产测试数据。
- 固定管理员／主管的 zh/en 页面均渲染 6 个班级和实际困难题目；12 次课次详情读取的花名册、逐题状态与原始快照完全一致。空评语按合同过滤；完整沟通正文按需返回，响应 `private, no-store`。
- 普通教师详情 403，学生与匿名跳转，受限页面没有生产教师或表格内容；未知课次 404、未知快照 400。Next 流式页面可能以 HTTP 200 承载权限跳转，因此同时检查响应中没有快照内容。
- 开发端首个重新编译页面约 28.95 秒；其余页面约 534–601 ms，详情约 355–615 ms，单课详情约 3.7–8.3 KB。这些是本机开发样本，不是生产性能承诺。
- 开发构建教学原路由引用清单 gzip 1,716,243 → 1,719,559 B；新增复现路由 1,708,311 B，均 32 个引用 chunk。清单包含共享壳与开发依赖，不代表生产首屏下载量；没有添加新的重型客户端边界。

人工验收重点：班级行能否帮助确定优先看哪节课、哪道题；展开后能否定位具体学生；记录覆盖不足和沟通背景是否能与教学表现分清。当前未进行浏览器自动化或替代人工视觉验收。

## 2026-09-15 图标与底色增量

按产品反馈，课堂表现摘要改为固定顺序的 SVG 与计数，逐题只读格采用登记面板相同的已选状态底色。复用 `LearningCheckStatusIcon`、`LEARNING_CHECK_STATUS_STYLE`，通过只读标记保留标题和可访问名称，明细提供单行图例。题目列压缩，逐格作者与时间保留在状态提示中；本批没有修改数据口径或加载边界。13 项相关展示／原位展开检查、受影响 ESLint 和类型检查通过，开发端待人工密度与阅读验收。
