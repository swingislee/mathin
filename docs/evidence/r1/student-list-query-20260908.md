# 学生列表批量查询与按需登记 · 2026-09-08

状态：开发端已交付，待人工验收。本批覆盖工作名单与全部档案的五个阶段；再联系建议保留其独立读取。生产发布与 R1-Live Gate 状态沿用现有记录。

列表改为一次授权 RPC 完成阶段事实、筛选、排序、列候选、计数和分页。原接口返回待续班全部 163 行；新接口返回当前 50 行及完整范围的统计。来源关联按批次汇总，来源冲突判断继续包含同一来源的全部身份归属。展开学生后才读取提醒、邀约及其并发版本；业务选项在进入邀约或报名登记时读取。

本机同一固定开发主管、同一工作范围及数据的结果如下。原接口采样 5 次；新接口连续采样 20 次，P95 使用排序后的第 19 个值。

| 量尺 | 结果 |
| --- | --- |
| 待续班 RPC 中位数 | 1,919 ms → 161 ms，约减少 92% |
| 新 RPC P95 / 最大值 | 167 / 221 ms |
| 新 RPC 首次调用 | 245 ms，单独记录，未并入连续样本 |
| 返回 JSON | 155,089 → 50,456 bytes，约减少 67% |
| 5 路并发、3 批共 15 请求 | 154–244 ms；中位数 158 ms |
| zh/en 开发页面首次出现列表 | 478–661 ms，均为 HTTP 200 |
| 全部档案首联、2,426 条范围 | RPC 1,352–1,426 ms；开发页面 1,741–1,868 ms |
| 学生路由客户端关联 chunk gzip | 519,225 → 519,209 bytes，体积基本不变 |

工作范围共有 351 条记录，管理员全部档案范围为 5,444 条。管理员/教师 × 工作/全部档案 × 五阶段共 20 组对照，累计 9,646 条记录的身份、展示字段、阶段及计数一致；推定来源编号按集合比较。完整邀约与提醒由按需读取和表单测试单独覆盖，前两个阶段未显示的测评与课程背景字段不进入展示比较。

默认计划在第六次相同调用后由约 150 ms 增至 680 ms；仅两个新列表函数使用 `force_custom_plan` 后，连续调用保持稳定。没有修改数据库全局规划策略。

验证覆盖：

- 本机目标组合指纹核对、候选迁移事务回滚、匿名/非员工拒绝、私有辅助函数 ACL、查询字段白名单及日期校验。
- PostgreSQL 实际执行与共享字段合同对照，覆盖 zh/en、自然数字顺序、缺失值、Unicode 空白、文本组合、枚举、组织日期、忽略自身条件的列候选及越界页码。
- 按需读取失败重试、切换学生时忽略旧响应、完整事实到达后初始化草稿、已有草稿保留、邀约并发版本及按需业务选项。
- 应用源码与本次测试的 TypeScript 检查、定向 ESLint、学生页面定向生产编译及 `bundle:report` 前后对照。编译使用独立目录；它不代表完整发布构建或正式 Gate。
- 091 应用事务核对 13 张业务表内容指纹不变；092/093 仅调整函数，均已完成本地检查与应用。

可复用数据库检查入口为 `tests/student-list-query-database.test.ts`：在本机开发环境设置 `MATHIN_STUDENT_LIST_DB_TEST=1` 后通过 Vitest 运行。脚本先核对主机、Supabase origin、监听进程与隔离数据库指纹；测试只使用固定开发身份，候选 DDL 与合成字段记录留在回滚事务内。相关表单与适配测试为 `student-stage-entry-loading`、`student-list-query-data`、`student-stage-table-fields`、`student-stage-interaction` 和 `student-stage-workspace`。

本批 migration 为 `20260908009100_student_list_query`、`20260908009200_student_list_parameter_plans`、`20260908009300_student_list_field_compatibility`。规范化 checksum、去标识化样本和 bundle 结果见 [结构化记录](student-list-query-20260908.json)。提交版本以本文件同一 Git 提交为准。

全部档案页面仍有约 1.8 秒的开发端加载开销；当前机器检查不代表生产容量或人工体验已验收。本次保留该可复测数据点，不关闭正式 Gate。
