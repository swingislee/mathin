# 教学工作台时间维度 · 2026-09-15

状态：开发端已交付，待人工视觉与业务验收。生产未迁移、写入或部署；R1-Live Gate 保持。

## 操作与数据范围

- 教学概览、团队完成情况与三位老师复现页使用同一时间控件：周／月／学期，当周／上周、当月／上月、本学期／上学期，前后切换与日期选择。
- 周按机构时区的周一至周日，月按自然月，学期按 `school_terms` 的配置日期并包含结束日。未配置完整日期的学期不猜测边界。
- 时间变化保留老师、班级和分组，清除旧课次与沟通分页定位。复现页初次打开仍使用上周；普通工作台默认当周。
- 快照按所选课次日期筛选，沟通按所选区间内的沟通日期或录入时间筛选。详情请求携带范围，缓存键隔离周／月／学期；范围外课次返回 404，畸形或超过 366 天的请求返回 400。
- 当前受控快照仍是 9 月 7–13 日，已有记录保持。月／学期显示部分覆盖提示，未相交范围显示尚未采集，避免把缺失资料显示为完整的零记录。
- 已通过只读方式核对学期元数据并添加到本地快照：暑期 2026-06-30 至 2026-08-31，秋季 2026-09-01 至 2027-01-01。

扩大生产明细导出曾被自动审批拒绝，理由为需要用户明确确认敏感资料范围与保存位置，导出未执行。待确认的具体方案为三位老师在 2026-06-30 至 2027-01-01 的课次、花名册、学情、课评与沟通，保存到 gitignored `.tmp/teaching-production-review/periods-2026.private.json`，用于本地时间维度复现；生产仅只读。当前可独立验收页面和既有一周数据。

## 本地数据改动

`20260915006000_teaching_term_window.sql` 将既有教学读取函数的跨度上限从 32 天扩展为 366 天，保留函数签名、任课／主管授权、过滤条件、1000 课次上限和截断提示。迁移只替换已核对的跨度表达式，基线不符即拒绝执行。

本机 host、Supabase origin、监听进程和数据库指纹已核对。既有函数属于维护角色，首次普通本地连接的修改因所有权不足被数据库拒绝；改用相同目标的维护连接并复核指纹后，事务检查、回滚和正式本地应用通过。未更改函数所有权或生产目标。

规范化迁移 SHA-256：`f9dddcf26e491b4d26ac9b2bbc1c5650a2569781fd5c219fe20348ee6e043112`。本地类型生成核对函数签名未变，仅同步 migrations digest。

## 验证

- 周界、跨年周、闰月、上月、真实学期边界、未配置学期及筛选保留的定向测试通过。
- 快照完整／部分／未覆盖、沟通实际日期与录入日期、范围缓存隔离、详情范围校验及原位展开合同通过。
- 隔离 SQL：长学期与包含同一批课次的短月范围完整 JSON 相等；教师仅本人课次、主管全校范围、学生拒绝、过长与空区间拒绝通过。测试数据随事务回滚。
- 固定本地主管检查普通工作台与复现页各 6 组时间状态全部 HTTP 200；中文快捷入口、英文页面、真实学期起止、数据覆盖提示与详情状态码核对通过。
- 本机完整响应：复现约 407–1012 ms，普通概览约 418–799 ms，仅作为开发参考；类型检查、双语消息和数据库类型摘要一致性通过。
- 开发客户端引用清单：教学概览 32 chunks / 1,728,664 gzip B，复现 32 / 1,717,742，较本轮前各增加约 4.2 KB。无新增引用 chunk；该引用清单不代表生产下载量。

原始验证结果在 gitignored `.tmp/teaching-period/`，仅本机开发者可访问。本轮没有运行浏览器视觉自动化或全量发布 Gate；时间选择手感与业务范围由产品负责人实际验收。
