# 原表字段与重复提示：开发交付

2026-09-10。状态：**机器检查通过，开发端已交付，待人工验收**。生产仅执行字段审计读取；本轮没有生产写入，不关闭阶段或 Gate。使用方式与边界见[原表资料与可能重复提示](../../runbooks/school-source-record-review.md)。

## 原字段核对

- 只读核对两份已归档 Base 原文件，文件字节与本次导入输入一致。5,338 条记录的字段集合与 221,301 个单元格文本比对一致；`rawValue` 项数量一致，未单独宣称每个结构化 `rawValue` 或外部附件文件均完成比对。
- 两份 Base 共涉及 345 个业务字段定义，其中 274 个有非空值，共 92,809 个非空业务单元格。
- 初始运营导入只读取部分字段。获客表中的获取日期（2,652 条非空）、获客区位（3,051 条）、触达内容（2,667 条）没有被初始映射器读取。归档保留与业务字段完整是两个检查项。
- 新增的逐字段报告在同一份原输入上记录 46,283 个未被映射器查询的非空单元格；其中包含身份预处理、组织和素材等非个人业务字段，不能全部解释为当前界面缺失。后续 SQL 读取与既有修复仍需按目标字段另行核对。
- 在全部 5,338 条 Base 记录上比较新增报告前后的运营输出：业务行、来源锚点、沟通补充与行覆盖输出一致，规范化 JSON SHA-256 为 `86d225d672c065e3f5acf38db7f0b18e48cc23e1c13110ff41fbefeae2c3a9bf`。报告增量未改变身份或业务导入结果。

## 开发验证

- 本机应用迁移 `20260910002000_school_source_record_review`，SHA-256 为 `49b007c4b1079f541a6dd37c2cfc1d2f165c38efb6e9a534529cb1705f4a995e`。核对本机执行目标后完成事务验证及回滚，再应用只读函数与检索索引。
- 固定开发身份与事务内合成业务记录覆盖：学生／线索双向提示、姓名和电话规范化、父母号码、同家庭不同姓名排除、已关联线索排除、不可见候选排除、非员工及匿名拒绝、分页无重复、共享或冲突来源排除、未知字段完整返回和多行原文保留。
- 合成数据在 savepoint 中回滚；原学生、线索、沟通、来源、身份关联、参与人及业务记录内容指纹不变。迁移试跑回滚后 public 函数定义、ACL 与索引均恢复。
- 原表查看组件验证按需读取、不同来源值分别展示、新字段、失败重试、zh/en 文案及标签点击只打开原对象。学生列表、首联和线索表的受影响检查，以及类型与相关文件 ESLint 检查通过。
- 管理员与老师的 zh/en 学生、线索、沟通页面均返回正常页面；管理员读取真实原表字段，固定学生身份读取被拒绝。固定老师的当前名单无原表资料，老师的原表权限由事务合同验证覆盖。
- 以上检查证明读取合同及页面可启动。页面视觉、交互和业务口径等待产品负责人在开发端验收；没有执行浏览器自动操作或截图验收。

详细含原记录的比较结果留在本机私有验证目录，共享仓库仅登记不含个人资料的摘要。
