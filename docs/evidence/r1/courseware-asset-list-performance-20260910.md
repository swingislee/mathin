# 课件素材列表读取提速 · 2026-09-10

状态：**已于 2026-09-11 部署生产，机器检查通过，待人工使用验收**。本页保留开发阶段测量与验证记录；发布范围、生产检查和备份见[本轮生产发布](read-performance-production-20260911.md)。R1-Live-2 和 Gate 状态保持。

## 本次调整

- 在 65,843 个素材、552,254 条绑定记录的本地目录中，将逐素材引用查询改为按候选素材批量汇总。原查询每次展示 10 条，却先为数万个素材计算完整引用明细。
- 用引用次数完成筛选、排序和分页，再为当前页读取课程数、课次数、版本及文件信息。继续按绑定条数计算引用次数；课程和课次分别去重，已删除页面继续排除。
- 保留 16:9 / 4:3 的 published → draft → 基础 published 版本回退顺序，以及无可用版本的排除条件。名称/来源键搜索、类型、角色、最小引用数和稳定分页顺序沿用原合同。
- 新增迁移 `20260910008000_courseware_asset_list_usage`，调整现有 `list_cw_shared_assets` 的读取查询与执行计划。页面和预览签名 URL 流程沿用现有实现。
- 为该函数设置 `plan_cache_mode=force_custom_plan`，根据当前筛选选择计划。单独的缓存计划诊断中，同一默认查询的通用计划为 3,452 / 3,617 ms，按参数选择的计划为 1,801 / 1,266 ms；新设置覆盖连续查询时的这项回退。

## 页面测量

本机 Next 开发服务、固定管理员，`/zh/dashboard/courseware-assets`，每次均为 200 且显示 10 条素材：

| 测量 | 两次预热后的完整 HTTP 响应 |
| --- | --- |
| 调整前 | 5,770 / 5,728 ms |
| 调整后 | 2,049 / 1,468 ms |

调整后的另一次预热请求为 1,557 ms。首个复测请求为 6,469 ms，其中首字节等待 4,737 ms，单独保留，不混入预热对比；本批未定位这次额外首字节等待的来源。英文 4:3 第二页为 200、10 行，完整响应 2,320 ms。

这些是开发服务的 HTTP 读取测量，未代替浏览器交互验收或生产测量。

## 定向验证

- `node scripts/courseware-asset-list-usage.mjs --check` 通过：39 个场景包括两种画幅的首屏、下一页、5,000 偏移/101 行、完整目录、最小引用数、五种类型、角色、搜索空结果、超界分页及现有 null 参数行为。完整 JSONB 或全目录有序摘要一致；16:9 和 4:3 完整目录分别为 65,843 / 65,842 行。
- 管理员、教研、多岗位身份读取保持；教师、销售、教务、主管、学生、家长继续得到原拒绝结果，匿名执行权限拒绝保持。无效画幅、类型、角色、查询长度、引用数和分页参数错误码一致。
- 对照整个 public 函数目录、策略、表/视图权限和索引，仅目标读取正文及预期的计划设置变化。签名、默认参数值/类型、owner、ACL、security definer、search path 保持。默认参数 AST 仅规范化源码位置编号，其他结构和取值严格比较。
- 素材、画幅版本头、素材版本、文件对象、页面绑定、页面、课程讲次共 7 张表的完整行摘要与计数保持；事务回滚后目录及业务摘要恢复一致。
- 检查脚本 ESLint 与变更文本检查通过。页面代码、类型合同和存储签名流程均沿用现有实现。

最终事务对照的完整列表 RPC：16:9 首屏 4,192 → 2,157 ms、下一页 3,625 → 1,588 ms；4:3 首屏 3,499 → 1,177 ms、下一页 3,452 → 952 ms。收益主要在较大的候选集；视频、H5 等小集合的单次样本有约 0.1～0.3 秒波动，本批不据此宣称所有过滤组合都提速。

`node scripts/courseware-asset-list-usage.mjs --apply` 通过。本地应用前按执行主机、实际 Supabase origin、监听进程、Docker 网络及数据库标识重新核对目标；应用脚本要求迁移 checksum、迁移头与完整检查前状态一致，保存原函数，并在同一事务内写入 DDL 和迁移账本。详细对照与回退定义保留在本机受控临时目录，仓库仅记录无个人资料的摘要。

## 其他页面排查

本机 Next 开发服务预热后的管理员完整 HTTP 响应，均为 200：沟通列表 525 ms、线索表 628 ms、活动页 345 ms、班级页 287 ms、课件任务列表 1,829 ms、素材库 4,712 ms。首次编译单独处理，未混入这些预热数据。

课件任务列表剩余主要开销在 `list_courseware_tasks`：为候选任务汇总页面、草稿及最新版本，约 1,359 ms。本批优先交付学生档案和素材库两个较重的读取增量；这些测量用于确定后续顺序，不代表所有页面已完成性能验收。
