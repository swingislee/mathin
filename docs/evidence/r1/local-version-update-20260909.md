# 2026-09-09 本地版本对账与 0.1.2 更新

状态：**本地代码与所需迁移已更新，定向机器检查通过，开发端已交付，待人工验收**。本次生产操作仅只读核对 current/previous 元数据；没有生产迁移、通知发送、业务写入或服务变更。

## 版本和来源

本地工作树起始为干净状态，分支 `codex/principal-dashboard-v1`、HEAD `5550c1f5`。原 `package.json` 版本为 `0.1.0`，未发现 `0.1.1` 或 `0.1.2` 产品版本记录。

| 版本口径 | 范围与状态 |
| --- | --- |
| `0.1.1` | 归档同日已发布的课堂与学服增量，最新对应 `20260909-042441 / c63a47b5` |
| `0.1.2` | 在 `0.1.1` 上同步员工桌面通知、保留本地点名与开课修复；本地 package 版本更新为此值，完整组合尚未作为新应用发布生产 |
| 实际生产 current | 只读核对为 `20260909-074028 / c7009ec054d3397b235cb5acda96e82f6a9fbbcc`，构建时间 `2026-09-09T07:42:41Z` |
| 实际生产 previous | `20260909-042441 / c63a47b5b0d5031ea3c34cd934fbf6307abd37b0` |

版本归档不重写历史 release 元数据。面向业务使用者的完整说明统一维护在[更新说明](../../../CHANGELOG.md)。

当前本地已包含课堂、学服及 RPC 修复；缺少的是独立发布分支 `codex/web-push-production-20260909` 中的以下五笔增量，本次将其同步到当前分支：

- `a15c8f09`：专用推送任务处理服务和邮件告警。
- `8058efdf`：Windows Edge 范围、同源入口与双语提示。
- `b0c23f99`：通知启用安全、网络与数据库合同，以及退出登录容错。
- `c7009ec0`：独立 Worker 依赖打包修复；该提交为生产实际运行版本。
- `9fa485ff`：生产启用事实、专题状态及诊断脚本修正。

同步时退出登录实现只有注释差异，保留本地已通过验证的绑定 RPC 与 `try / await / catch`；证据索引保留两边条目。通知实现、迁移和生成类型与来源分支一致，本地点名抽屉、今日课次统计和微课开课的三处调用修复继续保留。退出修复已随通知发布上线，因此相应故障记录更新为“退出已部署，点名与开课待部署”。

## 本地数据库更新

首次目标核对发现开发服务未监听 3130，停止数据库步骤。确认本机 Docker 网关仍正常后，使用仓库 `scripts/dev.mjs` 后台启动开发服务，再完成主机、应用实际回环 origin、两个监听进程、Docker endpoint、网络及数据库标识核对。

核对结果为 Windows `WhiteHouse`、本机 Docker Desktop 的隔离回环 Supabase；数据库指纹为 `5af56ae69b51ca0a78b9357ec4792533a6e59f0a529a9a918f6ba4c93da68d0f`。唯一缺少的本批迁移为 `20260909009000_employee_web_push_activation_safety`，与生产候选规范化 SHA-256 一致：`44e3be268a19c8eda10abbfa0ec102da395b8271c05a3b53cce1bfc405b8d45b`。

先使用已有固定开发账号运行该迁移的事务行为断言，完整回滚并独立核对快照；通过后在带数据库身份、旧账本、事务锁和超时守卫的事务内正式应用同一文件。迁移前后核对 13 张本地业务、通知与配置表的内容摘要，结果一致。

本地 ledger 从 `354 / 20260909002100_enrollment_confirmed_class_mismatch` 更新为 `355 / 20260909009000_employee_web_push_activation_safety`；独立回读确认摘要、专用领取 RPC 和员工失效触发器存在。通知开关、通道、人员范围及业务数据保留原值。本次没有启动本地推送 Worker，也没有将生产业务数据或通知配置复制到开发环境。

## 检查与验收边界

| 检查 | 本次结果 |
| --- | --- |
| 通知与 RPC 集成 | 8 个定向文件、97 项通过，覆盖退出客户端绑定、点名与开课路径、Edge 范围、网络目标防护、Worker 处理和隔离依赖打包；HTTP 使用内存替身 |
| 本地数据库合同 | 1 项通过，包含固定账号行为、迁移回滚和独立零残留核对；随后正式本地应用与独立 postflight 通过 |
| TypeScript 与 ESLint | 全项目 TypeScript、同步涉及的 TS/TSX/MJS/MTS 文件 ESLint 通过 |
| 生成类型与双语 | 迁移摘要与生成类型匹配；zh/en 的 9182 个消息键一致 |
| 开发入口 | health 为 `ok / development`；zh/en 首页最终均 HTTP 200；分班、班级和账号安全的匿名访问均转入带原路径的登录页 |
| 文本与来源 | Git 文本检查通过；同步后的通知代码与来源分支一致，原本地课次修复保留 |

首次并发冷编译首页时，英文页出现一次 `Unexpected end of JSON input` / HTTP 500；编译结束后定向串行重读英文和中文首页均为 200，含 Mathin 页面内容且无已检查的 digest 错误标记。该观察不足以判定根因或证明后续不会复现，未据此修改业务代码。

复用同日来源候选已有的生产构建、通知真实 provider 和监控验证记录；本次没有重复构建、浏览器控制、截图或真实通知投递，也没有执行阶段关闭。用户需在开发页面验收交互；实际系统 Push 仍需 HTTPS、启用配置及 Windows Edge 设备条件。局域网 HTTP 只用于页面入口和普通业务功能验收。

本地执行小摘要保存在 Git 忽略的 `.tmp/version-0.1.2/`：目标核对、数据库回滚检查、同文件正式应用、开发服务日志。仓库只记录上述无个人信息的事实与结果。
