# R1-7 · 初始化、导入、质量、修复与导出（已关闭）

## 状态

R1-7A～E 全部通过并于 2026-08-01 关闭：学生 CSV 导入、初始化 manifest、版本化数据质量扫描、领域修复计划、用户权利 artifact 与运营导出分流均具备 E1～E3 开发证据。当前阶段推进到 R1-8；R1-7 的开发库证据不代表生产初始化、正式清理或生产导出已经验收。

| 子阶段 | 状态 | 实现提交 | 结果 |
| --- | --- | --- | --- |
| R1-7A 学生 CSV 导入 | passed | `e193165` | 服务端 dry-run、逐行错误/重复、批次账本和幂等 apply |
| R1-7B 初始化 manifest | passed | `c9901e2` | 自然键、文件摘要、preflight/post-apply 对账和计划只读 |
| R1-7C 数据质量 | passed | `0b95f1c` | 5 条版本化规则、持久 run/finding、稳定结果 hash 和权限边界 |
| R1-7D 修复 | passed | `54587479152e03de4a69f69b14579506d720d8fa` | 4 类领域能力边界；订单状态预览、执行、真实回滚与再次执行；最终复扫 0 条异常 |
| R1-7E 导出 | passed | `a6159f144c3da2ee168704a2949e5acf7cc04def` | 用户权利/运营导出分流、角色字段裁剪、精确字节 hash、下载审计、7 天过期与受控清理 |

## R1-7D 关闭门

| 字段 | 值 |
| --- | --- |
| `gate_id` | `DATA-R1-7D-REPAIR-CLOSEOUT-20260801` |
| `domain` | 数据质量 / 领域修复 / 财务派生状态 / RLS / 审计 / 恢复点 / zh-en |
| `result` | pass |
| `measured_value` | 实现提交 `54587479152e03de4a69f69b14579506d720d8fa` 上 `pnpm ci:checks` 14/14；R1 合同 13 文件、86/86；production build 314/314 页面；messages 3673×2；数据库类型摘要与 154 个迁移文件一致。开发库 10 项 R1 SQL 断言全部通过并回滚。固定 principal 浏览器实际完成订单 `refunding → partial → refunding（回滚）→ partial`，最终质量复扫 0 条；开发库保留 1 个 `executed`、1 个 `rolled_back` 计划和 5 个不可变事件，修复领域事件产生通知 0 条。 |
| `threshold` | 写动作按领域白名单；先预览影响数量、目标 hash 和恢复快照；执行与回滚重新锁定目标并复算 hash；目标变化、过期或后置条件失败时事务无部分写；可恢复动作实际回滚；读取/执行权限分离；金额字段不由状态修复改写。 |
| `commit_sha` | `54587479152e03de4a69f69b14579506d720d8fa` |
| `migration_head` | `20260801000800_r1_data_repair_plans`；标准化文本 checksum `416876e944bfde90ed1d3763541231ce365bc63559817cc0d25383571e4605d4` 已登记开发库。 |
| `environment` | Windows 本地 Next.js 16.2.11；小米自托管 Supabase 开发库；Codex in-app Browser。 |
| `dataset_manifest` | 复用 `.claude/test-accounts.local.md` 固定 principal 开发账号和既有订单异常；未新建账号。真实演练仅重算订单派生 `status`，未改金额或流水；计划、事件和最终扫描作为开发/RC 数据保留至 R1-15 清理。凭据、token 与可识别未成年人信息未写入仓库。 |
| `started_at` | `2026-08-01T09:55:00+08:00` |
| `finished_at` | `2026-08-01T10:36:00+08:00` |
| `actor` | Codex desktop agent（实现、数据库断言、真实回滚演练、构建与 CI 收口） |
| `approver` | `swingislee`（以“继续”及此前推送授权批准本轮线性施工） |
| `command_or_runbook` | `SUPABASE_META_SSH=xiaomi pnpm db:types`；`pnpm typecheck`；`pnpm lint`；`pnpm messages:check`；`pnpm db:types:check`；`pnpm r1:test`；`pnpm build`；10 项 R1 SQL 断言；固定 principal 在 `/zh|en/dashboard/data-maintenance` 执行预览→执行→回滚→再次执行→质量复扫；`pnpm ci:checks`。 |
| `artifact_url_or_path` | not_applicable；R1-7D 子阶段关闭时未单独生成 artifact，整体汇总见下方 R1-7E 关闭门。 |
| `artifact_hash` | not_applicable；R1-7D 子阶段事实由实现提交、迁移 checksum、数据库断言和本页小摘要固定。 |
| `retention` | 本摘要随 Git 历史永久保存；开发业务数据按 R1-15 清理。 |
| `access_roles` | 仓库读权限持有者 |
| `failure_ticket` | not_applicable；首次 Vitest 在受限 Windows token 下 `spawn EPERM`，改在已批准的沙箱外同一工作树重跑后通过；代码与测试本身无失败。 |

## R1-7D 已验收合同

- `data_repair_capability_versions` 版本化登记订单状态重算、学生合并、课件 replacement 回滚和测试数据永久清理；恢复边界分别标记为自动回滚、领域回滚或外部备份必需。
- 计划调度只开放 `order_status_recompute`。金额不平、`void` 订单、目标已恢复或非订单发现均拒绝生成计划；不存在可传表名、列名或任意 SQL 的通用写入口。
- 预览保存影响数量 1、24 小时执行期限、执行前/预期执行后 SHA-256 与恢复快照。执行先锁计划和订单，当前 hash 与目标 hash 不同即停止；成功后校验预期后置 hash。
- 回滚仅接受仍处于已执行后 hash 的订单，并恢复预览时的状态；执行、回滚和审计事件处于同一事务，失败不留部分状态。旧计划不可重新执行，需重新预览。
- `audit.view` 读取能力、计划和事件账本；`system.operations.manage` 生成、执行和回滚；学生角色无法读取账本或调用 RPC。领域事件无目标用户，不产生通知噪音。
- `/dashboard/data-maintenance` 的 zh/en 界面展示能力恢复边界、可修复发现、影响数量、目标/预期 hash、执行有效期和事件数；执行及回滚使用项目 AlertDialog 确认。

## R1-7E 关闭门

| 字段 | 值 |
| --- | --- |
| `gate_id` | `DATA-R1-7E-EXPORT-CLOSEOUT-20260801` |
| `domain` | 用户权利 / 运营导出 / 字段最小化 / SHA-256 / 下载审计 / 保留与清理 / RLS / zh-en |
| `result` | pass |
| `measured_value` | 实现提交 `a6159f144c3da2ee168704a2949e5acf7cc04def` 上 `pnpm ci:checks` 14/14；R1 合同 14 文件、93/93；production build 314/314 页面；messages 3707×2。155 个仓库迁移在标准 PostgreSQL 15 一次性库完整重放，11 项 R1 SQL 断言通过；GitHub Actions 运行 30682780479 的 database、checks、supply-chain 全部通过。开发库固定 student→principal→student 浏览器链路完成申请、核验、批准、生成和下载，下载计数 0→1；固定 teacher 导出既有板书 WebP 后只新增 operational 审计。精确 JSON 的存储 hash 与 UTF-8 字节数一致，禁入字段检查通过。 |
| `threshold` | 用户权利 artifact 与即时运营导出分表/分类；主体和角色字段白名单；支持人员不可读取正文；跨主体、过期、清理后下载拒绝；生成和每次下载可审计；WebP 在触发浏览器下载前登记同一 Blob 的 hash/字节数；不暴露内部备注和非必要未成年人资料。 |
| `commit_sha` | `7ba9cb8bd83dbf5aa5b94713979edd44a1849c86`（核心导出实现：`a6159f144c3da2ee168704a2949e5acf7cc04def`） |
| `migration_head` | `20260801000900_r1_export_artifacts`；标准化文本 checksum `18e69f7b547598452d09586841f7157635c2233473cbb43d9c59f943cc601ec8` 已登记开发库。 |
| `environment` | Windows 本地 Next.js 16.2.11；小米自托管 Supabase 开发库；一次性 PostgreSQL 15/PG meta 容器；Codex in-app Browser。 |
| `dataset_manifest` | 复用固定 student、principal、teacher 开发账号和既有两条 board solution record；未新建账号。浏览器验收保留 1 个已完成权利请求、1 个 7 天 artifact、1 次主体下载和 1 次运营导出审计，统一由 R1-15 清理。凭据、token、artifact 正文与可识别未成年人资料未写入仓库。 |
| `started_at` | `2026-08-01T10:37:00+08:00` |
| `finished_at` | `2026-08-01T11:54:00+08:00` |
| `actor` | Codex desktop agent（迁移、应用、断言、开发库与浏览器验收、CI 收口） |
| `approver` | `swingislee`（以“继续”及此前推送授权批准本轮线性施工） |
| `command_or_runbook` | 标准 PostgreSQL 15 容器重放 155 个仓库迁移；`supabase/tests/r1_export_artifacts_assertions.sql` 及 11 项 R1 数据库总门；开发库 student→principal→student 权利导出链路；teacher 板书 WebP 导出；`pnpm lint`；`pnpm typecheck`；`pnpm messages:check`；`pnpm db:types:check`；`pnpm r1:test`；`pnpm build`；`pnpm ci:checks`；GitHub Actions 运行 `30682780479`。 |
| `artifact_url_or_path` | `docs/evidence/r1/artifacts/r1-7/verification-summary.txt` |
| `artifact_hash` | `c43b4d2403a8a73ea38c450be7f6a607aca71523e08af98066054ab536b0b017` |
| `retention` | 本摘要与小型验证 artifact 随 Git 历史永久保存；开发 artifact/审计按 R1-15 清理；正式环境保留策略在 R1-16/18 固定。 |
| `access_roles` | 仓库读权限持有者 |
| `failure_ticket` | 首轮受限 Windows 沙箱中的 build/Vitest 因 `spawn EPERM` 无法创建子进程，随后在获批的沙箱外同一工作树重跑通过。远端运行 `30682454751` 揭示 CI 平台垫片未创建 Supabase 的 `extensions` schema；提交 `7ba9cb8bd83dbf5aa5b94713979edd44a1849c86` 补齐 schema、`pgcrypto` 与跨连接 `search_path`，本地标准 PostgreSQL 15 全量重放及远端运行 `30682780479` 均通过。 |

## R1-7E 已验收合同

- `user_rights_export_artifacts` 与权利请求一一绑定，保存精确 JSON 文本、标准化 schema 版本、字段 manifest、SHA-256、UTF-8 字节数和 7 天期限；过期先拒绝读取，再由受控 RPC 清空正文并保留元数据。
- `prepare_user_rights_export` 仅允许 `account.support.manage` 在身份已核验且请求已批准/处理中时调用；export 请求不能再通过通用处理 RPC 手工标为完成。
- 学生只得到本人安全档案和所选范围内的报名、考勤、提交及已发布成果；家长只得到家庭关系；staff/admin 只得到本人岗位。账号锁原因、绑定码、内部备注、跟进、监护人联系方式、审核备注、其他学生和未发布成果均排除。
- artifact 正文没有 authenticated 列级读取权限；主体只能通过 `download_user_rights_export` 取得并同时写入不可变下载事件，关联家长、其他学生和支持人员不能代取。
- 板书解析 WebP 属于 `operational/solution_record_webp`；浏览器对最终 Blob 计算 SHA-256 并等待领域 RPC 写入资源、操作者、字段 manifest 和字节数后才触发下载，不创建权利 artifact。
- 中英文账户安全页显示范围、hash、大小、到期、状态和下载次数；账户支持页只显示 artifact 元数据与运营审计，不显示正文。

## 下一施工边界

R1-8 按财务“默认关闭”合同施工：先核对关闭态是否同时移除路由入口、导航、work item、通知、指标和 job；若要启用，必须一次完成订单、收款、退款/冲正、学生账户、台账、对账、异常、权限和导出门。不得在 R1-8 提前执行 R1-15/R1-18 的生产清理。
