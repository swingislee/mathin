# 多人活动安排推定关联开发交付 · 2026-09-11

状态：开发库已应用，开发端已交付，待人工业务与视觉验收。机器检查通过，生产未写入，阶段与 Gate 保持原状态。

## 结果

产品负责人要求将 Base 第三轮剩余的一条多人活动安排按最大可能性匹配，并沿用仓库既有标签提示可能有误。四段安排现已分别关联四个现有线索档案，全部保留推定状态；四个首选均无并列候选。同名对象按独立到访日期、时段、年级和学服老师等证据排序，原记录署名也作为对应学员的证据。

360 的“查看原表资料与相关记录”优先展示该学员的一段安排，沿用现有 `Badge` 和“资料关联待核对”双语文案。原整条记录、各行原文和匹配依据继续保留；源片段保存独立目标，当前身份、学生年级和业务阶段保持原值。

新增来源片段支持学生或线索目标，线索日后确认到学生时可由现有档案入口继续读取。匹配逻辑与执行步骤见 [Base 业务资料整理](../../runbooks/base-business-field-organization.md#多名学员的活动安排)。

## 验证

- 应用迁移：`20260911002000_base_activity_source_links`，规范化 SHA-256 为 `a82533b552f5e79997bc1fa0050858d2d2f0b72991cf4f3da04caffd1eabd625`。本机主机、实际 Supabase origin、监听进程、Docker 网络与数据库标识核对通过。
- `2026-09-11T02:51:38Z` 应用完成，`02:52:34Z` 逐项再读与计划一致。新增四条片段，19 张既有业务表及 Base 版本资料的计数、指纹保持一致。
- 回滚事务覆盖来源原文防伪与不可覆盖、管理员／有范围教师／范围外教师／学生／匿名读取、直接写入拒绝、现有来源分页、个人片段隔离和线索转学生后的读取连续性；回滚后受影响函数定义、权限及新增表恢复。
- 匹配与既有标签的定向测试、TypeScript、受影响代码 ESLint 通过。固定开发管理员通过实际 RPC 逐个读取四个档案，目标、片段原文、业务显示与推定标签状态均一致；中英文线索页面 HTTP 响应正常。以上属于机器检查，人工验收单独记录。

含个人资料的清单与执行证据保存在 gitignored `.tmp/base-activity-associations/`。`review.md` 列出四条匹配结果、区分同名对象的联系方式、匹配依据与开发档案搜索入口。开发验收：打开 [线索表](http://192.168.5.213:3130/zh/dashboard/followups/leads)，点击姓名进入 360，再打开“查看原表资料与相关记录”。
