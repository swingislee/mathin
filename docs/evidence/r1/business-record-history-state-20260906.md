# 单家庭历史事实归入原业务表 · 2026-09-06

状态：开发端已交付，待业务与视觉验收。本次没有生产操作，不关闭阶段或发布 Gate。

该开发案例的 8 条领域事实已从试验结构复制到现有业务表，以 `record_state=historical` 标识；关联活动、报名与授课安排共形成 13 个规范业务对象。现有 Student 身份、原始来源和字段依据继续复用。

- SQL 合并演练及正式本机事务：逐字段一致；47 张受保护表的原有行数与内容指纹一致。合并演练回滚后新增 schema 不留残余，旧结构和资料保留。
- 权限与写入：管理员读取历史行；其他固定开发身份读取为零。历史修改、测评、报名、分班、活动分流 RPC 被拒绝，当前必填约束继续生效。
- 机器检查：受影响 TypeScript、ESLint、数据库生成类型摘要通过；测评、首联、分班和 Student 360 的 7 组定向回归通过。
- 实际 API 与 HTTP：直接从 7 张原业务表查询历史状态，业务字段与原计划一致；管理员 zh/en 的原测评、首联、续费、分班、活动页面可检索，学生档案保留全部业务内容。当前筛选不显示历史行；员工无历史数据，匿名访问受保护页跳转登录。
- 首联缺项由真实 Student 的后续历史事实形成查询占位，未生成 Lead、沟通事件或今日工作。

产品负责人随后明确授权删除五张旧试验表。独立清理迁移先完成全部 8 条副本与现有业务字段的核对、删除后备份恢复一致性及事务回滚演练，再提交清理。五张旧表已移除，25 张现有业务、身份与来源表的内容指纹保持一致；13 个已合并业务对象和原始来源完整保留。

清理后，五个原业务工作表、学生历史档案及学生跟进页再次读取成功，关键业务内容保持完整。生成类型已去除旧表定义，与当前迁移摘要一致；受影响类型与最终 ESLint 检查通过。

随后按产品负责人提出的组件复用要求，五个工作表的历史专用整行布局归入原组件。当前与历史共用数据行、字段、详情和筛选排序；只读能力及缺失事实由组件参数表达。首联缺失项接入同一工作列表，当前列表为空时也保留已授权的历史查询结果。

该组件增量的受影响 TypeScript、ESLint 和定向渲染、字段、键盘合同检查通过；五个开发工作表实际 HTTP 读取及关键历史事实检查通过。当前首联与历史首联使用同一行和字段实现，分班共用班级列及学生磁贴，历史记录继续保持只读。维护入口已登记在运行手册，并由前端规则统一约束；本轮仍待人工业务与视觉验收。

备份、原资料与完整核对报告位于 gitignored `.tmp/business-record-history/`，只限本机管理员访问。共享证据不含姓名、联系方式、原文或本机标识。

人工检查流程见 [历史状态运行手册](../../runbooks/business-record-history-state.md)。

用户随后反馈测评展开区冗余、跨工作表备注重复及学生 360 历程为空。本次精简历史测评为一份反馈，首联缺失项只显示说明；历史档案按业务与来源字段整理沟通归属。360 复用现有业务历史读取，并使用原时间线组件显示已知日期及未知日期。

受影响类型、ESLint 和四组定向回归通过；本机管理员通过实际 `getStudent360Action` 读取到完整案例的五个业务节点，测评反馈去重、两次报名金额及完整续班原文核对通过，活动和沟通的未知发生日期仍为空。首联实际页面读取未再携带其他业务备注。原始资料与业务数据保持不变；本次没有生产操作，仍待人工视觉与业务验收。该增量的私有核对报告位于 gitignored `.tmp/business-record-feedback/`。

2026-09-07，按产品负责人提出的导入数据纠错要求，为活动、测评、历史报名／授课安排、续班及沟通增加共用修订入口。历史状态继续区分运营用途；修订直接更新原业务事实，同时保留来源与不可修改的前后值记录。此前的“历史修改拒绝”只读规则由该管理员修订能力补充，普通运营 RPC 的状态边界继续有效。

本机新增 migration `20260906002300`、`20260906002400` 已应用。五类保存及再次读取、管理员／非管理员、旧版本冲突、字段白名单、审计不可改、关联失败整体回滚、日期与金额精度检查通过；演练中的修订全部回滚，25 张关键表的既有字段指纹一致。实际 HTTP 验证五个页面的修订入口及五类 Action 读取成功，旧版本与未改变内容均返回明确提示，未产生业务修订记录。受影响类型、ESLint、输入授权及反馈同步的定向测试通过；兼容原导入 ID，并确认从沟通历史进入同一测评反馈修订。完整私有结果保存在 gitignored `.tmp/business-record-revisions/`；开发端已交付，待人工视觉与交互验收。本增量未操作生产，也未关闭阶段或正式 Gate。
