# 2026-09-20 白板自定义颜色

状态：开发端已交付，待人工视觉与书写验收。当前阶段保持 R1-Live-2，生产未部署。

“更多颜色”保留原有颜色，增加浏览器原生取色器、HEX 输入和“使用此颜色”。输入支持三位或六位 HEX，确认时统一为六位小写；输入非法时提示并禁止应用。编辑过程只更新弹层草稿，确认后应用一次，避免拖动取色器时反复广播对象改色。工具栏显示当前自定义色色点，图形填充使用同一控件；中英文标签完整，弹层色块及取色控件触控目标为 44 px。

常用色继续保存主题 token，自定义色以精确 RGB 保存。描边、填充、选中对象改色、撤销、远端操作、活动笔迹、checkpoint、备课批注、解析归档以及 Canvas/SVG/图片导出沿用共享颜色解析。主、副板书的画笔颜色继续沿用既有工具态同步。此次没有修改 PF 轮廓、压力计算或线宽设置。

数据库迁移 `20260920120000_whiteboard_custom_colors.sql` 扩展内部批注校验，接受七种 token 或六位 HEX，并补齐前端现有的 `brush` / `samples` 校验，保留笔刷版本、采样时间和压力。样本必须与点列等长、时间非负且递增或相等、压力为 null 或 0～1；内部函数权限继续对匿名及登录角色关闭，业务保存仍走原受保护 RPC。

验证：

- 颜色边界、checkpoint、压力笔迹、教师备课、双语与课堂表面的六个定向测试文件通过；覆盖精确颜色及压力回放、远端改色、撤销、主题色兼容、非法色值拒绝。
- 全库 TypeScript、涉及文件的 ESLint 通过。
- 共享画布输入检查通过，覆盖局域网 HTTP 缺少 `randomUUID` 时的书写路径。
- 本机目标核对通过：应用使用回环 Supabase origin，开发服务和数据库网关监听进程、Docker 网络及数据库实例标识均符合本地目标。未操作生产。
- `node scripts/whiteboard-colors-local.mjs --check` 在事务内替换校验函数，验证颜色、压力元数据、非法输入及内部权限，通过后回滚；原函数定义和 ACL 指纹保持一致。
- `--apply` 重新核对本机目标、函数指纹、迁移及断言校验和后应用；事务内复验通过并登记迁移账本。业务表未写入，开发服务未重启。
- 开发端健康接口返回 `200 / ok / development`；课堂入口未登录请求按预期跳转登录。未执行浏览器自动操作或截图验收。

迁移规范化 SHA-256：`7812d235ede95c06827f8a01d6e7f63847b503ee9d7682418af30fb573e387e7`。

人工验收：在课堂“更多颜色”中选择颜色或输入 HEX，确认后分别在主、副板书书写；切回常用色，对比自定义色显示；检查图形填充、改色撤销、翻页/重新进入后的笔迹及图片导出。当前页面有未保存笔迹时，先确认已保存再刷新。
