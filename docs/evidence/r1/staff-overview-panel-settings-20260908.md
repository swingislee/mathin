# 总览区块展示设置与保存修复

状态：开发端已交付，待人工视觉与交互验收。

- 学服、老师参与、班级容量各自在区块标题旁提供“展示设置”。学服和老师参与分别选择展示名单；班级容量分别选择老师和年级。
- 三个区块独立保存当前浏览器、当前账号的选择，提供搜索、全选、清空和恢复默认。选择影响明细展示，机构汇总保持完整。
- 修复来源学服标识在 Cookie 读取时被解码、导致名单恢复默认的问题。新格式分离传输编码和对象标识，并兼容此前保存的名单。
- 保存后确认浏览器已保留设置，再显示成功提示；同一区块的多项设置保存失败时恢复原值并提示重试。
- 来源署名保留在展示目录中，切换周期时继续可选；个别选项失效时保留其余选择。空名单也能保存。

验证：17 项相关单测通过，覆盖实际 Next Cookie 解码、旧名单兼容、区块与账号隔离、空名单、失败保存，以及机构汇总和不可用数据保持原意。受影响文件 ESLint、定向 TypeScript 通过。实际 HTTP 验证完成默认页面、中文保存后重载、英文重载、历史周切换和空名单重载共 5 个请求：三个标题内均有设置入口，老师／年级筛选和来源学服名单保持，机构合计不因展示选择改变。

本批只调整展示与浏览器偏好，无数据库迁移或业务数据写入。私有 HTTP 结果位于 `.tmp/staff-overview-panel-settings/http-result.json`，仅供本机项目维护者复核，保留 30 天。自动检查覆盖保存和渲染合同，视觉及实际点击手感由产品负责人在开发页验收。
