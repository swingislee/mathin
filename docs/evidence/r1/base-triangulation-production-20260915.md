# Base、开发库与生产库独立核查及生产增量 · 2026-09-15

状态：**生产已提交，机器复核通过，待一线业务核对**。本批只更新业务资料，沿用生产现有问题标注字段；阶段仍为 R1-Live-2。

## 授权与对照范围

用户要求深查三方数据、区分测试种子与真实资料，并按最可能信息导入生产、显式标注问题。用户随后选择“仅在各自环境独立核查”。开发资料在 Windows 本机分析，生产资料在 Xiaomi 内分析；未上传开发库个人明细。以用户提供 Base 的表 ID 与来源行 ID 对照计数和匹配状态。

Base 为 9 月 14 日版，SHA-256 `60c36cf906ec15fc6d3f4d4dc515d34287db391c83b6ea631efbb359a01fe6dd`，5,256 行。生产每行都有原档；开发缺少本次新增的 46 行。开发学生 3,670、线索 4,001；生产写前学生 1,469、线索 3,941。数量差不能解释为应搬入的学生差额。

## 真实来源与测试资料

- 开发 2,220 份 `Lead first contact` 档案全部关联到线索，且全部能追到 Base 来源。这些是开发首联操作形成的真实资料投影，不能直接认定为虚构种子，也不作为整体迁移依据。
- 两库在本次明确列举的运营种子来源前缀中均未命中学生。该结论只覆盖已登记前缀，不能证明两库不存在其他测试资料。
- 班级已有显式 `purpose=test`：开发 3 个、生产 2 个。正式在读继续按生产用途及当前有效班级统计。
- 开发有 4 份匹配 Base 姓名的档案备注出现测试相关字样，说明不能只按关键词清理。生产 3 份缺少 Base 同名依据且含测试字样的档案，使用现有 `students.remark` 标记“性质待核”；其中 1 份已有正式在读关系，继续保留。
- 本次身份合并、账号创建、测试清理和删除均为 0。

## 本次生产结果

| 对象 | 新增 | 更新 | 说明 |
| --- | ---: | ---: | --- |
| 学生 | 1 | 7 | 新档案取秋季原表姓名、年级；已有档案只补问题备注 |
| 正式成员 | 4 | 0 | 原有 171 条逐行保留；同组及排课依据写入成员备注 |
| 线索 | 1 | 98 | 新线索保留同名身份歧义；98 条既有线索补日期问题 |
| 活动／登记 | 2／2 | 0／1 | 补 2 条已确认诺访；另 1 条既有登记按来源推定归属 |
| 课程报名 | 0 | 6 | 关联、成员办理说明及重复投影标记 |
| 报名分班描述 | 0 | 5 | 4 条关联新增正式成员；1 条重复投影保留历史 |
| 来源推定关联 | 3 | 0 | 沿用关联表与事件审计，未写成一线已确认 |

共 127 项显式操作及 3 条来源关联；现有触发器新增 5 条业务参与关系、各 1 条学期／学年年级记录。原档仍为 15,433 份、结构化资料仍为 16,249 份；班级 34、课次 429、考勤 1、跟进 200 均保持。

当前正式在读 **159 → 163**；Base 在读 161 条均有对应正式成员，生产额外保留 2 人：一人原表在读标签为空，另一人性质待核。9 月诺访 **60 → 62**，新增只认定已确认诺访，不据此认定到场或完成测评。

## 重点处理依据

| Base 来源行 | 处理 |
| --- | --- |
| `recvuIkKpXuUG8` | 两库均无同名档案；按秋季原表新建，3 条同组在读来源指向唯一生产班级，15 个排课开始时间匹配；身份、电话和班级均待老师核对 |
| `recvuTcUxnBq53` | 沿用生产已存在的推定身份；同组来源指向唯一生产班级，15 个排课开始时间匹配，补正式成员 |
| `recvueRLMIBZ97`、`recvup6NEqXKGk` | 同老师同七年级唯一生产班级，现有排课周次和开始时间匹配；分别补成员。前者开发库已有正式关系，后者开发无同名学生 |
| `recvv5GlzoPUO6` | 姓名空，按唯一电话关联的生产学生推定；显式写明原表六年级、关联学生二年级以及可能同家庭不同孩子，待核对 |
| `recvuGqZ5kyQL1` | 无电话，生产 2 个同名候选均不能唯一定位；建立独立待核线索及诺访，保留学校、性别、年级，不绑定同名档案 |
| `recvqb1cMl69LS` | 与 `recvqb1decmpl6` 原表姓名和号码相同；关联到后者已有在读身份，保留推定状态；未合并学生或认领账号 |
| `recvuNyzoBWNxn` | 与旧行 `recvueIGksnghc` 是同一学生、同一秋季报名。旧报名已有机会及后续办理记录，保留为当前；新行的重复来源投影转历史。两版原档、旧报名业务及正式班级保留，时段差异注明待核对 |

3 条 Base 一年级／生产二年级差异、1 条在读空标签差异、3 份性质待核档案均补既有学生备注。此前用户确认的姓名更正及两人的生产班级保留规则继续保持。

## 标注字段与未改动的读取问题

使用生产现有 `history_import_associations.match_state=inferred`、`match_reason` 与关联事件；可阅读的原因使用 `students.remark`、`leads.note`、`enrollments.remark`、报名／分班的 `note`、活动 `remark` 和登记 `outcome`。统一正文前缀为“【资料待核对 2026-09-15】”。没有新增数据库字段或迁移。

8 月 95 条和 9 月 3 条获客日期问题均找到现有沟通／线索链路，98 条线索分别追加原因。保留原文、日期精度及既有备注。27 条完整日期的单数字小时兼容、57 条月日推断条件、10 条空日期、历史年份与紧凑年月问题分开说明。

获客总览固定旧文件入口与单数字小时解析仍属于应用读取缺陷；本次未发布应用代码，补资料和问题备注不代表获客大屏已经修复。9 月 17 条新增获客此前已经导入，本次未重复新建。

## 备份、演练和持久复读

目标配置、运行进程、监听与指纹重新核对：Xiaomi / `https://supabase.mathin.club`，PID 3825682，current `20260913-155934` / `3d9892bb87c2361e6662e685b606ac77a3e82e29`。账本 385，未新增迁移或重启服务。

备份：`/home/swing/services/mathin/backups/mathin-db-prechange-20260914T163900Z-base-triangulation-20260915`，350656252 字节，TOC 6504 行；SHA-256 `c99802fa0337555a4d29a1e076db81f1ba076f7ada318e9f6336fa068a587edf`。同一 SERIALIZABLE 事务先完整演练再回滚，新连接核对零残留后提交。第一次演练暴露新增线索缺少规范化姓名；回滚无残留，补字段后重跑通过。复用经摘要确认不变的备份，另存修订计划绑定记录。

最终事务 SHA-256 `4358909a4e93f7b85488c1ac5f372c2356452b7bf8ed0b4bc240f89f49d5774f`；计划 SHA-256 `5c2f34fce9115c1a2a36ecdb3de88272ff655e3b1ed01f0b3f94b9a529070774`。39 张表纳入写前／写后摘要，既有行除精确更新清单外逐行保护。正式提交后新连接逐项复读 127 个操作通过；现有管理员 authenticated RPC 返回 3 个推定徽标。当前成员 163、独立学生 163、9 月诺访 62、重复报名当前／历史各 1；健康通过、提交后业务错误新增 0。

机器数据与接口复核通过，身份和班级的真实业务确认仍由一线完成。若需回退，使用备份、精确操作清单及后续业务变化核对后执行，不将正式资料直接清理。

[机器汇总](base-triangulation-production-20260915.json)，换行归一化 SHA-256：`6d8a2bf90d0aaa9f7f0ba631a81956f057219816fce1cd77caeb7241b5993333`。本机具名清单在 gitignored `.tmp/base-triangulation-20260915/result-detail.md`。生产现场明细、SQL、前后快照及日志保存在 `/home/swing/services/mathin/staging/base-incremental-20260914/.tmp/base-incremental-20260914/tri-*`，至少 90 天，仅管理员和获授权执行人访问。开发个人资料仅保存在本机私有目录。
