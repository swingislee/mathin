# 单学生业务历史转换验证 · 2026-09-06

状态：**开发端已交付，待人工业务与视觉验收**。本次没有生产操作。

产品负责人要求把历史原表拆分后融入现有业务入口。本次复用已确认案例的既有 Student 及已导入原文，将四种来源拆为 8 条结构化业务历史：续费 2、活动参与 1、测评 1、报名 2、沟通 2。另四处来源保留用于身份、档案或班级核对。

历史续费只表达原文中对应期别的意向，最终结果保持未知。竞赛报名与结果来源分开，沟通中的奖项显示届次待核对。历史金额保持原报名资料性质；未知日期、作者和分数均未补造。

## 机器检查

- 本机 host、Supabase origin、监听进程、Docker 网络及 PostgreSQL system identifier 重新核对通过。
- 独立迁移 `20260906001000_student_business_history` 已保存 schema 备份；schema + 实际转换样本在同一事务内通过管理员读取、其他身份不可读、API 写权限拒绝和跨学生来源外键断言。rollback 后五张新增表均不存在，再提交 schema。
- 实际导入新增 8 条；同一计划第二次运行新增 0 条。两次事务均核对 40 张受保护表的行数与完整内容指纹相等，包括当前业务、工作清单和原始档案。
- 定向业务转换单测 8 项通过，覆盖期别拆分、金额与课表列组边界、同名资料隔离、未知日期、原等级、奖项证据及稳定标识。
- 受影响 TypeScript、ESLint、数据库生成类型摘要和 diff 文本检查通过。
- 固定开发管理员经真实 Supabase API 读取五类历史，逐列与转换计划一致；普通学辅经相同 API 读取均为空。
- 中文学生历史总览、各业务分类、跟进页、续费／活动历史入口、搜索空结果和当前页面入口均返回 200 并包含对应内容；英文三个主要入口同样通过。
- 普通学辅历史入口显示无权限且无样本内容；匿名学生历史请求重定向且不泄露内容。

以上为其覆盖范围内的机器检查。未运行浏览器截图或视觉替代验收；工作流适用性、业务归类和实际阅读体验等待产品负责人按交付链接验收。

## 受控证据与范围

私有计划、schema 备份、两次导入报告、RLS／rollback 结果及 HTTP 检查保存于 gitignored `.tmp/student-business-history-trial/`。仓库不保存学生姓名、手机号、原文或页面 HTML。

执行与验收步骤见 [单案例 runbook](../../runbooks/student-business-history-trial.md)。本增量限定一个案例与已知来源配方，没有全量导入、生产发布、身份属性更正或阶段 Gate 关闭。
