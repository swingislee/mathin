# 2026-09-08 首批生产员工与教室目录

> 状态：**生产写入完成，定向读取与身份合同核验通过，待员工首次登录及产品人工验收**。产品负责人要求先审核名单再写入，并在审核后确认两个既有账号的对应关系及三名补充员工的岗位。本批为正式业务初始化，不关闭 R1-Live Gate 2 或 Production 1.0 Gate。

## 执行范围

| 对象 | 执行结果 |
| --- | --- |
| 员工名单 | 28 名现有员工；新增 26 个 Auth 账号及员工档案，2 人在原邮箱账号上绑定手机号 |
| 补充员工 | 2 名学服（`sales`）和 1 名教师（`teacher`），使用审核名单中的三枚内部虚拟占位号 |
| 来源占位号码 | 2 名真实员工沿用原表中的占位号码，号码性质保留在受控交接清单 |
| 排除范围 | 27 条占位老师与历史老师不建立账号；未明确当前实名的兼职称呼留待后续批次 |
| 校区 | 复用紫辰阁、利港两个启用校区及既有完整地址 |
| 教室 | 紫辰阁新增 Z311，利港新增审核的 16 间 L 编号教室；新增 17 间，启用教室共 31 间 |
| 现有场地 | 原有 15 条教室记录保持完整，其中 14 间启用、1 间停用；原有校区记录不变 |
| 本批暂缓 | 临滨 B 座、合工大 CEICC，以及利港数字教室 6–11，等待名称、用途及对应关系明确 |

员工开户复用 `preview_staff_account_import`、`prepare_staff_import_account`、Supabase Auth Admin 创建及 `finalize_staff_import_account`。26 行导入批次为 `completed`，错误数为 0。岗位沿用现有来源映射，校长对应 `principal`；前台、财务、直播主管、教学管理等未单独映射的来源标签留存在受控来源记录，不据此新增岗位或权限。

两个既有账号使用 Auth Admin 追加手机号 identity，保持原 UUID、邮箱、密码、员工档案和岗位；绑定使用一次性员工身份邀请及 assurance 记录，`provider_verified=false`。整个批次没有发送短信，也没有将占位号码标记为短信服务商验证通过。新增账号具备独立初始密码，首次改密前本人 staff 权限保持关闭；档案可由有权管理者用于排课。

教室通过 `create_campus_room_v2` 在同一事务内创建，来源没有容量的新增教室保持容量为空。既有教室名称、容量及业务引用保持完整。

## 生产目标与核验

| 核验 | 结果 |
| --- | --- |
| 执行目标 | SSH `xiaomi`；实际 Supabase origin 为 `https://supabase.mathin.club` |
| 数据库指纹 | `10e3f97e32b018403c9074efa4e258d699530a487c47de89b5d307ab7ff21a0c` |
| 应用 | current `20260908-053849`；`mathin.service` 用户服务 active，监听 `127.0.0.1:3131` |
| 数据库 schema | ledger 356，head `20260908140000_student_record_unassigned_access`；本批没有 migration |
| Auth / profiles | `14 → 40`；原账号密码摘要、邮箱、顶层角色及原 profile 字段保持一致 |
| 员工目录 | 管理员 `list_staff_members()` 读取 36 个启用员工/管理员；其中本批涉及 28 人，其他既有账号保留 |
| 既有账号绑定 | 两个账号均同时具有 `email`、`phone` identities，UUID 与原账号相同 |
| 新账号 | 26 人的姓名、手机号、岗位、`password_change_required=true` 逐人核对通过；Auth 临时注册密码元数据键均已清除 |
| 权限门 | 三名补充员工在各自授权上下文中，首次改密前 `is_staff`、学生及排课权限均按预期关闭 |
| 既有数据 | 原岗位、MFA、校区及原教室记录一致；学生、班级、课次、报名及现存考勤表的计数和字段摘要一致 |
| 运行健康 | `/api/health` 返回 production / ok；`operational_errors` 为 1962，与写前一致 |

本批复用已发布的开户与目录合同，执行只读 preflight、实际写入及定向 postflight。按会话中产品负责人指令，没有追加备份、迁移回滚演练、全量构建或全量回归。机器结果未替代真实员工登录、岗位工作流及人工视觉验收。

## 受控产物

生产受控目录为 `/home/swing/services/mathin/staging/directory-20260908/`，目录权限 `0700`；身份明细、执行状态和初始密码交接文件仅向生产运维角色开放。初始密码交接文件权限为 `0600`，凭据不进入仓库、日志或本证据。业务来源及执行证据按 R1 受控产物保留政策管理，密码文件由管理员在交接后按凭据管理流程处理。

| 产物 | SHA-256 |
| --- | --- |
| `approved-payload.json` | `1e512a355ac01cf7eddd45df9ce620e40f206a783285e9b7a42521490f30e0df` |
| `execute-directory.py` | `cd2d8900bbe8c96d9ea44f279f6ff0ee6cdc0b5d936d4a368d39b348e55e146e` |
| `result.json` | `ee5878d8e2b512cb09c80b96cfafd58cb8e66442a13b23541c043403918803a6` |

以上文件以 UTF-8 / LF 保存，摘要与规范化文本一致。仓库只保留本份去标识化摘要；完整员工名单、联系方式和凭据留在受控目录。
